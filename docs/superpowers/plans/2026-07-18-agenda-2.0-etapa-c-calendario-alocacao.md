# Etapa C — Calendário + Alocação — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Substituir o form de UUID cru por uma jornada select-first com calendário semanal de duas visões (terapeuta/paciente), criando regras recorrentes e sessões avulsas com detecção de conflito.

**Architecture:** Lógica pura em `src/lib/agenda/*` (sem banco, 100% testável). Leitura/escrita ctx-accepting em `agenda/queries.ts` (fora de `use server`). Server Actions finas em `agenda/semana/actions.ts`. UI nova em `agenda/semana/*` reusando exclusivamente o design system `src/components/ui/*`. Etapa C **não** materializa ocorrências (Etapa D) nem calcula métricas (Etapa F).

**Tech Stack:** Next.js 16 (App Router, Server Components), React 19, Drizzle+Postgres, Better Auth, Tailwind v4, Radix (via DS), Vitest, axe-core.

## Global Constraints

- RLS multi-tenant `WITH CHECK` em toda leitura/escrita; toda query passa por `withTenant(ctx, fn)`.
- Anti-IDOR paciente: FK composta `(patientId, clinicId)` → `patient` (`uq_patient_id_clinic`). Terapeuta: tenant via `app_user_in_clinic()` no RLS (não FK composta).
- ctx-accepting fora de `use server` em `queries.ts`; actions só wrappers (evita RPC forjável — commit 087780d).
- **NUNCA componente hardcoded** — reusar `src/components/ui/*`. Não existe Combobox/Popover/Command no DS: construir sobre Radix `Select`/`Dialog` existentes.
- a11y: axe wcag2a/aa com `color-contrast` **religado** nas telas novas.
- Intervalos meia-abertos `[início, fim)` em toda comparação de horário.
- C1: alocar regra recorrente grava **só** `agendamento_recorrente` (0 linhas `session`).
- C2/C5: conflito de regra é pré-check **app-level** (regra não tem linha `session`); EXCLUDE gist do banco é backstop TOCTOU só p/ avulsa.
- C7: `vigenciaInicio = max(segunda visível, segunda atual)`; alocação em semana passada **desabilitada**.
- C10: `criarAvulsa` ancora via `FUSO_CLINICA_OFFSET`; conversão de avulsas do banco p/ minutos-locais SP na leitura antes de comparar.
- Commits: Conventional Commits pt-BR. Branch: `feat/agenda-2.0-etapa-c`.
- Pré-execução: garantir branch + Postgres local reset+re-migrate (memory `dev-db-migrate-desync`). Baseline conhecido: 15 falhas `revisao/*` não são regressão.

---

### Task 1: Matemática de semana + vigência C7 (`src/lib/agenda/semana.ts`)

**Files:**
- Create: `src/lib/agenda/semana.ts`
- Test: `src/lib/agenda/semana.test.ts`

**Interfaces:**
- Consumes: nada (lógica pura sobre strings ISO `YYYY-MM-DD`).
- Produces:
  - `segundaDaSemana(dataISO: string): string`
  - `diasDaSemana(segundaISO: string): string[]` (7 ISO, seg→dom)
  - `vigenciaInicioC7(semanaVisivelISO: string, hojeISO: string): string`
  - `semanaEhPassada(semanaVisivelISO: string, hojeISO: string): boolean`

- [ ] **Step 1: Write the failing test**

```ts
// src/lib/agenda/semana.test.ts
import { describe, expect, test } from "vitest";
import {
  diasDaSemana,
  segundaDaSemana,
  semanaEhPassada,
  vigenciaInicioC7,
} from "./semana";

describe("segundaDaSemana", () => {
  test("quarta-feira volta para a segunda da mesma semana", () => {
    expect(segundaDaSemana("2026-07-15")).toBe("2026-07-13"); // qua → seg
  });
  test("domingo volta para a segunda anterior (semana ISO)", () => {
    expect(segundaDaSemana("2026-07-19")).toBe("2026-07-13"); // dom → seg
  });
  test("segunda é idempotente", () => {
    expect(segundaDaSemana("2026-07-13")).toBe("2026-07-13");
  });
});

describe("diasDaSemana", () => {
  test("gera 7 dias começando na segunda", () => {
    expect(diasDaSemana("2026-07-13")).toEqual([
      "2026-07-13", "2026-07-14", "2026-07-15", "2026-07-16",
      "2026-07-17", "2026-07-18", "2026-07-19",
    ]);
  });
});

describe("vigenciaInicioC7", () => {
  test("semana futura usa a própria segunda", () => {
    expect(vigenciaInicioC7("2026-07-22", "2026-07-13")).toBe("2026-07-20");
  });
  test("semana atual usa a segunda atual", () => {
    expect(vigenciaInicioC7("2026-07-15", "2026-07-15")).toBe("2026-07-13");
  });
});

describe("semanaEhPassada", () => {
  test("semana anterior à atual é passada", () => {
    expect(semanaEhPassada("2026-07-06", "2026-07-15")).toBe(true);
  });
  test("semana atual não é passada", () => {
    expect(semanaEhPassada("2026-07-15", "2026-07-15")).toBe(false);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test src/lib/agenda/semana.test.ts`
Expected: FAIL — `Cannot find module './semana'`.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/lib/agenda/semana.ts
// Aritmética de datas em UTC para não sofrer deslocamento de DST — opera
// sobre strings ISO date-only (YYYY-MM-DD), nunca sobre horário local.

function paraUTC(dataISO: string): Date {
  return new Date(`${dataISO}T00:00:00Z`);
}

/** Segunda-feira da semana que contém `dataISO` (semana ISO, seg→dom). */
export function segundaDaSemana(dataISO: string): string {
  const d = paraUTC(dataISO);
  const dow = d.getUTCDay(); // 0=domingo … 6=sábado
  const diff = dow === 0 ? -6 : 1 - dow;
  d.setUTCDate(d.getUTCDate() + diff);
  return d.toISOString().slice(0, 10);
}

/** 7 datas ISO a partir de uma segunda (inclusive), seg→dom. */
export function diasDaSemana(segundaISO: string): string[] {
  const base = paraUTC(segundaISO);
  return Array.from({ length: 7 }, (_, i) => {
    const d = new Date(base);
    d.setUTCDate(d.getUTCDate() + i);
    return d.toISOString().slice(0, 10);
  });
}

/** C7: vigência nunca começa no passado — max(segunda visível, segunda atual). */
export function vigenciaInicioC7(semanaVisivelISO: string, hojeISO: string): string {
  const segVisivel = segundaDaSemana(semanaVisivelISO);
  const segAtual = segundaDaSemana(hojeISO);
  return segVisivel > segAtual ? segVisivel : segAtual;
}

/** C7: alocação em semana passada é desabilitada na UI. */
export function semanaEhPassada(semanaVisivelISO: string, hojeISO: string): boolean {
  return segundaDaSemana(semanaVisivelISO) < segundaDaSemana(hojeISO);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test src/lib/agenda/semana.test.ts`
Expected: PASS (todos os describes verdes).

- [ ] **Step 5: Commit**

```bash
git add src/lib/agenda/semana.ts src/lib/agenda/semana.test.ts
git commit -m "feat(agenda): matematica de semana + vigencia C7 (etapa C)"
```

---

### Task 2: Detecção de conflito (`src/lib/agenda/conflito.ts`)

**Files:**
- Create: `src/lib/agenda/conflito.ts`
- Test: `src/lib/agenda/conflito.test.ts`

**Interfaces:**
- Consumes: nada (minutos locais desde a meia-noite).
- Produces:
  - `interface Intervalo { inicioMin: number; fimMin: number }`
  - `sobrepoe(a: Intervalo, b: Intervalo): boolean` (meia-aberto `[ini,fim)`)
  - `interface Slot { diaSemana: number; inicioMin: number; fimMin: number }`
  - `conflita(nova: Slot, existentes: Slot[]): boolean`
- Nota de uso (C5): o chamador testa **2 dimensões** — `conflita(slot, ocupacaoTerapeuta) || conflita(slot, ocupacaoPaciente)`. Conflito = terapeuta ocupado **ou** paciente ocupado no slot.

- [ ] **Step 1: Write the failing test**

```ts
// src/lib/agenda/conflito.test.ts
import { describe, expect, test } from "vitest";
import { conflita, sobrepoe, type Slot } from "./conflito";

describe("sobrepoe (meia-aberto [ini,fim))", () => {
  test("intervalos que se cruzam sobrepõem", () => {
    expect(sobrepoe({ inicioMin: 60, fimMin: 120 }, { inicioMin: 90, fimMin: 150 })).toBe(true);
  });
  test("adjacentes NÃO sobrepõem (fim exclusivo)", () => {
    expect(sobrepoe({ inicioMin: 60, fimMin: 120 }, { inicioMin: 120, fimMin: 180 })).toBe(false);
  });
  test("disjuntos não sobrepõem", () => {
    expect(sobrepoe({ inicioMin: 60, fimMin: 120 }, { inicioMin: 200, fimMin: 260 })).toBe(false);
  });
});

describe("conflita", () => {
  const existentes: Slot[] = [
    { diaSemana: 1, inicioMin: 540, fimMin: 600 }, // seg 09:00–10:00
  ];
  test("mesmo dia com sobreposição conflita", () => {
    expect(conflita({ diaSemana: 1, inicioMin: 570, fimMin: 630 }, existentes)).toBe(true);
  });
  test("mesmo dia adjacente NÃO conflita", () => {
    expect(conflita({ diaSemana: 1, inicioMin: 600, fimMin: 660 }, existentes)).toBe(false);
  });
  test("dia diferente não conflita", () => {
    expect(conflita({ diaSemana: 2, inicioMin: 570, fimMin: 630 }, existentes)).toBe(false);
  });
  test("lista vazia não conflita", () => {
    expect(conflita({ diaSemana: 1, inicioMin: 540, fimMin: 600 }, [])).toBe(false);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test src/lib/agenda/conflito.test.ts`
Expected: FAIL — `Cannot find module './conflito'`.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/lib/agenda/conflito.ts
export interface Intervalo {
  inicioMin: number;
  fimMin: number;
}

/** Sobreposição meia-aberta [início, fim): adjacentes não colidem. */
export function sobrepoe(a: Intervalo, b: Intervalo): boolean {
  return a.inicioMin < b.fimMin && b.inicioMin < a.fimMin;
}

export interface Slot extends Intervalo {
  diaSemana: number;
}

/**
 * True se `nova` colide com alguma ocupação de UMA entidade (terapeuta OU
 * paciente). C5: o chamador roda para ambas dimensões e faz OR do resultado.
 */
export function conflita(nova: Slot, existentes: Slot[]): boolean {
  return existentes.some((e) => e.diaSemana === nova.diaSemana && sobrepoe(nova, e));
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test src/lib/agenda/conflito.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/agenda/conflito.ts src/lib/agenda/conflito.test.ts
git commit -m "feat(agenda): deteccao de conflito meia-aberta 2 dimensoes (etapa C)"
```

---

### Task 3: Projeção da semana para blocos (`src/lib/agenda/projecao.ts`)

**Files:**
- Create: `src/lib/agenda/projecao.ts`
- Test: `src/lib/agenda/projecao.test.ts`

**Interfaces:**
- Consumes: `horaParaMin(hhmm: string): number` de `./janela` (existente).
- Produces:
  - `type OrigemBloco = "previsto" | "concreto"`
  - `interface RegraProjecao { id; diaSemana; horaInicio; duracaoMin; disciplina; rotulo }`
  - `interface AvulsaProjecao { id; diaSemana; inicioMin; duracaoMin; disciplina; rotulo }`
  - `interface BlocoAgenda { id; origem: OrigemBloco; diaSemana; inicioMin; duracaoMin; disciplina; rotulo }`
  - `projetarSemana(regras: RegraProjecao[], avulsas: AvulsaProjecao[]): BlocoAgenda[]`
- Nota (C9): regra → `origem:"previsto"`; avulsa → `origem:"concreto"` (a UI pinta diferente). Avulsas já chegam com `inicioMin` em minutos-locais SP (conversão C10 feita na query).

- [ ] **Step 1: Write the failing test**

```ts
// src/lib/agenda/projecao.test.ts
import { describe, expect, test } from "vitest";
import { projetarSemana, type AvulsaProjecao, type RegraProjecao } from "./projecao";

const regra: RegraProjecao = {
  id: "r1", diaSemana: 1, horaInicio: "09:00", duracaoMin: 60,
  disciplina: "aba", rotulo: "João",
};
const avulsa: AvulsaProjecao = {
  id: "a1", diaSemana: 1, inicioMin: 480, duracaoMin: 50,
  disciplina: "to", rotulo: "Maria",
};

describe("projetarSemana", () => {
  test("regra vira bloco previsto com inicioMin derivado da hora", () => {
    const [bloco] = projetarSemana([regra], []);
    expect(bloco).toMatchObject({ id: "r1", origem: "previsto", inicioMin: 540, duracaoMin: 60 });
  });
  test("avulsa vira bloco concreto preservando inicioMin", () => {
    const [bloco] = projetarSemana([], [avulsa]);
    expect(bloco).toMatchObject({ id: "a1", origem: "concreto", inicioMin: 480 });
  });
  test("ordena por dia e depois por inicioMin", () => {
    const blocos = projetarSemana([regra], [avulsa]);
    expect(blocos.map((b) => b.id)).toEqual(["a1", "r1"]); // 480 antes de 540
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test src/lib/agenda/projecao.test.ts`
Expected: FAIL — `Cannot find module './projecao'`.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/lib/agenda/projecao.ts
import { horaParaMin } from "./janela";

export type OrigemBloco = "previsto" | "concreto";

export interface RegraProjecao {
  id: string;
  diaSemana: number;
  horaInicio: string; // "HH:MM" (hora crua da regra)
  duracaoMin: number;
  disciplina: string;
  rotulo: string; // paciente ou terapeuta, conforme o eixo
}

export interface AvulsaProjecao {
  id: string;
  diaSemana: number;
  inicioMin: number; // minutos-locais SP (já convertido na query, C10)
  duracaoMin: number;
  disciplina: string;
  rotulo: string;
}

export interface BlocoAgenda {
  id: string;
  origem: OrigemBloco;
  diaSemana: number;
  inicioMin: number;
  duracaoMin: number;
  disciplina: string;
  rotulo: string;
}

/** Unifica regras (previsto) e avulsas (concreto) em blocos ordenados. */
export function projetarSemana(
  regras: RegraProjecao[],
  avulsas: AvulsaProjecao[],
): BlocoAgenda[] {
  const previstos: BlocoAgenda[] = regras.map((r) => ({
    id: r.id,
    origem: "previsto",
    diaSemana: r.diaSemana,
    inicioMin: horaParaMin(r.horaInicio),
    duracaoMin: r.duracaoMin,
    disciplina: r.disciplina,
    rotulo: r.rotulo,
  }));
  const concretos: BlocoAgenda[] = avulsas.map((a) => ({
    id: a.id,
    origem: "concreto",
    diaSemana: a.diaSemana,
    inicioMin: a.inicioMin,
    duracaoMin: a.duracaoMin,
    disciplina: a.disciplina,
    rotulo: a.rotulo,
  }));
  return [...previstos, ...concretos].sort(
    (x, y) => x.diaSemana - y.diaSemana || x.inicioMin - y.inicioMin,
  );
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test src/lib/agenda/projecao.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/agenda/projecao.ts src/lib/agenda/projecao.test.ts
git commit -m "feat(agenda): projecao semana previsto/concreto (etapa C)"
```

---

### Task 4: Query `listarPacientes` (`agenda/queries.ts`)

**Files:**
- Modify: `src/app/(app)/agenda/queries.ts` (adicionar export; se o arquivo não existir ainda, criar seguindo o padrão ctx-accepting de `equipe/[id]/queries.ts`)
- Test: `db/tests/agenda2-listar-pacientes-rls.int.test.ts`

**Interfaces:**
- Consumes: `withTenant(ctx, fn)` (`@/db/rls`), `requireRole(ctx, ...papeis)` (`@/auth/require-role`), `TenantContext`, `schema.patient` (`@/db/schema`), drizzle `and/eq/ilike/asc`.
- Produces: `listarPacientes(ctx: TenantContext, termo: string): Promise<{ id: string; nome: string }[]>`
- Nota: gate coordenador (defesa em profundidade; RLS é a fronteira real). Consent LGPD é append-only sem revogação (spec §3.2) → sem filtro de consent aqui.

- [ ] **Step 1: Write the failing test**

```ts
// db/tests/agenda2-listar-pacientes-rls.int.test.ts
import { beforeAll, describe, expect, test, vi } from "vitest";
vi.mock("server-only", () => ({}));
import { and, eq, sql } from "drizzle-orm";
import { db } from "@/db/client";
import * as schema from "@/db/schema";
import { listarPacientes } from "@/app/(app)/agenda/queries";

const hasDb = !!process.env.DATABASE_URL;
const CLINIC_A = "00000000-0000-0000-0000-0000000000a1";
const CLINIC_B = "00000000-0000-0000-0000-0000000000b1";
const ctxA = { clinicId: CLINIC_A, userId: "00000000-0000-0000-0000-00000000c001", role: "coordenador" as const };

describe.skipIf(!hasDb)("listarPacientes (RLS/IDOR)", () => {
  beforeAll(async () => {
    await db.execute(sql`TRUNCATE ${schema.patient} CASCADE`);
    await db.insert(schema.patient).values([
      { id: "00000000-0000-0000-0000-0000000a0001", clinicId: CLINIC_A, nome: "Ana Alfa" },
      { id: "00000000-0000-0000-0000-0000000a0002", clinicId: CLINIC_A, nome: "Bruno Beta" },
      { id: "00000000-0000-0000-0000-0000000b0001", clinicId: CLINIC_B, nome: "Ana Outra Clinica" },
    ] as (typeof schema.patient.$inferInsert)[]);
  });

  test("só retorna pacientes do tenant do ctx (isolamento)", async () => {
    const r = await listarPacientes(ctxA, "Ana");
    expect(r.map((p) => p.nome)).toEqual(["Ana Alfa"]); // não vaza a clínica B
  });

  test("filtra por termo case-insensitive", async () => {
    const r = await listarPacientes(ctxA, "bruno");
    expect(r).toHaveLength(1);
    expect(r[0].nome).toBe("Bruno Beta");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test:rls db/tests/agenda2-listar-pacientes-rls.int.test.ts`
Expected: FAIL — `listarPacientes` não exportado.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/app/(app)/agenda/queries.ts  (adicionar imports no topo se faltarem)
import { and, asc, eq, ilike } from "drizzle-orm";
import { requireRole } from "@/auth/require-role";
import { withTenant, type TenantContext } from "@/db/rls";
import * as schema from "@/db/schema";

export async function listarPacientes(
  ctx: TenantContext,
  termo: string,
): Promise<{ id: string; nome: string }[]> {
  requireRole(ctx, "coordenador");
  return withTenant(ctx, (tx) =>
    tx
      .select({ id: schema.patient.id, nome: schema.patient.nome })
      .from(schema.patient)
      .where(
        and(
          eq(schema.patient.clinicId, ctx.clinicId),
          ilike(schema.patient.nome, `%${termo}%`),
        ),
      )
      .orderBy(asc(schema.patient.nome))
      .limit(20),
  );
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test:rls db/tests/agenda2-listar-pacientes-rls.int.test.ts`
Expected: PASS (isolamento + filtro).

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/agenda/queries.ts" db/tests/agenda2-listar-pacientes-rls.int.test.ts
git commit -m "feat(agenda): listarPacientes com gate coordenador + RLS (etapa C)"
```

---

### Task 5: Queries `carregarSemana` + `disponibilidadeTerapeutaNoDia` (`agenda/queries.ts`)

**Files:**
- Modify: `src/app/(app)/agenda/queries.ts`
- Create: `src/lib/agenda/fuso-min.ts` (conversão timestamptz → minutos-locais SP, C10) + `src/lib/agenda/fuso-min.test.ts`
- Test: `db/tests/agenda2-carregar-semana-rls.int.test.ts`

**Interfaces:**
- Consumes: `withTenant`, `TenantContext`, `schema.{agendamentoRecorrente,session,janelaTrabalho,bloqueio,patient,appUser,userRole}`, `projetarSemana`/`RegraProjecao`/`AvulsaProjecao` (Task 3), `FaixaDia` (`@/lib/agenda/janela`), `diasDaSemana` (Task 1), `FUSO_CLINICA` (`@/app/(app)/agenda/fuso`).
- Produces:
  - `src/lib/agenda/fuso-min.ts`: `paraMinutosLocais(instante: Date, fuso: string): { diaSemana: number; inicioMin: number }`
  - `interface SemanaCarregada { blocos: BlocoAgenda[]; janelas: FaixaDia[]; bloqueios: { dataInicio: string; dataFim: string }[] }`
  - `interface CarregarSemanaParams { eixo: "terapeuta" | "paciente"; entidadeId: string; semanaInicioISO: string }`
  - `carregarSemana(ctx: TenantContext, p: CarregarSemanaParams): Promise<SemanaCarregada>`
  - `disponibilidadeTerapeutaNoDia(ctx: TenantContext, terapeutaId: string, diaISO: string): Promise<FaixaDia[]>`

- [ ] **Step 1: Write the failing unit test da conversão (C10)**

```ts
// src/lib/agenda/fuso-min.test.ts
import { describe, expect, test } from "vitest";
import { paraMinutosLocais } from "./fuso-min";

describe("paraMinutosLocais (C10)", () => {
  test("timestamptz UTC vira minutos-locais SP (-03:00)", () => {
    // 2026-07-13T12:00:00Z = seg 09:00 em São Paulo
    const r = paraMinutosLocais(new Date("2026-07-13T12:00:00Z"), "America/Sao_Paulo");
    expect(r).toEqual({ diaSemana: 1, inicioMin: 540 });
  });
  test("vira o dia correto quando o UTC cruza meia-noite local", () => {
    // 2026-07-14T02:00:00Z = seg 23:00 em SP (ainda diaSemana 1)
    const r = paraMinutosLocais(new Date("2026-07-14T02:00:00Z"), "America/Sao_Paulo");
    expect(r).toEqual({ diaSemana: 1, inicioMin: 23 * 60 });
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test src/lib/agenda/fuso-min.test.ts`
Expected: FAIL — módulo inexistente.

- [ ] **Step 3: Implement `paraMinutosLocais`**

```ts
// src/lib/agenda/fuso-min.ts
// Converte um instante absoluto (timestamptz) para o dia-da-semana e minuto
// locais na zona da clínica, via Intl (respeita DST). C10: avulsas do banco
// precisam virar minutos-locais SP antes de comparar com regras (hora crua).

const DIA_INDEX: Record<string, number> = {
  Sun: 0, Mon: 1, Tue: 2, Wed: 3, Thu: 4, Fri: 5, Sat: 6,
};

export function paraMinutosLocais(
  instante: Date,
  fuso: string,
): { diaSemana: number; inicioMin: number } {
  const fmt = new Intl.DateTimeFormat("en-US", {
    timeZone: fuso,
    weekday: "short",
    hour: "2-digit",
    minute: "2-digit",
    hour12: false,
  });
  const partes = Object.fromEntries(
    fmt.formatToParts(instante).map((p) => [p.type, p.value]),
  );
  const diaSemana = DIA_INDEX[partes.weekday as string];
  const hora = Number(partes.hour === "24" ? "0" : partes.hour);
  const minuto = Number(partes.minute);
  return { diaSemana, inicioMin: hora * 60 + minuto };
}
```

- [ ] **Step 4: Run to verify it passes**

Run: `pnpm test src/lib/agenda/fuso-min.test.ts`
Expected: PASS.

- [ ] **Step 5: Write the failing integration test de `carregarSemana`**

```ts
// db/tests/agenda2-carregar-semana-rls.int.test.ts
import { beforeAll, describe, expect, test, vi } from "vitest";
vi.mock("server-only", () => ({}));
import { sql } from "drizzle-orm";
import { db } from "@/db/client";
import * as schema from "@/db/schema";
import { carregarSemana } from "@/app/(app)/agenda/queries";

const hasDb = !!process.env.DATABASE_URL;
const CLINIC_A = "00000000-0000-0000-0000-0000000000a1";
const TERA = "00000000-0000-0000-0000-00000000e001";
const PAC = "00000000-0000-0000-0000-0000000a0001";
const ctxA = { clinicId: CLINIC_A, userId: TERA, role: "coordenador" as const };

describe.skipIf(!hasDb)("carregarSemana", () => {
  beforeAll(async () => {
    await db.execute(sql`TRUNCATE ${schema.agendamentoRecorrente}, ${schema.session}, ${schema.patient} CASCADE`);
    await db.insert(schema.patient).values([
      { id: PAC, clinicId: CLINIC_A, nome: "Ana Alfa" },
    ] as (typeof schema.patient.$inferInsert)[]);
    await db.insert(schema.agendamentoRecorrente).values([
      {
        clinicId: CLINIC_A, patientId: PAC, terapeutaId: TERA, disciplina: "aba",
        diaSemana: 1, horaInicio: "09:00", duracaoMin: 60,
        vigenciaInicio: "2026-07-13", status: "ativo",
      },
    ] as (typeof schema.agendamentoRecorrente.$inferInsert)[]);
  });

  test("eixo terapeuta traz regra ativa como bloco previsto", async () => {
    const r = await carregarSemana(ctxA, {
      eixo: "terapeuta", entidadeId: TERA, semanaInicioISO: "2026-07-13",
    });
    expect(r.blocos).toHaveLength(1);
    expect(r.blocos[0]).toMatchObject({ origem: "previsto", diaSemana: 1, inicioMin: 540, rotulo: "Ana Alfa" });
  });
});
```

- [ ] **Step 6: Run to verify it fails**

Run: `pnpm test:rls db/tests/agenda2-carregar-semana-rls.int.test.ts`
Expected: FAIL — `carregarSemana` não exportado.

- [ ] **Step 7: Implement `carregarSemana` + `disponibilidadeTerapeutaNoDia`**

```ts
// src/app/(app)/agenda/queries.ts  (adicionar)
import { and, eq, gte, lte } from "drizzle-orm";
import { FUSO_CLINICA } from "@/app/(app)/agenda/fuso";
import { diasDaSemana } from "@/lib/agenda/semana";
import { paraMinutosLocais } from "@/lib/agenda/fuso-min";
import {
  projetarSemana,
  type AvulsaProjecao,
  type BlocoAgenda,
  type RegraProjecao,
} from "@/lib/agenda/projecao";
import type { FaixaDia } from "@/lib/agenda/janela";

export interface CarregarSemanaParams {
  eixo: "terapeuta" | "paciente";
  entidadeId: string;
  semanaInicioISO: string;
}
export interface SemanaCarregada {
  blocos: BlocoAgenda[];
  janelas: FaixaDia[]; // só preenchido quando eixo="terapeuta"
  bloqueios: { dataInicio: string; dataFim: string }[];
}

export async function carregarSemana(
  ctx: TenantContext,
  { eixo, entidadeId, semanaInicioISO }: CarregarSemanaParams,
): Promise<SemanaCarregada> {
  requireRole(ctx, "coordenador");
  const dias = diasDaSemana(semanaInicioISO);
  const primeiro = dias[0];
  const ultimo = dias[6];
  const colEntidade =
    eixo === "terapeuta"
      ? schema.agendamentoRecorrente.terapeutaId
      : schema.agendamentoRecorrente.patientId;
  const colRotulo =
    eixo === "terapeuta" ? schema.patient.nome : schema.appUser.name;

  return withTenant(ctx, async (tx) => {
    // Regras ativas vigentes na semana (previsto).
    const regrasRaw = await tx
      .select({
        id: schema.agendamentoRecorrente.id,
        diaSemana: schema.agendamentoRecorrente.diaSemana,
        horaInicio: schema.agendamentoRecorrente.horaInicio,
        duracaoMin: schema.agendamentoRecorrente.duracaoMin,
        disciplina: schema.agendamentoRecorrente.disciplina,
        rotulo: colRotulo,
      })
      .from(schema.agendamentoRecorrente)
      .innerJoin(
        schema.patient,
        eq(schema.agendamentoRecorrente.patientId, schema.patient.id),
      )
      .innerJoin(
        schema.appUser,
        eq(schema.agendamentoRecorrente.terapeutaId, schema.appUser.id),
      )
      .where(
        and(
          eq(schema.agendamentoRecorrente.clinicId, ctx.clinicId),
          eq(colEntidade, entidadeId),
          eq(schema.agendamentoRecorrente.status, "ativo"),
          lte(schema.agendamentoRecorrente.vigenciaInicio, ultimo),
        ),
      );
    const regras: RegraProjecao[] = regrasRaw.map((r) => ({
      id: r.id,
      diaSemana: r.diaSemana,
      horaInicio: r.horaInicio,
      duracaoMin: r.duracaoMin,
      disciplina: r.disciplina,
      rotulo: r.rotulo ?? "—",
    }));

    // Avulsas (session recorrenteId null) na janela de datas (concreto).
    const colSessEntidade =
      eixo === "terapeuta" ? schema.session.terapeutaId : schema.session.patientId;
    const avulsasRaw = await tx
      .select({
        id: schema.session.id,
        agendadaPara: schema.session.agendadaPara,
        duracaoMin: schema.session.duracaoMin,
        disciplina: schema.session.disciplina,
        rotulo: colRotulo,
      })
      .from(schema.session)
      .innerJoin(schema.patient, eq(schema.session.patientId, schema.patient.id))
      .innerJoin(schema.appUser, eq(schema.session.terapeutaId, schema.appUser.id))
      .where(
        and(
          eq(schema.session.clinicId, ctx.clinicId),
          eq(colSessEntidade, entidadeId),
          isNull(schema.session.recorrenteId), // só avulsas; não puxar sessões materializadas (Etapa D)
          gte(schema.session.agendadaPara, new Date(`${primeiro}T00:00:00${FUSO_CLINICA_OFFSET}`)),
          lte(schema.session.agendadaPara, new Date(`${ultimo}T23:59:59${FUSO_CLINICA_OFFSET}`)),
        ),
      );
    const avulsas: AvulsaProjecao[] = avulsasRaw.map((a) => {
      const { diaSemana, inicioMin } = paraMinutosLocais(a.agendadaPara, FUSO_CLINICA);
      return {
        id: a.id,
        diaSemana,
        inicioMin,
        duracaoMin: a.duracaoMin,
        disciplina: a.disciplina ?? "—",
        rotulo: a.rotulo ?? "—",
      };
    });

    // Janelas de trabalho (só no eixo terapeuta).
    let janelas: FaixaDia[] = [];
    if (eixo === "terapeuta") {
      const jr = await tx
        .select({
          diaSemana: schema.janelaTrabalho.diaSemana,
          horaInicio: schema.janelaTrabalho.horaInicio,
          horaFim: schema.janelaTrabalho.horaFim,
        })
        .from(schema.janelaTrabalho)
        .where(eq(schema.janelaTrabalho.terapeutaId, entidadeId));
      janelas = jr.map((j) => ({
        diaSemana: j.diaSemana,
        inicio: j.horaInicio,
        fim: j.horaFim,
      }));
    }

    // Bloqueios que tocam a semana (do escopo aplicável).
    const bloq = await tx
      .select({
        dataInicio: schema.bloqueio.dataInicio,
        dataFim: schema.bloqueio.dataFim,
      })
      .from(schema.bloqueio)
      .where(
        and(
          eq(schema.bloqueio.clinicId, ctx.clinicId),
          lte(schema.bloqueio.dataInicio, ultimo),
          gte(schema.bloqueio.dataFim, primeiro),
        ),
      );

    return { blocos: projetarSemana(regras, avulsas), janelas, bloqueios: bloq };
  });
}

/** Aviso suave por-paciente (C8): as faixas de trabalho do terapeuta no dia. */
export async function disponibilidadeTerapeutaNoDia(
  ctx: TenantContext,
  terapeutaId: string,
  diaISO: string,
): Promise<FaixaDia[]> {
  requireRole(ctx, "coordenador");
  const diaSemana = new Date(`${diaISO}T00:00:00Z`).getUTCDay();
  return withTenant(ctx, async (tx) => {
    const rows = await tx
      .select({
        diaSemana: schema.janelaTrabalho.diaSemana,
        horaInicio: schema.janelaTrabalho.horaInicio,
        horaFim: schema.janelaTrabalho.horaFim,
      })
      .from(schema.janelaTrabalho)
      .where(
        and(
          eq(schema.janelaTrabalho.terapeutaId, terapeutaId),
          eq(schema.janelaTrabalho.diaSemana, diaSemana),
        ),
      );
    return rows.map((j) => ({ diaSemana: j.diaSemana, inicio: j.horaInicio, fim: j.horaFim }));
  });
}
```

> **Nota ao executor:** confirme os nomes reais dos campos de `FaixaDia` em
> `src/lib/agenda/janela.ts` (`inicio`/`fim` vs `horaInicio`/`horaFim`) e de
> `appUser.name` no schema antes de rodar — ajuste o mapeamento se divergir.

- [ ] **Step 8: Run integration test to verify it passes**

Run: `pnpm test:rls db/tests/agenda2-carregar-semana-rls.int.test.ts`
Expected: PASS.

- [ ] **Step 9: Commit**

```bash
git add src/lib/agenda/fuso-min.ts src/lib/agenda/fuso-min.test.ts "src/app/(app)/agenda/queries.ts" db/tests/agenda2-carregar-semana-rls.int.test.ts
git commit -m "feat(agenda): carregarSemana + disponibilidade + conversao fuso C10 (etapa C)"
```

---

### Task 6: Escrita `criarRegra` com pré-check de conflito (`agenda/queries.ts`)

**Files:**
- Modify: `src/app/(app)/agenda/queries.ts`
- Test: `db/tests/agenda2-criar-regra.int.test.ts`

**Interfaces:**
- Consumes: `withTenant`, `requireRole`, `conflita`/`Slot` (Task 2), `vigenciaInicioC7` (Task 1), `horaParaMin` (`@/lib/agenda/janela`), `schema.agendamentoRecorrente`.
- Produces:
  - `interface NovaRegra { patientId; terapeutaId; disciplina; diaSemana; horaInicio; duracaoMin; semanaVisivelISO; hojeISO }`
  - `criarRegra(ctx: TenantContext, dados: NovaRegra): Promise<{ id: string }>`
- Regras: C1 grava **só** `agendamento_recorrente`; C2/C5 pré-check `conflita` nas 2 dimensões (regras ativas do terapeuta E do paciente) — lança `ConflitoError` se colidir; C7 `vigenciaInicio = vigenciaInicioC7(...)`.

- [ ] **Step 1: Write the failing test**

```ts
// db/tests/agenda2-criar-regra.int.test.ts
import { beforeEach, describe, expect, test, vi } from "vitest";
vi.mock("server-only", () => ({}));
import { sql } from "drizzle-orm";
import { db } from "@/db/client";
import * as schema from "@/db/schema";
import { criarRegra, ConflitoError } from "@/app/(app)/agenda/queries";

const hasDb = !!process.env.DATABASE_URL;
const CLINIC_A = "00000000-0000-0000-0000-0000000000a1";
const TERA = "00000000-0000-0000-0000-00000000e001";
const PAC = "00000000-0000-0000-0000-0000000a0001";
const ctxA = { clinicId: CLINIC_A, userId: TERA, role: "coordenador" as const };
const base = {
  patientId: PAC, terapeutaId: TERA, disciplina: "aba", diaSemana: 1,
  horaInicio: "09:00", duracaoMin: 60, semanaVisivelISO: "2026-07-13", hojeISO: "2026-07-13",
};

describe.skipIf(!hasDb)("criarRegra", () => {
  beforeEach(async () => {
    await db.execute(sql`TRUNCATE ${schema.agendamentoRecorrente}, ${schema.patient} CASCADE`);
    await db.insert(schema.patient).values([{ id: PAC, clinicId: CLINIC_A, nome: "Ana" }] as (typeof schema.patient.$inferInsert)[]);
  });

  test("grava só agendamento_recorrente (0 sessions) com vigencia C7", async () => {
    const { id } = await criarRegra(ctxA, base);
    const [regra] = await db.select().from(schema.agendamentoRecorrente).where(sql`${schema.agendamentoRecorrente.id} = ${id}`);
    expect(regra.vigenciaInicio).toBe("2026-07-13");
    const sess = await db.select().from(schema.session);
    expect(sess).toHaveLength(0); // C1
  });

  test("colisão de horário no mesmo terapeuta lança ConflitoError (C2/C5)", async () => {
    await criarRegra(ctxA, base);
    await expect(criarRegra(ctxA, { ...base, horaInicio: "09:30" })).rejects.toBeInstanceOf(ConflitoError);
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test:rls db/tests/agenda2-criar-regra.int.test.ts`
Expected: FAIL — `criarRegra`/`ConflitoError` não exportados.

- [ ] **Step 3: Implement `criarRegra` + `ConflitoError`**

```ts
// src/app/(app)/agenda/queries.ts  (adicionar)
import { horaParaMin } from "@/lib/agenda/janela";
import { conflita, type Slot } from "@/lib/agenda/conflito";
import { vigenciaInicioC7 } from "@/lib/agenda/semana";

export class ConflitoError extends Error {
  constructor(public dimensao: "terapeuta" | "paciente") {
    super(`Horário em conflito para ${dimensao}.`);
    this.name = "ConflitoError";
  }
}

export interface NovaRegra {
  patientId: string;
  terapeutaId: string;
  disciplina: string;
  diaSemana: number;
  horaInicio: string; // "HH:MM"
  duracaoMin: number;
  semanaVisivelISO: string;
  hojeISO: string;
}

export async function criarRegra(
  ctx: TenantContext,
  dados: NovaRegra,
): Promise<{ id: string }> {
  requireRole(ctx, "coordenador");
  const inicioMin = horaParaMin(dados.horaInicio);
  const novo: Slot = {
    diaSemana: dados.diaSemana,
    inicioMin,
    fimMin: inicioMin + dados.duracaoMin,
  };
  const vigenciaInicio = vigenciaInicioC7(dados.semanaVisivelISO, dados.hojeISO);

  // ⚠️ ERRATA (implementação final, commit 764ff10 pós review whole-branch):
  // este snippet checa conflito SÓ contra `agendamento_recorrente` — o que
  // deixa passar colisão regra×avulsa (C5/§5.2 exigem checar regras E avulsas).
  // O código shipado ALÉM do `ativas` abaixo TAMBÉM carrega as avulsas do mesmo
  // dia-da-semana (`session`, `isNull(recorrenteId)`, `estado='agendada'`,
  // `agendadaPara >= vigenciaInicio`), converte cada uma via
  // `paraMinutosLocais(.., FUSO_CLINICA)`, filtra `diaSemana === dados.diaSemana`
  // e as inclui como `Slot`s nas DUAS checagens `conflita` abaixo (terapeuta e
  // paciente). Ver `src/app/(app)/agenda/queries.ts` para a versão autoritativa.
  return withTenant(ctx, async (tx) => {
    const ativas = await tx
      .select({
        terapeutaId: schema.agendamentoRecorrente.terapeutaId,
        patientId: schema.agendamentoRecorrente.patientId,
        diaSemana: schema.agendamentoRecorrente.diaSemana,
        horaInicio: schema.agendamentoRecorrente.horaInicio,
        duracaoMin: schema.agendamentoRecorrente.duracaoMin,
      })
      .from(schema.agendamentoRecorrente)
      .where(
        and(
          eq(schema.agendamentoRecorrente.clinicId, ctx.clinicId),
          eq(schema.agendamentoRecorrente.status, "ativo"),
          eq(schema.agendamentoRecorrente.diaSemana, dados.diaSemana),
        ),
      );
    const paraSlots = (rows: typeof ativas): Slot[] =>
      rows.map((r) => {
        const ini = horaParaMin(r.horaInicio);
        return { diaSemana: r.diaSemana, inicioMin: ini, fimMin: ini + r.duracaoMin };
      });
    if (conflita(novo, paraSlots(ativas.filter((r) => r.terapeutaId === dados.terapeutaId)))) {
      throw new ConflitoError("terapeuta");
    }
    if (conflita(novo, paraSlots(ativas.filter((r) => r.patientId === dados.patientId)))) {
      throw new ConflitoError("paciente");
    }
    const [row] = await tx
      .insert(schema.agendamentoRecorrente)
      .values({
        clinicId: ctx.clinicId,
        patientId: dados.patientId,
        terapeutaId: dados.terapeutaId,
        disciplina: dados.disciplina,
        diaSemana: dados.diaSemana,
        horaInicio: dados.horaInicio,
        duracaoMin: dados.duracaoMin,
        vigenciaInicio,
        status: "ativo",
      })
      .returning({ id: schema.agendamentoRecorrente.id });
    return row;
  });
}
```

- [ ] **Step 4: Run to verify it passes**

Run: `pnpm test:rls db/tests/agenda2-criar-regra.int.test.ts`
Expected: PASS (grava só a regra; colisão lança).

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/agenda/queries.ts" db/tests/agenda2-criar-regra.int.test.ts
git commit -m "feat(agenda): criarRegra com pre-check de conflito e vigencia C7 (etapa C)"
```

---

### Task 7: Escrita `criarAvulsa` com backstop EXCLUDE gist (`agenda/queries.ts`)

**Files:**
- Modify: `src/app/(app)/agenda/queries.ts`
- Test: `db/tests/agenda2-criar-avulsa.int.test.ts`

**Interfaces:**
- Consumes: `withTenant`, `requireRole`, `ancorarNoFusoDaClinica` (`@/app/(app)/agenda/actions` — se não exportado, replicar via `FUSO_CLINICA_OFFSET`), `schema.session`.
- Produces:
  - `interface NovaAvulsa { patientId; terapeutaId; disciplina; tipo; dataISO; horaInicio; duracaoMin; modalidade }`
  - `criarAvulsa(ctx: TenantContext, dados: NovaAvulsa): Promise<{ id: string }>`
- Regras: cria linha `session` real com `recorrenteId=null`, `estado="agendada"`, `tipo`, ancorada no fuso da clínica (C10). O EXCLUDE gist do banco barra overbook concorrente (backstop TOCTOU) — capturar violação e relançar como `ConflitoError`.

- [ ] **Step 1: Write the failing test**

```ts
// db/tests/agenda2-criar-avulsa.int.test.ts
import { beforeEach, describe, expect, test, vi } from "vitest";
vi.mock("server-only", () => ({}));
import { sql } from "drizzle-orm";
import { db } from "@/db/client";
import * as schema from "@/db/schema";
import { criarAvulsa, ConflitoError } from "@/app/(app)/agenda/queries";

const hasDb = !!process.env.DATABASE_URL;
const CLINIC_A = "00000000-0000-0000-0000-0000000000a1";
const TERA = "00000000-0000-0000-0000-00000000e001";
const PAC = "00000000-0000-0000-0000-0000000a0001";
const ctxA = { clinicId: CLINIC_A, userId: TERA, role: "coordenador" as const };
const base = {
  patientId: PAC, terapeutaId: TERA, disciplina: "aba", tipo: "avaliacao" as const,
  dataISO: "2026-07-13", horaInicio: "09:00", duracaoMin: 60, modalidade: "presencial" as const,
};

describe.skipIf(!hasDb)("criarAvulsa", () => {
  beforeEach(async () => {
    await db.execute(sql`TRUNCATE ${schema.session}, ${schema.patient} CASCADE`);
    await db.insert(schema.patient).values([{ id: PAC, clinicId: CLINIC_A, nome: "Ana" }] as (typeof schema.patient.$inferInsert)[]);
  });

  test("cria session avulsa com recorrenteId null e estado agendada", async () => {
    const { id } = await criarAvulsa(ctxA, base);
    const [s] = await db.select().from(schema.session).where(sql`${schema.session.id} = ${id}`);
    expect(s.recorrenteId).toBeNull();
    expect(s.estado).toBe("agendada");
    expect(s.tipo).toBe("avaliacao");
  });

  test("overbook no mesmo terapeuta é barrado pelo EXCLUDE gist → ConflitoError", async () => {
    await criarAvulsa(ctxA, base);
    await expect(criarAvulsa(ctxA, { ...base, horaInicio: "09:30" })).rejects.toBeInstanceOf(ConflitoError);
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test:rls db/tests/agenda2-criar-avulsa.int.test.ts`
Expected: FAIL — `criarAvulsa` não exportado.

- [ ] **Step 3: Implement `criarAvulsa`**

```ts
// src/app/(app)/agenda/queries.ts  (adicionar)
import { FUSO_CLINICA_OFFSET } from "@/app/(app)/agenda/fuso";

export interface NovaAvulsa {
  patientId: string;
  terapeutaId: string;
  disciplina: string;
  tipo: "avaliacao" | "devolutiva" | "reuniao_pais" | "outro";
  dataISO: string; // "YYYY-MM-DD"
  horaInicio: string; // "HH:MM"
  duracaoMin: number;
  modalidade: "presencial" | "online";
}

export async function criarAvulsa(
  ctx: TenantContext,
  dados: NovaAvulsa,
): Promise<{ id: string }> {
  requireRole(ctx, "coordenador");
  // ⚠️ ERRATA (implementação final, commit 764ff10): este snippet confia SÓ no
  // EXCLUDE gist, que não enxerga regras ativas (não têm linha em `session`) —
  // deixaria passar colisão avulsa×regra. O código shipado adiciona um PRÉ-CHECK
  // app-level ANTES do insert: carrega as regras ativas do mesmo dia-da-semana
  // (`agendamento_recorrente`, `status='ativo'`, `vigenciaInicio <= dataISO`),
  // monta `Slot`s e roda `conflita` nas DUAS dimensões (terapeuta e paciente),
  // lançando `ConflitoError` na sobreposição. O gist segue como backstop TOCTOU
  // para avulsa×avulsa. Ver `src/app/(app)/agenda/queries.ts` (autoritativo).
  // C10: ancora o horário naive no offset da clínica ao gravar timestamptz.
  const agendadaPara = new Date(`${dados.dataISO}T${dados.horaInicio}:00${FUSO_CLINICA_OFFSET}`);
  try {
    return await withTenant(ctx, async (tx) => {
      const [row] = await tx
        .insert(schema.session)
        .values({
          clinicId: ctx.clinicId,
          patientId: dados.patientId,
          terapeutaId: dados.terapeutaId,
          recorrenteId: null,
          disciplina: dados.disciplina,
          tipo: dados.tipo,
          agendadaPara,
          duracaoMin: dados.duracaoMin,
          estado: "agendada",
          modalidade: dados.modalidade,
        })
        .returning({ id: schema.session.id });
      return row;
    });
  } catch (e) {
    // EXCLUDE gist (btree_gist) → SQLSTATE 23P01 exclusion_violation.
    // ⚠️ ERRATA (commit bc9ffca): postgres-js embrulha o erro em
    // `DrizzleQueryError`, então o `.code` fica em `e.cause`, não no topo. O
    // código shipado testa `e.code` E `e.cause.code` antes de comparar a 23P01.
    if (e && typeof e === "object" && "code" in e && (e as { code?: string }).code === "23P01") {
      throw new ConflitoError("terapeuta");
    }
    throw e;
  }
}
```

> **Nota ao executor:** confirme o nome real da coluna de horário em `session`
> (`agendadaPara`) e do offset exportado (`FUSO_CLINICA_OFFSET`) antes de rodar.

- [ ] **Step 4: Run to verify it passes**

Run: `pnpm test:rls db/tests/agenda2-criar-avulsa.int.test.ts`
Expected: PASS (cria avulsa; overbook relança `ConflitoError`).

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/agenda/queries.ts" db/tests/agenda2-criar-avulsa.int.test.ts
git commit -m "feat(agenda): criarAvulsa com backstop EXCLUDE gist (etapa C)"
```

---

### Task 8: Server Actions finas (`agenda/semana/actions.ts`)

**Files:**
- Create: `src/app/(app)/agenda/semana/actions.ts`
- Test: `db/tests/agenda2-semana-actions.int.test.ts`

**Interfaces:**
- Consumes: `getTenantContext` (`@/auth/tenant`), `criarRegra`/`criarAvulsa`/`ConflitoError` (Task 6/7), `requireRole`/`RoleError` (`@/auth/require-role`), `revalidatePath` (`next/cache`).
- Produces (assinaturas `useActionState`):
  - `criarRegraAction(prev: EstadoAcao, formData: FormData): Promise<EstadoAcao>`
  - `criarAvulsaAction(prev: EstadoAcao, formData: FormData): Promise<EstadoAcao>`
  - `type EstadoAcao = { error?: string; ok?: boolean }`

- [ ] **Step 1: Write the failing test (defesa de papel)**

```ts
// db/tests/agenda2-semana-actions.int.test.ts
import { describe, expect, test, vi } from "vitest";
vi.mock("server-only", () => ({}));
vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }));
const getTenantContext = vi.fn();
vi.mock("@/auth/tenant", () => ({ getTenantContext: () => getTenantContext() }));
import { criarRegraAction } from "@/app/(app)/agenda/semana/actions";

describe("criarRegraAction (defesa em profundidade)", () => {
  test("papel não-coordenador retorna erro, não grava", async () => {
    getTenantContext.mockResolvedValue({ clinicId: "c", userId: "u", role: "terapeuta" });
    const fd = new FormData();
    fd.set("patientId", "p"); fd.set("terapeutaId", "t"); fd.set("disciplina", "aba");
    fd.set("diaSemana", "1"); fd.set("horaInicio", "09:00"); fd.set("duracaoMin", "60");
    fd.set("semanaVisivelISO", "2026-07-13"); fd.set("hojeISO", "2026-07-13");
    const r = await criarRegraAction({}, fd);
    expect(r.error).toBeTruthy();
    expect(r.ok).toBeUndefined();
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test db/tests/agenda2-semana-actions.int.test.ts`
Expected: FAIL — módulo inexistente.

- [ ] **Step 3: Implement actions**

```ts
// src/app/(app)/agenda/semana/actions.ts
"use server";

import { revalidatePath } from "next/cache";
import { getTenantContext } from "@/auth/tenant";
import { RoleError } from "@/auth/require-role";
import {
  ConflitoError,
  criarAvulsa,
  criarRegra,
  type NovaAvulsa,
  type NovaRegra,
} from "@/app/(app)/agenda/queries";

export type EstadoAcao = { error?: string; ok?: boolean };

function trata(e: unknown): EstadoAcao {
  if (e instanceof ConflitoError) return { error: e.message };
  if (e instanceof RoleError) return { error: "Sem permissão para alocar." };
  throw e;
}

export async function criarRegraAction(
  _prev: EstadoAcao,
  formData: FormData,
): Promise<EstadoAcao> {
  const ctx = await getTenantContext();
  const dados: NovaRegra = {
    patientId: String(formData.get("patientId")),
    terapeutaId: String(formData.get("terapeutaId")),
    disciplina: String(formData.get("disciplina")),
    diaSemana: Number(formData.get("diaSemana")),
    horaInicio: String(formData.get("horaInicio")),
    duracaoMin: Number(formData.get("duracaoMin")),
    semanaVisivelISO: String(formData.get("semanaVisivelISO")),
    hojeISO: String(formData.get("hojeISO")),
  };
  try {
    await criarRegra(ctx, dados);
  } catch (e) {
    return trata(e);
  }
  revalidatePath("/agenda/semana");
  return { ok: true };
}

export async function criarAvulsaAction(
  _prev: EstadoAcao,
  formData: FormData,
): Promise<EstadoAcao> {
  const ctx = await getTenantContext();
  const dados: NovaAvulsa = {
    patientId: String(formData.get("patientId")),
    terapeutaId: String(formData.get("terapeutaId")),
    disciplina: String(formData.get("disciplina")),
    tipo: formData.get("tipo") as NovaAvulsa["tipo"],
    dataISO: String(formData.get("dataISO")),
    horaInicio: String(formData.get("horaInicio")),
    duracaoMin: Number(formData.get("duracaoMin")),
    modalidade: (formData.get("modalidade") as NovaAvulsa["modalidade"]) ?? "presencial",
  };
  try {
    await criarAvulsa(ctx, dados);
  } catch (e) {
    return trata(e);
  }
  revalidatePath("/agenda/semana");
  return { ok: true };
}
```

- [ ] **Step 4: Run to verify it passes**

Run: `pnpm test db/tests/agenda2-semana-actions.int.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/agenda/semana/actions.ts" db/tests/agenda2-semana-actions.int.test.ts
git commit -m "feat(agenda): server actions finas criarRegra/criarAvulsa (etapa C)"
```

---

### Task 9: Combobox de entidade por nome (`agenda/semana/combobox-entidade.tsx`)

**Files:**
- Create: `src/app/(app)/agenda/semana/combobox-entidade.tsx`
- Test: `src/app/(app)/agenda/semana/combobox-entidade.a11y.test.tsx`

**Interfaces:**
- Consumes: `Input` (`@/components/ui/input`), `Field` (`@/components/ui/field`), padrão de foco de `equipe/[id]/grade-disponibilidade.tsx`.
- Produces:
  - `interface Opcao { id: string; nome: string }`
  - `interface ComboboxEntidadeProps { label: string; opcoes: Opcao[]; valor: string | null; aoSelecionar: (id: string) => void; aoBuscar?: (termo: string) => void; }`
  - `export function ComboboxEntidade(props: ComboboxEntidadeProps): JSX.Element`
- Padrão a11y: `role="combobox"` no input com `aria-expanded`/`aria-controls`/`aria-activedescendant`; lista `role="listbox"` com `role="option"` + `aria-selected`; teclado ↑/↓/Enter/Esc. NÃO existe Combobox no DS — este é o primeiro; mantê-lo genérico (paciente|terapeuta).

- [ ] **Step 1: Write the failing a11y + comportamento test**

```tsx
// src/app/(app)/agenda/semana/combobox-entidade.a11y.test.tsx
import { axe } from "jest-axe";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, test, vi } from "vitest";
import { ComboboxEntidade } from "./combobox-entidade";

const opcoes = [
  { id: "1", nome: "Ana Alfa" },
  { id: "2", nome: "Bruno Beta" },
];

describe("ComboboxEntidade", () => {
  test("sem violações axe (color-contrast religado)", async () => {
    const { container } = render(
      <ComboboxEntidade label="Paciente" opcoes={opcoes} valor={null} aoSelecionar={() => {}} />,
    );
    const resultado = await axe(container); // config default = color-contrast ligado
    expect(resultado).toHaveNoViolations();
  });

  test("filtra por texto e seleciona por teclado", async () => {
    const aoSelecionar = vi.fn();
    render(<ComboboxEntidade label="Paciente" opcoes={opcoes} valor={null} aoSelecionar={aoSelecionar} />);
    const input = screen.getByRole("combobox");
    await userEvent.type(input, "bru");
    expect(screen.getByRole("option", { name: /Bruno Beta/ })).toBeVisible();
    await userEvent.keyboard("{ArrowDown}{Enter}");
    expect(aoSelecionar).toHaveBeenCalledWith("2");
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test src/app/(app)/agenda/semana/combobox-entidade.a11y.test.tsx`
Expected: FAIL — módulo inexistente.

- [ ] **Step 3: Implement `ComboboxEntidade`**

```tsx
// src/app/(app)/agenda/semana/combobox-entidade.tsx
"use client";

import { useId, useMemo, useState } from "react";
import { Field } from "@/components/ui/field";
import { Input } from "@/components/ui/input";

export interface Opcao {
  id: string;
  nome: string;
}
export interface ComboboxEntidadeProps {
  label: string;
  opcoes: Opcao[];
  valor: string | null;
  aoSelecionar: (id: string) => void;
  aoBuscar?: (termo: string) => void;
}

export function ComboboxEntidade({
  label,
  opcoes,
  valor,
  aoSelecionar,
  aoBuscar,
}: ComboboxEntidadeProps) {
  const listId = useId();
  const [termo, setTermo] = useState("");
  const [aberto, setAberto] = useState(false);
  const [ativo, setAtivo] = useState(0);

  const filtradas = useMemo(
    () => opcoes.filter((o) => o.nome.toLowerCase().includes(termo.toLowerCase())),
    [opcoes, termo],
  );
  const selecionada = opcoes.find((o) => o.id === valor);

  function selecionar(o: Opcao) {
    aoSelecionar(o.id);
    setTermo(o.nome);
    setAberto(false);
  }

  function aoTeclar(e: React.KeyboardEvent) {
    if (e.key === "ArrowDown") {
      e.preventDefault();
      setAberto(true);
      setAtivo((i) => Math.min(i + 1, filtradas.length - 1));
    } else if (e.key === "ArrowUp") {
      e.preventDefault();
      setAtivo((i) => Math.max(i - 1, 0));
    } else if (e.key === "Enter" && aberto && filtradas[ativo]) {
      e.preventDefault();
      selecionar(filtradas[ativo]);
    } else if (e.key === "Escape") {
      setAberto(false);
    }
  }

  return (
    <Field label={label}>
      <div className="relative">
        <Input
          role="combobox"
          aria-expanded={aberto}
          aria-controls={listId}
          aria-activedescendant={aberto ? `${listId}-${ativo}` : undefined}
          autoComplete="off"
          value={aberto ? termo : (selecionada?.nome ?? termo)}
          onChange={(e) => {
            setTermo(e.target.value);
            setAberto(true);
            setAtivo(0);
            aoBuscar?.(e.target.value);
          }}
          onFocus={() => setAberto(true)}
          onKeyDown={aoTeclar}
        />
        {aberto && filtradas.length > 0 && (
          <ul
            id={listId}
            role="listbox"
            className="absolute z-10 mt-1 w-full border-2 border-black bg-white shadow-[4px_4px_0_#000]"
          >
            {filtradas.map((o, i) => (
              <li
                key={o.id}
                id={`${listId}-${i}`}
                role="option"
                aria-selected={i === ativo}
                className={i === ativo ? "bg-brand-primary px-3 py-2" : "px-3 py-2"}
                onMouseDown={(e) => {
                  e.preventDefault();
                  selecionar(o);
                }}
              >
                {o.nome}
              </li>
            ))}
          </ul>
        )}
      </div>
    </Field>
  );
}
```

> **Nota ao executor:** confirme as classes utilitárias do DS (borda/sombra
> brutalista, `bg-brand-primary`) contra `src/styles/globals.css`; prefira um
> helper de `primitives/surface.ts` se existir, em vez de classes soltas.

- [ ] **Step 4: Run to verify it passes**

Run: `pnpm test src/app/(app)/agenda/semana/combobox-entidade.a11y.test.tsx`
Expected: PASS (axe limpo + seleção por teclado).

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/agenda/semana/combobox-entidade.tsx" "src/app/(app)/agenda/semana/combobox-entidade.a11y.test.tsx"
git commit -m "feat(agenda): combobox de entidade por nome acessivel (etapa C)"
```

---

### Task 10: Grade do calendário semanal (`agenda/semana/calendario-semana.tsx`)

**Files:**
- Create: `src/app/(app)/agenda/semana/calendario-semana.tsx`
- Test: `src/app/(app)/agenda/semana/calendario-semana.a11y.test.tsx`

**Interfaces:**
- Consumes: `BlocoAgenda` (Task 3), `FaixaDia` (`@/lib/agenda/janela`), `colunasDaGrade`/`horaParaMin` (`@/lib/agenda/grade`,`janela`), padrão `role="grid"`+teclado de `equipe/[id]/grade-disponibilidade.tsx`.
- Produces:
  - `interface CalendarioSemanaProps { dias: string[]; passoMin: number; abertura: string; fechamento: string; janelas: FaixaDia[]; bloqueios: { dataInicio: string; dataFim: string }[]; blocos: BlocoAgenda[]; aoAlocar: (diaSemana: number, inicioMin: number) => void; }`
  - `export function CalendarioSemana(props: CalendarioSemanaProps): JSX.Element`
- Regras: `role="grid"`, células navegáveis por seta/Enter (roving `tabIndex`); overlay absoluto com blocos proporcionais a `duracaoMin` (C3); célula fora-da-janela = aviso suave visual mas clicável (C8); célula bloqueada = `aria-disabled`, não dispara `aoAlocar` (C8); bloco `origem="previsto"` vs `"concreto"` com estilo distinto (C9).

- [ ] **Step 1: Write the failing a11y + interação test**

```tsx
// src/app/(app)/agenda/semana/calendario-semana.a11y.test.tsx
import { axe } from "jest-axe";
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, test, vi } from "vitest";
import { CalendarioSemana } from "./calendario-semana";

const base = {
  dias: ["2026-07-13", "2026-07-14", "2026-07-15", "2026-07-16", "2026-07-17", "2026-07-18", "2026-07-19"],
  passoMin: 30,
  abertura: "08:00",
  fechamento: "12:00",
  janelas: [{ diaSemana: 1, inicio: "08:00", fim: "12:00" }],
  bloqueios: [],
  blocos: [
    { id: "r1", origem: "previsto" as const, diaSemana: 1, inicioMin: 540, duracaoMin: 60, disciplina: "aba", rotulo: "Ana" },
  ],
};

describe("CalendarioSemana", () => {
  test("é um grid sem violações axe (color-contrast religado)", async () => {
    const { container } = render(<CalendarioSemana {...base} aoAlocar={() => {}} />);
    expect(screen.getByRole("grid")).toBeInTheDocument();
    const r = await axe(container);
    expect(r).toHaveNoViolations();
  });

  test("clicar célula livre dispara aoAlocar com dia e minuto", async () => {
    const aoAlocar = vi.fn();
    render(<CalendarioSemana {...base} aoAlocar={aoAlocar} />);
    // célula seg 10:00 (livre)
    await userEvent.click(screen.getByRole("gridcell", { name: /segunda.*10:00/i }));
    expect(aoAlocar).toHaveBeenCalledWith(1, 600);
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test src/app/(app)/agenda/semana/calendario-semana.a11y.test.tsx`
Expected: FAIL — módulo inexistente.

- [ ] **Step 3: Implement `CalendarioSemana`**

```tsx
// src/app/(app)/agenda/semana/calendario-semana.tsx
"use client";

import { useRef, useState } from "react";
import { colunasDaGrade } from "@/lib/agenda/grade";
import { horaParaMin, minParaHora, type FaixaDia } from "@/lib/agenda/janela";
import type { BlocoAgenda } from "@/lib/agenda/projecao";

const DIAS_LABEL = ["Domingo", "Segunda", "Terça", "Quarta", "Quinta", "Sexta", "Sábado"];

export interface CalendarioSemanaProps {
  dias: string[];
  passoMin: number;
  abertura: string;
  fechamento: string;
  janelas: FaixaDia[];
  bloqueios: { dataInicio: string; dataFim: string }[];
  blocos: BlocoAgenda[];
  aoAlocar: (diaSemana: number, inicioMin: number) => void;
}

export function CalendarioSemana({
  dias,
  passoMin,
  abertura,
  fechamento,
  janelas,
  bloqueios,
  blocos,
  aoAlocar,
}: CalendarioSemanaProps) {
  const colunas = colunasDaGrade(passoMin, abertura, fechamento); // minutos por linha da grade
  const [foco, setFoco] = useState<{ dia: number; col: number }>({ dia: 1, col: 0 });
  const refs = useRef<Record<string, HTMLDivElement | null>>({});

  function dentroDaJanela(diaSemana: number, inicioMin: number) {
    return janelas.some(
      (j) =>
        j.diaSemana === diaSemana &&
        inicioMin >= horaParaMin(j.inicio) &&
        inicioMin < horaParaMin(j.fim),
    );
  }
  function estaBloqueado(diaISO: string) {
    return bloqueios.some((b) => diaISO >= b.dataInicio && diaISO <= b.dataFim);
  }

  function aoTeclar(e: React.KeyboardEvent, diaSemana: number, inicioMin: number, bloqueado: boolean) {
    const totalCol = colunas.length;
    if (e.key === "ArrowRight") setFoco((f) => ({ ...f, col: Math.min(f.col + 1, totalCol - 1) }));
    else if (e.key === "ArrowLeft") setFoco((f) => ({ ...f, col: Math.max(f.col - 1, 0) }));
    else if (e.key === "ArrowDown") setFoco((f) => ({ ...f, dia: Math.min(f.dia + 1, 6) }));
    else if (e.key === "ArrowUp") setFoco((f) => ({ ...f, dia: Math.max(f.dia - 1, 0) }));
    else if ((e.key === "Enter" || e.key === " ") && !bloqueado) {
      e.preventDefault();
      aoAlocar(diaSemana, inicioMin);
    }
  }

  return (
    <div role="grid" aria-label="Calendário semanal" className="grid gap-px border-2 border-black">
      {dias.map((diaISO, idx) => {
        const diaSemana = new Date(`${diaISO}T00:00:00Z`).getUTCDay();
        const bloqueado = estaBloqueado(diaISO);
        return (
          <div role="row" key={diaISO} className="relative flex">
            <div className="w-24 shrink-0 px-2 py-1 font-bold">{DIAS_LABEL[diaSemana]}</div>
            {colunas.map((inicioMin, col) => {
              const ehFoco = foco.dia === idx && foco.col === col;
              const foraJanela = !dentroDaJanela(diaSemana, inicioMin);
              return (
                <div
                  key={inicioMin}
                  role="gridcell"
                  aria-label={`${DIAS_LABEL[diaSemana]} ${minParaHora(inicioMin)}`}
                  aria-disabled={bloqueado}
                  tabIndex={ehFoco ? 0 : -1}
                  ref={(el) => {
                    refs.current[`${idx}-${col}`] = el;
                  }}
                  className={[
                    "h-10 flex-1 border-l border-black/20",
                    bloqueado ? "cursor-not-allowed bg-black/10" : "cursor-pointer",
                    foraJanela && !bloqueado ? "bg-amber-50" : "",
                  ].join(" ")}
                  onClick={() => !bloqueado && aoAlocar(diaSemana, inicioMin)}
                  onKeyDown={(e) => aoTeclar(e, diaSemana, inicioMin, bloqueado)}
                />
              );
            })}
            {/* Overlay absoluto: blocos proporcionais a duracaoMin (C3/C9). */}
            {blocos
              .filter((b) => b.diaSemana === diaSemana)
              .map((b) => {
                const base = horaParaMin(abertura);
                const larguraCol = 100 / colunas.length;
                const left = ((b.inicioMin - base) / passoMin) * larguraCol;
                const width = (b.duracaoMin / passoMin) * larguraCol;
                return (
                  <div
                    key={b.id}
                    className={[
                      "absolute top-0 h-10 overflow-hidden text-xs",
                      b.origem === "previsto"
                        ? "border-2 border-dashed border-brand-primary-text bg-brand-primary/40"
                        : "border-2 border-black bg-mint-200",
                    ].join(" ")}
                    style={{ left: `calc(6rem + ${left}%)`, width: `${width}%` }}
                  >
                    {b.rotulo} · {b.disciplina}
                  </div>
                );
              })}
          </div>
        );
      })}
    </div>
  );
}
```

> **Nota ao executor:** valide `colunasDaGrade`/`minParaHora`/`FaixaDia` (nomes
> de campo `inicio`/`fim`) contra `src/lib/agenda/`. Troque as classes de cor
> soltas (`bg-mint-200`, `bg-amber-50`) pelos tokens reais do DS (Menta/alerta
> de `globals.css`). O overlay é aproximação; refine posicionamento se o passo
> visual divergir.

- [ ] **Step 4: Run to verify it passes**

Run: `pnpm test src/app/(app)/agenda/semana/calendario-semana.a11y.test.tsx`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/agenda/semana/calendario-semana.tsx" "src/app/(app)/agenda/semana/calendario-semana.a11y.test.tsx"
git commit -m "feat(agenda): grade do calendario semanal com overlay (etapa C)"
```

---

### Task 11: Popover de alocação + página `/agenda/semana`

**Files:**
- Create: `src/app/(app)/agenda/semana/popover-alocar.tsx`
- Create: `src/app/(app)/agenda/semana/semana-cliente.tsx` (client shell: estado de eixo/semana/slot + orquestra grade+popover)
- Create: `src/app/(app)/agenda/semana/page.tsx` (server: tenant + carrega dados iniciais)
- Test: `src/app/(app)/agenda/semana/popover-alocar.a11y.test.tsx`

**Interfaces:**
- Consumes: `Dialog`+partes (`@/components/ui/dialog`), `Select`+partes (`@/components/ui/select`), `Button`, `Field`, `Alert` (`@/components/ui/*`), `ComboboxEntidade` (Task 9), `CalendarioSemana` (Task 10), `criarRegraAction`/`criarAvulsaAction` (Task 8), `Tabs`+partes (`@/components/ui/tabs`), queries `carregarSemana`/`listarTerapeutas`/`listarPacientes` (Task 4/5), `useActionState` (react).
- Produces:
  - `interface PopoverAlocarProps { aberto: boolean; aoFechar: () => void; diaSemana: number; inicioMin: number; dataISO: string; semanaVisivelISO: string; hojeISO: string; eixo: "terapeuta" | "paciente"; entidadeFixa: { id: string; nome: string }; pacientes: Opcao[]; terapeutas: Opcao[]; disciplinas: string[]; duracaoPadrao: Record<string, number>; }`
  - `export function PopoverAlocar(props): JSX.Element` — toggle Recorrente|Avulsa (C6); no eixo terapeuta pede paciente+disciplina, no eixo paciente pede terapeuta+disciplina (C4); data/hora read-only; `duracaoMin` pré de `duracaoPadrao[disciplina]`, editável (D2); avulsa adiciona select `tipo`.
  - `page.tsx`: server component que resolve `getTenantContext`, `requireRole(ctx,'coordenador')`, carrega terapeutas e a semana atual, e renderiza `<SemanaCliente>`.

- [ ] **Step 1: Write the failing a11y test do popover**

```tsx
// src/app/(app)/agenda/semana/popover-alocar.a11y.test.tsx
import { axe } from "jest-axe";
import { render, screen } from "@testing-library/react";
import { describe, expect, test } from "vitest";
import { PopoverAlocar } from "./popover-alocar";

const props = {
  aberto: true,
  aoFechar: () => {},
  diaSemana: 1,
  inicioMin: 540,
  dataISO: "2026-07-13",
  semanaVisivelISO: "2026-07-13",
  hojeISO: "2026-07-13",
  eixo: "terapeuta" as const,
  entidadeFixa: { id: "t1", nome: "Dra. Sofia" },
  pacientes: [{ id: "p1", nome: "Ana Alfa" }],
  terapeutas: [{ id: "t1", nome: "Dra. Sofia" }],
  disciplinas: ["aba", "fono", "to"],
  duracaoPadrao: { aba: 60, fono: 30, to: 50 },
};

describe("PopoverAlocar", () => {
  test("dialog acessível sem violações axe (color-contrast religado)", async () => {
    const { container } = render(<PopoverAlocar {...props} />);
    expect(screen.getByRole("dialog")).toBeInTheDocument();
    const r = await axe(container);
    expect(r).toHaveNoViolations();
  });

  test("data e hora são read-only (fixadas pelo slot)", () => {
    render(<PopoverAlocar {...props} />);
    expect(screen.getByText(/13\/07/)).toBeInTheDocument();
    expect(screen.getByText(/09:00/)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test src/app/(app)/agenda/semana/popover-alocar.a11y.test.tsx`
Expected: FAIL — módulo inexistente.

- [ ] **Step 3: Implement `PopoverAlocar`**

```tsx
// src/app/(app)/agenda/semana/popover-alocar.tsx
"use client";

import { useActionState, useState } from "react";
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogTitle,
} from "@/components/ui/dialog";
import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";
import { Alert } from "@/components/ui/alert";
import { Button } from "@/components/ui/button";
import { Field } from "@/components/ui/field";
import { Input } from "@/components/ui/input";
import { minParaHora } from "@/lib/agenda/janela";
import {
  criarAvulsaAction,
  criarRegraAction,
  type EstadoAcao,
} from "./actions";
import { ComboboxEntidade, type Opcao } from "./combobox-entidade";

const TIPOS_AVULSA = [
  { v: "avaliacao", label: "Avaliação" },
  { v: "devolutiva", label: "Devolutiva" },
  { v: "reuniao_pais", label: "Reunião de pais" },
  { v: "outro", label: "Outro" },
] as const;

export interface PopoverAlocarProps {
  aberto: boolean;
  aoFechar: () => void;
  diaSemana: number;
  inicioMin: number;
  dataISO: string;
  semanaVisivelISO: string;
  hojeISO: string;
  eixo: "terapeuta" | "paciente";
  entidadeFixa: { id: string; nome: string };
  pacientes: Opcao[];
  terapeutas: Opcao[];
  disciplinas: string[];
  duracaoPadrao: Record<string, number>;
}

export function PopoverAlocar(props: PopoverAlocarProps) {
  const [modo, setModo] = useState<"recorrente" | "avulsa">("recorrente");
  const [disciplina, setDisciplina] = useState(props.disciplinas[0] ?? "");
  const [tipo, setTipo] = useState<(typeof TIPOS_AVULSA)[number]["v"]>("avaliacao");
  const [entidadeVar, setEntidadeVar] = useState<string | null>(null);
  const [duracao, setDuracao] = useState(props.duracaoPadrao[disciplina] ?? 60);

  const action = modo === "recorrente" ? criarRegraAction : criarAvulsaAction;
  const [estado, formAction] = useActionState<EstadoAcao, FormData>(action, {});

  const pedePaciente = props.eixo === "terapeuta";
  const opcoesVar = pedePaciente ? props.pacientes : props.terapeutas;
  const patientId = pedePaciente ? entidadeVar : props.entidadeFixa.id;
  const terapeutaId = pedePaciente ? props.entidadeFixa.id : entidadeVar;

  function trocarDisciplina(d: string) {
    setDisciplina(d);
    setDuracao(props.duracaoPadrao[d] ?? 60);
  }

  return (
    <Dialog open={props.aberto} onOpenChange={(o) => !o && props.aoFechar()}>
      <DialogContent>
        <DialogTitle>Alocar horário</DialogTitle>
        <DialogDescription>
          {props.dataISO.split("-").reverse().slice(0, 2).join("/")} às {minParaHora(props.inicioMin)}
        </DialogDescription>

        {/* Toggle Recorrente | Avulsa (C6) */}
        <div role="group" aria-label="Tipo de alocação" className="my-3 flex gap-2">
          <Button
            variante={modo === "recorrente" ? "primaria" : "neutra"}
            aria-pressed={modo === "recorrente"}
            onClick={() => setModo("recorrente")}
          >
            Recorrente
          </Button>
          <Button
            variante={modo === "avulsa" ? "primaria" : "neutra"}
            aria-pressed={modo === "avulsa"}
            onClick={() => setModo("avulsa")}
          >
            Avulsa
          </Button>
        </div>

        {estado.error && <Alert severidade="erro" titulo="Não foi possível alocar">{estado.error}</Alert>}

        <form action={formAction} className="space-y-3">
          <input type="hidden" name="patientId" value={patientId ?? ""} />
          <input type="hidden" name="terapeutaId" value={terapeutaId ?? ""} />
          <input type="hidden" name="diaSemana" value={props.diaSemana} />
          <input type="hidden" name="horaInicio" value={minParaHora(props.inicioMin)} />
          <input type="hidden" name="duracaoMin" value={duracao} />
          <input type="hidden" name="semanaVisivelISO" value={props.semanaVisivelISO} />
          <input type="hidden" name="hojeISO" value={props.hojeISO} />
          <input type="hidden" name="dataISO" value={props.dataISO} />

          <ComboboxEntidade
            label={pedePaciente ? "Paciente" : "Terapeuta"}
            opcoes={opcoesVar}
            valor={entidadeVar}
            aoSelecionar={setEntidadeVar}
          />

          <Field label="Disciplina">
            <Select value={disciplina} onValueChange={trocarDisciplina} name="disciplina">
              <SelectTrigger><SelectValue /></SelectTrigger>
              <SelectContent>
                {props.disciplinas.map((d) => (
                  <SelectItem key={d} value={d}>{d.toUpperCase()}</SelectItem>
                ))}
              </SelectContent>
            </Select>
          </Field>

          {modo === "avulsa" && (
            <Field label="Tipo">
              <Select value={tipo} onValueChange={(v) => setTipo(v as typeof tipo)} name="tipo">
                <SelectTrigger><SelectValue /></SelectTrigger>
                <SelectContent>
                  {TIPOS_AVULSA.map((t) => (
                    <SelectItem key={t.v} value={t.v}>{t.label}</SelectItem>
                  ))}
                </SelectContent>
              </Select>
            </Field>
          )}

          <Field label="Duração (min)">
            <Input
              type="number"
              min={5}
              step={5}
              value={duracao}
              onChange={(e) => setDuracao(Number(e.target.value))}
            />
          </Field>

          <input type="hidden" name="modalidade" value="presencial" />
          <Button type="submit" variante="primaria">Confirmar alocação</Button>
        </form>
      </DialogContent>
    </Dialog>
  );
}
```

> **Nota ao executor:** o `<Select name=...>` do Radix pode não emitir valor
> nativo no FormData — confirme em `ui/select.tsx`; se não emitir, adicione um
> `<input type="hidden" name="disciplina" value={disciplina} />`. Idem `tipo`.

- [ ] **Step 4: Implement `page.tsx` + `semana-cliente.tsx` (wiring)**

```tsx
// src/app/(app)/agenda/semana/page.tsx  (Server Component)
import { getTenantContext } from "@/auth/tenant";
import { requireRole } from "@/auth/require-role";
import { listarTerapeutas } from "@/app/(app)/equipe/[id]/queries";
import { segundaDaSemana } from "@/lib/agenda/semana";
import { SemanaCliente } from "./semana-cliente";

export default async function Page() {
  const ctx = await getTenantContext();
  requireRole(ctx, "coordenador");
  const terapeutas = await listarTerapeutas(ctx);
  const hojeISO = new Date().toISOString().slice(0, 10);
  return (
    <SemanaCliente
      terapeutas={terapeutas.map((t) => ({ id: t.id, nome: t.nome ?? "—" }))}
      semanaInicialISO={segundaDaSemana(hojeISO)}
      hojeISO={hojeISO}
    />
  );
}
```

```tsx
// src/app/(app)/agenda/semana/semana-cliente.tsx  (Client shell)
"use client";

import { useState } from "react";
import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Button } from "@/components/ui/button";
import { diasDaSemana, semanaEhPassada } from "@/lib/agenda/semana";
import { CalendarioSemana } from "./calendario-semana";
import { PopoverAlocar } from "./popover-alocar";
import { ComboboxEntidade, type Opcao } from "./combobox-entidade";
import type { BlocoAgenda } from "@/lib/agenda/projecao";
import type { FaixaDia } from "@/lib/agenda/janela";

interface Props {
  terapeutas: Opcao[];
  semanaInicialISO: string;
  hojeISO: string;
}

// NOTA: a busca de dados por semana/eixo/entidade é feita via um route handler
// ou server action de leitura que chama `carregarSemana`. Este shell mantém o
// estado de UI e delega o fetch; ver Step 5 para o adendo de leitura.
export function SemanaCliente({ terapeutas, semanaInicialISO, hojeISO }: Props) {
  const [eixo, setEixo] = useState<"terapeuta" | "paciente">("terapeuta");
  const [semanaISO, setSemanaISO] = useState(semanaInicialISO);
  const [entidade, setEntidade] = useState<Opcao | null>(terapeutas[0] ?? null);
  const [slot, setSlot] = useState<{ diaSemana: number; inicioMin: number; dataISO: string } | null>(null);
  const [dados, setDados] = useState<{ blocos: BlocoAgenda[]; janelas: FaixaDia[]; bloqueios: { dataInicio: string; dataFim: string }[] }>({
    blocos: [], janelas: [], bloqueios: [],
  });

  const dias = diasDaSemana(semanaISO);
  const passada = semanaEhPassada(semanaISO, hojeISO);

  return (
    <section className="space-y-4 p-4">
      <header className="flex flex-wrap items-center gap-3">
        <Tabs value={eixo} onValueChange={(v) => setEixo(v as typeof eixo)}>
          <TabsList>
            <TabsTrigger value="terapeuta">Por terapeuta</TabsTrigger>
            <TabsTrigger value="paciente">Por paciente</TabsTrigger>
          </TabsList>
        </Tabs>
        <div className="min-w-64">
          <ComboboxEntidade
            label={eixo === "terapeuta" ? "Terapeuta" : "Paciente"}
            opcoes={terapeutas}
            valor={entidade?.id ?? null}
            aoSelecionar={(id) => setEntidade(terapeutas.find((t) => t.id === id) ?? null)}
          />
        </div>
        <div className="ml-auto flex gap-2">
          <Button variante="neutra" onClick={() => setSemanaISO(recuar(semanaISO))}>← Semana</Button>
          <Button variante="neutra" onClick={() => setSemanaISO(avancar(semanaISO))}>Semana →</Button>
        </div>
      </header>

      {passada && (
        <p role="status" className="text-sm">Semana passada — alocação desabilitada (C7).</p>
      )}

      <CalendarioSemana
        dias={dias}
        passoMin={30}
        abertura="08:00"
        fechamento="18:00"
        janelas={dados.janelas}
        bloqueios={dados.bloqueios}
        blocos={dados.blocos}
        aoAlocar={(diaSemana, inicioMin) => {
          if (passada) return;
          const dataISO = dias[diaSemana === 0 ? 6 : diaSemana - 1];
          setSlot({ diaSemana, inicioMin, dataISO });
        }}
      />

      {slot && entidade && (
        <PopoverAlocar
          aberto
          aoFechar={() => setSlot(null)}
          diaSemana={slot.diaSemana}
          inicioMin={slot.inicioMin}
          dataISO={slot.dataISO}
          semanaVisivelISO={semanaISO}
          hojeISO={hojeISO}
          eixo={eixo}
          entidadeFixa={entidade}
          pacientes={[]}
          terapeutas={terapeutas}
          disciplinas={["aba", "fono", "to", "psicologia"]}
          duracaoPadrao={{ aba: 60, fono: 30, to: 50, psicologia: 50 }}
        />
      )}
    </section>
  );
}

function recuar(iso: string) {
  const d = new Date(`${iso}T00:00:00Z`);
  d.setUTCDate(d.getUTCDate() - 7);
  return d.toISOString().slice(0, 10);
}
function avancar(iso: string) {
  const d = new Date(`${iso}T00:00:00Z`);
  d.setUTCDate(d.getUTCDate() + 7);
  return d.toISOString().slice(0, 10);
}
```

> **Nota ao executor (leitura reativa):** este shell precisa buscar `dados`
> quando `eixo`/`semanaISO`/`entidade` mudam. Padrão recomendado: um route
> handler `GET /agenda/semana/api` (ou server action de leitura) que chama
> `carregarSemana(ctx, {...})` e devolve JSON; disparar via `useEffect`. Como
> `carregarSemana` já valida tenant+papel, o handler fica fino. Preencher
> `pacientes` do popover via `listarPacientes` (debounce sobre `aoBuscar` do
> combobox). Manter a a11y (`color-contrast` ligado) na página inteira.

- [ ] **Step 5: Run popover test to verify it passes**

Run: `pnpm test src/app/(app)/agenda/semana/popover-alocar.a11y.test.tsx`
Expected: PASS.

- [ ] **Step 6: Run typecheck + lint + a11y da pasta**

Run: `pnpm typecheck && pnpm lint && pnpm test "src/app/(app)/agenda/semana"`
Expected: PASS (ajustar imports/props divergentes do DS conforme as notas).

- [ ] **Step 7: Commit**

```bash
git add "src/app/(app)/agenda/semana/"
git commit -m "feat(agenda): popover de alocacao + rota /agenda/semana (etapa C)"
```

---

## Self-Review

**1. Spec coverage (design C1–C10 + §3–§5):**
- C1 (regra grava só `agendamento_recorrente`) → Task 6, testado (`sess` length 0).
- C2/C5 (pré-check app-level 2 dimensões) → Task 2 + Task 6.
- C3 (grade nova, overlay, não paint-toggle) → Task 10.
- C4 (ambas visões alocam) → Task 11 (`pedePaciente`).
- C6 (popover único toggle) → Task 11.
- C7 (vigência não-passado + semana passada off) → Task 1 + Task 6 + shell.
- C8 (fora-janela suave / bloqueio duro) → Task 10.
- C9 (previsto vs concreto) → Task 3 + Task 10 (estilos).
- C10 (fuso avulsa) → Task 5 (`fuso-min`) + Task 7 (`criarAvulsa`).
- §4 UI (page/calendario/popover/combobox) → Tasks 9–11.
- §3 leitura (`carregarSemana`, `listarPacientes`, `disponibilidade...`) → Task 4/5.
- Testes §7 (unit/int/a11y color-contrast on) → cada task.
- **Gap conhecido:** leitura reativa por semana (route handler/server action) está como adendo na nota da Task 11 Step 4, não como task própria — o executor deve materializá-la. `disponibilidadeTerapeutaNoDia` (aviso suave por-paciente, C8) é criada na Task 5 mas seu consumo na UI por-paciente não tem task dedicada; foldar no wiring da Task 11.

**2. Placeholder scan:** sem TODO/TBD; todo código é concreto. As "Notas ao executor" são pontos de verificação contra nomes reais do DS/schema, não placeholders de lógica.

**3. Type consistency:** `Slot`/`Intervalo` (conflito) consistentes entre Task 2/6. `BlocoAgenda`/`OrigemBloco` entre Task 3/5/10. `Opcao` entre Task 9/11. `EstadoAcao` entre Task 8/11. `ConflitoError` entre Task 6/7/8.

## Execution Handoff

Plan completo e salvo em `docs/superpowers/plans/2026-07-18-agenda-2.0-etapa-c-calendario-alocacao.md`.

**Pré-requisitos antes de executar:** branch `feat/agenda-2.0-etapa-c`; Postgres local reset+re-migrate (memory `dev-db-migrate-desync`); confirmar `btree_gist` no banco local.

Duas opções de execução:

1. **Subagent-Driven (recomendado)** — dispatch de subagent fresco por task, review entre tasks, iteração rápida. REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`.
2. **Inline** — executa as tasks nesta sessão com checkpoints. REQUIRED SUB-SKILL: `superpowers:executing-plans`.
