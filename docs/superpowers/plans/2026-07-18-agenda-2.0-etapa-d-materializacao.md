# Etapa D — Materialização IANA — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Gerar as ocorrências `session` a partir das regras `agendamento_recorrente` de forma idempotente, IANA-correta e respeitando bloqueios, mais o ciclo de vida da regra ("esta e futuras" / "estender").

**Architecture:** Lógica pura de data/fuso em `src/lib/agenda/materializar.ts` (sem banco, 100% testável). Escrita/leitura ctx-accepting em `agenda/queries.ts` (fora de `use server`), reusando `withTenant` + savepoint por ocorrência. Server Actions finas em `agenda/semana/actions.ts`. UI mínima no calendário existente. A materialização inicial roda **na mesma transação** de `criarRegra` (atômico).

**Tech Stack:** Next.js 16 (App Router, RSC), React 19, Drizzle 0.45.2 + postgres-js, Better Auth, Tailwind v4, Radix (via DS), Vitest, axe-core.

**Design de referência:** `docs/superpowers/specs/2026-07-18-agenda-2.0-etapa-d-materializacao-design.md` (invariantes D-1..D-10; achados adversariais F1..F5).

## Global Constraints

- RLS multi-tenant `WITH CHECK`; toda leitura/escrita passa por `withTenant(ctx, fn)`. Seed de teste via conexão `owner` (`MIGRATION_DATABASE_URL`) que bypassa RLS.
- ctx-accepting fora de `use server` em `queries.ts`; `actions.ts` só wrappers (`getTenantContext` + repassa).
- **NUNCA componente hardcoded** — reusar `src/components/ui/*` (Dialog etc.).
- a11y: axe `wcag2a/aa` com `color-contrast` religado nas telas novas.
- Intervalos meia-abertos `[início, fim)` em toda comparação de horário.
- **D-1 (IANA):** materialização resolve naive→instante via `resolverInstante` na zona `clinic.timezone`; **nunca** offset fixo congelado.
- **D-2/D-3 (F1, crítico):** insert por ocorrência dentro de savepoint (`tx.transaction`); no `catch`, `23505`→skip silencioso (idempotente), `23P01`→`puladas[]` (overbook), **outro código → rethrow** (aborta). **Não** usar `onConflictDoNothing` (índice parcial complica o arbiter e engoliria o `23P01`).
- **D-5 (F5a):** "esta e futuras" deleta só `estado='agendada' AND agendada_para >= cutoff`; cutoff = início do dia seguinte a `dataFim` (hoje fica, amanhã+ sai). Passado preservado.
- **D-7/DD5:** `criarRegra` materializa o horizonte inicial na **mesma tx** (atômico).
- **D-8 (F3):** `resolverInstante` é o único caminho naive→instante; `criarAvulsa` migra p/ ele.
- Códigos SQLSTATE: `23505` = unique_violation, `23P01` = exclusion_violation. Driver postgres-js expõe o código em `e.code` **ou** `e.cause.code` (drizzle embrulha) — usar `codigoPg(e)`.
- Horizonte default = **12 semanas = 84 dias**.
- Commits: Conventional Commits pt-BR. Branch: `feat/agenda-2.0-etapa-d` (já criada).
- Pré-execução: garantir Postgres local reset+re-migrate (memory `dev-db-migrate-desync` / `dev-env-corepack-docker-db`: `docker compose infra/docker-compose.yml` :5433; `corepack pnpm`). Baseline: falhas `revisao/*` pré-existentes não são regressão.

---

### Task 1: Helpers puros de data/instante (`src/lib/agenda/materializar.ts`)

**Files:**
- Create: `src/lib/agenda/materializar.ts`
- Test: `src/lib/agenda/materializar.test.ts`

**Interfaces:**
- Consumes: nada (strings ISO + `Intl`).
- Produces:
  - `resolverInstante(dataISO: string, horaHHMM: string, fuso: string): Date`
  - `horizontePadrao(hojeISO: string): string` (hoje + 84 dias, ISO)
  - `proximoDia(dataISO: string): string`

- [ ] **Step 1: Write the failing test**

```ts
// src/lib/agenda/materializar.test.ts
import { describe, expect, test } from "vitest";
import { horizontePadrao, proximoDia, resolverInstante } from "./materializar";

describe("resolverInstante (São Paulo, UTC-3 fixo)", () => {
  test("09:00 local vira 12:00Z", () => {
    const r = resolverInstante("2026-07-13", "09:00", "America/Sao_Paulo");
    expect(r.getTime()).toBe(Date.UTC(2026, 6, 13, 12, 0));
  });
  test("aceita hora com segundos (coluna time do Postgres)", () => {
    const r = resolverInstante("2026-07-13", "09:00:00", "America/Sao_Paulo");
    expect(r.getTime()).toBe(Date.UTC(2026, 6, 13, 12, 0));
  });
});

describe("resolverInstante (DST — America/New_York, portabilidade inv1/§8)", () => {
  // Prova que o slot LOCAL não desloca através da virada de horário de verão.
  // 2026-03-08 é o spring-forward nos EUA (EST -5 → EDT -4).
  const fmtNY = (d: Date) =>
    new Intl.DateTimeFormat("en-US", {
      timeZone: "America/New_York", hour: "2-digit", minute: "2-digit", hour12: false,
    }).format(d);
  test("antes da virada: 09:00 EST = 14:00Z", () => {
    const r = resolverInstante("2026-03-07", "09:00", "America/New_York");
    expect(r.getTime()).toBe(Date.UTC(2026, 2, 7, 14, 0));
    expect(fmtNY(r)).toBe("09:00");
  });
  test("depois da virada: 09:00 EDT = 13:00Z (offset mudou, slot local NÃO)", () => {
    const r = resolverInstante("2026-03-09", "09:00", "America/New_York");
    expect(r.getTime()).toBe(Date.UTC(2026, 2, 9, 13, 0));
    expect(fmtNY(r)).toBe("09:00"); // continua 09:00 local — não deslocou 1h
  });
});

describe("horizontePadrao / proximoDia", () => {
  test("horizonte = hoje + 84 dias", () => {
    expect(horizontePadrao("2026-07-18")).toBe("2026-10-10");
  });
  test("proximoDia soma 1 dia (cruza mês)", () => {
    expect(proximoDia("2026-07-31")).toBe("2026-08-01");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test src/lib/agenda/materializar.test.ts`
Expected: FAIL — `Cannot find module './materializar'`.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/lib/agenda/materializar.ts
// Núcleo de materialização — resolução naive→instante IANA-correta (D-1/inv1) e
// aritmética de data em UTC (não sofre DST). NUNCA usa offset fixo congelado.

/** Offset da zona (ms) no instante `ts`: quanto o wall-clock local excede o UTC. */
function offsetMs(ts: number, fuso: string): number {
  const p = Object.fromEntries(
    new Intl.DateTimeFormat("en-US", {
      timeZone: fuso,
      year: "numeric", month: "2-digit", day: "2-digit",
      hour: "2-digit", minute: "2-digit", second: "2-digit", hour12: false,
    })
      .formatToParts(new Date(ts))
      .map((x) => [x.type, x.value]),
  );
  const asUTC = Date.UTC(
    Number(p.year), Number(p.month) - 1, Number(p.day),
    p.hour === "24" ? 0 : Number(p.hour), Number(p.minute), Number(p.second),
  );
  return asUTC - ts;
}

/**
 * Instante absoluto cujo wall-clock em `fuso` é o naive `dataISO`+`horaHHMM`.
 * Inverso de `paraMinutosLocais`. Ponto-fixo de 2 iterações cobre a virada de DST.
 */
export function resolverInstante(dataISO: string, horaHHMM: string, fuso: string): Date {
  const [h, m] = horaHHMM.split(":");
  const [Y, Mo, D] = dataISO.split("-");
  const naive = Date.UTC(Number(Y), Number(Mo) - 1, Number(D), Number(h), Number(m));
  let ts = naive;
  for (let i = 0; i < 2; i++) ts = naive - offsetMs(ts, fuso);
  return new Date(ts);
}

/** Horizonte rolling default: hoje + 12 semanas (84 dias). Aritmética em UTC. */
export function horizontePadrao(hojeISO: string): string {
  const d = new Date(`${hojeISO}T00:00:00Z`);
  d.setUTCDate(d.getUTCDate() + 84);
  return d.toISOString().slice(0, 10);
}

/** Dia seguinte a `dataISO` (ISO). Base do cutoff de encerramento (F5a). */
export function proximoDia(dataISO: string): string {
  const d = new Date(`${dataISO}T00:00:00Z`);
  d.setUTCDate(d.getUTCDate() + 1);
  return d.toISOString().slice(0, 10);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test src/lib/agenda/materializar.test.ts`
Expected: PASS (SP fixo + DST-NY + horizonte).

- [ ] **Step 5: Commit**

```bash
git add src/lib/agenda/materializar.ts src/lib/agenda/materializar.test.ts
git commit -m "feat(agenda): resolverInstante IANA + horizonte/proximoDia (etapa D)"
```

---

### Task 2: Enumeração de datas da regra (`src/lib/agenda/materializar.ts`)

**Files:**
- Modify: `src/lib/agenda/materializar.ts`
- Modify: `src/lib/agenda/materializar.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `interface RegraDatas { diaSemana: number; vigenciaInicio: string; vigenciaFim: string | null }`
  - `interface BloqueioPeriodo { dataInicio: string; dataFim: string }`
  - `datasDaRegra(regra: RegraDatas, deISO: string, ateISO: string, bloqueios: BloqueioPeriodo[]): string[]`
- Semântica: datas ISO do `diaSemana` dentro de `[max(deISO, vigenciaInicio), min(ateISO, vigenciaFim ?? ∞)]`, **excluindo** datas cobertas por qualquer bloqueio (intervalo inclusivo `[dataInicio, dataFim]`). O chamador já filtra os bloqueios por escopo aplicável.

- [ ] **Step 1: Write the failing test**

```ts
// src/lib/agenda/materializar.test.ts  (adicionar)
import { datasDaRegra } from "./materializar";

describe("datasDaRegra", () => {
  const regra = { diaSemana: 1, vigenciaInicio: "2026-07-01", vigenciaFim: null }; // segundas

  test("gera as segundas dentro da janela", () => {
    expect(datasDaRegra(regra, "2026-07-06", "2026-07-27", [])).toEqual([
      "2026-07-06", "2026-07-13", "2026-07-20", "2026-07-27",
    ]);
  });
  test("respeita vigenciaInicio (não gera antes)", () => {
    const r = { diaSemana: 1, vigenciaInicio: "2026-07-13", vigenciaFim: null };
    expect(datasDaRegra(r, "2026-07-01", "2026-07-20", [])).toEqual([
      "2026-07-13", "2026-07-20",
    ]);
  });
  test("respeita vigenciaFim (não gera depois)", () => {
    const r = { diaSemana: 1, vigenciaInicio: "2026-07-01", vigenciaFim: "2026-07-13" };
    expect(datasDaRegra(r, "2026-07-06", "2026-07-27", [])).toEqual([
      "2026-07-06", "2026-07-13",
    ]);
  });
  test("pula datas cobertas por bloqueio (borda inclusiva)", () => {
    const bloqueios = [{ dataInicio: "2026-07-13", dataFim: "2026-07-15" }];
    expect(datasDaRegra(regra, "2026-07-06", "2026-07-27", bloqueios)).toEqual([
      "2026-07-06", "2026-07-20", "2026-07-27", // 13 pulada
    ]);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test src/lib/agenda/materializar.test.ts`
Expected: FAIL — `datasDaRegra` não exportado.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/lib/agenda/materializar.ts  (adicionar)
export interface RegraDatas {
  diaSemana: number;
  vigenciaInicio: string;
  vigenciaFim: string | null;
}
export interface BloqueioPeriodo {
  dataInicio: string;
  dataFim: string;
}

function bloqueada(dataISO: string, bloqueios: BloqueioPeriodo[]): boolean {
  return bloqueios.some((b) => dataISO >= b.dataInicio && dataISO <= b.dataFim);
}

/** Datas ISO do diaSemana da regra em [de, ate] ∩ vigência, menos bloqueios. */
export function datasDaRegra(
  regra: RegraDatas,
  deISO: string,
  ateISO: string,
  bloqueios: BloqueioPeriodo[],
): string[] {
  const inicio = deISO > regra.vigenciaInicio ? deISO : regra.vigenciaInicio;
  const fim = regra.vigenciaFim && regra.vigenciaFim < ateISO ? regra.vigenciaFim : ateISO;
  const datas: string[] = [];
  const cur = new Date(`${inicio}T00:00:00Z`);
  const limite = new Date(`${fim}T00:00:00Z`);
  // avança até o primeiro diaSemana >= inicio
  while (cur.getUTCDay() !== regra.diaSemana && cur <= limite) {
    cur.setUTCDate(cur.getUTCDate() + 1);
  }
  while (cur <= limite) {
    const iso = cur.toISOString().slice(0, 10);
    if (!bloqueada(iso, bloqueios)) datas.push(iso);
    cur.setUTCDate(cur.getUTCDate() + 7);
  }
  return datas;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test src/lib/agenda/materializar.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/agenda/materializar.ts src/lib/agenda/materializar.test.ts
git commit -m "feat(agenda): datasDaRegra com vigencia e bloqueios (etapa D)"
```

---

### Task 3: Materialização no banco (`agenda/queries.ts`)

**Files:**
- Modify: `src/app/(app)/agenda/queries.ts`
- Test: `db/tests/agenda2-materializar-regra.int.test.ts`

**Interfaces:**
- Consumes: `withTenant`/`TenantContext`, `requireRole`, `schema.{agendamentoRecorrente,session,bloqueio,clinic}`, `resolverInstante`/`datasDaRegra`/`horizontePadrao` (`@/lib/agenda/materializar`), drizzle `and/eq/or/gte/lte/max`.
- Produces:
  - `type Tx` local = 1º arg de `withTenant`'s `fn` (reusar o tipo já usado no arquivo).
  - `interface ResultadoMaterializacao { geradas: number; puladas: string[] }`
  - `codigoPg(e: unknown): string | undefined`
  - `async function materializarNaTx(tx, params: { regra; bloqueios: BloqueioPeriodo[]; fuso: string; deISO: string; ateISO: string }): Promise<ResultadoMaterializacao>` (interno, exportado p/ Task 4 reusar)
  - `materializarRegra(ctx: TenantContext, regraId: string, ateISO: string): Promise<ResultadoMaterializacao>`
- Regras (Global Constraints D-2/D-3): insert por savepoint; `23505`→skip, `23P01`→`puladas`, outro→rethrow.

- [ ] **Step 1: Write the failing test**

```ts
// db/tests/agenda2-materializar-regra.int.test.ts
import postgres from "postgres";
import { beforeAll, beforeEach, describe, expect, test, vi } from "vitest";
vi.mock("server-only", () => ({}));
const hasDb = !!process.env.DATABASE_URL && !!process.env.MIGRATION_DATABASE_URL;

const CLINIC_A = "00000000-0000-0000-0000-0000000000d1";
const U_COORD = "00000000-0000-0000-0000-00000000c0d1";
const U_T1 = "00000000-0000-0000-0000-0000000071d1";
const PAC = "00000000-0000-0000-0000-00000000acd1";
const REGRA = "00000000-0000-0000-0000-000000009901";
const ctx = { clinicId: CLINIC_A, userId: U_COORD, role: "coordenador" } as const;

let owner: ReturnType<typeof postgres>;
let materializarRegra: typeof import("@/app/(app)/agenda/queries").materializarRegra;
let appSql: typeof import("@/db/client").sql;

describe.skipIf(!hasDb)("materializarRegra", () => {
  beforeAll(async () => {
    ({ materializarRegra } = await import("@/app/(app)/agenda/queries"));
    ({ sql: appSql } = await import("@/db/client"));
    owner = postgres(process.env.MIGRATION_DATABASE_URL!, { max: 1 });
  });

  beforeEach(async () => {
    await owner`TRUNCATE clinic, app_user, user_role, patient, care_team_membership,
      agendamento_recorrente, session, bloqueio RESTART IDENTITY CASCADE`;
    await owner`INSERT INTO clinic (id, nome, timezone) VALUES (${CLINIC_A}, 'A', 'America/Sao_Paulo')`;
    await owner`INSERT INTO app_user (id, name, email) VALUES
      (${U_COORD}, 'Coord', 'c@d1'), (${U_T1}, 'Tera', 't@d1')`;
    await owner`INSERT INTO user_role (user_id, clinic_id, papel) VALUES
      (${U_COORD}, ${CLINIC_A}, 'coordenador'), (${U_T1}, ${CLINIC_A}, 'terapeuta')`;
    await owner`INSERT INTO patient (id, clinic_id, nome) VALUES (${PAC}, ${CLINIC_A}, 'Ana')`;
    await owner`INSERT INTO agendamento_recorrente
      (id, clinic_id, patient_id, terapeuta_id, disciplina, dia_semana, hora_inicio, duracao_min, vigencia_inicio, status)
      VALUES (${REGRA}, ${CLINIC_A}, ${PAC}, ${U_T1}, 'aba', 1, '09:00', 60, '2026-07-13', 'ativo')`;
  });

  test("gera ocorrências nas segundas do horizonte (D-1)", async () => {
    const r = await materializarRegra(ctx, REGRA, "2026-08-03"); // 4 segundas: 13,20,27/07 + 03/08
    expect(r.geradas).toBe(4);
    expect(r.puladas).toEqual([]);
    const [{ n }] = await owner`SELECT count(*)::int AS n FROM session WHERE recorrente_id = ${REGRA}`;
    expect(n).toBe(4);
    // instante IANA correto: 09:00 SP = 12:00Z na 1ª segunda
    const [{ agendada_para }] = await owner`
      SELECT agendada_para FROM session WHERE recorrente_id = ${REGRA} ORDER BY agendada_para LIMIT 1`;
    expect(new Date(agendada_para).toISOString()).toBe("2026-07-13T12:00:00.000Z");
  });

  test("idempotente: rodar 2× não duplica (D-2, 23505 silencioso)", async () => {
    await materializarRegra(ctx, REGRA, "2026-08-03");
    const r2 = await materializarRegra(ctx, REGRA, "2026-08-03");
    expect(r2.geradas).toBe(0); // nada novo
    const [{ n }] = await owner`SELECT count(*)::int AS n FROM session WHERE recorrente_id = ${REGRA}`;
    expect(n).toBe(4); // estável
  });

  test("overbook: avulsa no slot vira pulada, resto materializa (D-3 / F1)", async () => {
    // avulsa ocupando o terapeuta na 2ª segunda (20/07 09:00 SP = 12:00Z)
    await owner`INSERT INTO session
      (clinic_id, patient_id, terapeuta_id, agendada_para, estado, duracao_min, tipo)
      VALUES (${CLINIC_A}, ${PAC}, ${U_T1}, '2026-07-20T12:00:00Z', 'agendada', 60, 'avaliacao')`;
    const r = await materializarRegra(ctx, REGRA, "2026-08-03");
    expect(r.puladas).toEqual(["2026-07-20"]); // buraco reportado, não engolido
    expect(r.geradas).toBe(3);
  });

  test("pula datas de bloqueio do paciente (D-4)", async () => {
    await owner`INSERT INTO bloqueio (clinic_id, escopo, patient_id, data_inicio, data_fim, motivo)
      VALUES (${CLINIC_A}, 'paciente', ${PAC}, '2026-07-20', '2026-07-20', 'viagem')`;
    const r = await materializarRegra(ctx, REGRA, "2026-08-03");
    expect(r.geradas).toBe(3);
    const datas = await owner`SELECT agendada_para FROM session WHERE recorrente_id = ${REGRA} ORDER BY 1`;
    expect(datas.map((d) => new Date(d.agendada_para).toISOString().slice(0, 10)))
      .not.toContain("2026-07-20");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm test:rls db/tests/agenda2-materializar-regra.int.test.ts`
Expected: FAIL — `materializarRegra` não exportado.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/app/(app)/agenda/queries.ts  (adicionar imports no topo)
import { max, or } from "drizzle-orm";
import {
  datasDaRegra,
  resolverInstante,
  type BloqueioPeriodo,
} from "@/lib/agenda/materializar";

// ... (adicionar ao corpo do arquivo)

export interface ResultadoMaterializacao {
  geradas: number;
  puladas: string[]; // datas ISO puladas por overbook (23P01)
}

/** SQLSTATE do erro do postgres-js (drizzle embrulha em DrizzleQueryError → .cause). */
export function codigoPg(e: unknown): string | undefined {
  return (
    (e as { code?: string } | undefined)?.code ??
    (e as { cause?: { code?: string } } | undefined)?.cause?.code
  );
}

// `Tx` = o tipo do 1º arg do callback de withTenant. Reusa a inferência local.
type TxMat = Parameters<Parameters<typeof withTenant<unknown>>[1]>[0];

interface MaterializarParams {
  regra: {
    id: string;
    clinicId: string;
    patientId: string;
    terapeutaId: string;
    disciplina: string;
    diaSemana: number;
    horaInicio: string;
    duracaoMin: number;
    vigenciaInicio: string;
    vigenciaFim: string | null;
  };
  bloqueios: BloqueioPeriodo[];
  fuso: string;
  deISO: string;
  ateISO: string;
}

/**
 * Materializa as ocorrências da regra DENTRO de uma tx já aberta. Insert por
 * savepoint (tx.transaction): 23505 (retry) → skip silencioso; 23P01 (overbook)
 * → puladas[]; outro código → rethrow (aborta a tx). Reusada por criarRegra (mesma
 * tx, atômico D-7) e materializarRegra (tx própria).
 */
export async function materializarNaTx(
  tx: TxMat,
  { regra, bloqueios, fuso, deISO, ateISO }: MaterializarParams,
): Promise<ResultadoMaterializacao> {
  const datas = datasDaRegra(
    { diaSemana: regra.diaSemana, vigenciaInicio: regra.vigenciaInicio, vigenciaFim: regra.vigenciaFim },
    deISO,
    ateISO,
    bloqueios,
  );
  let geradas = 0;
  const puladas: string[] = [];
  for (const data of datas) {
    const agendadaPara = resolverInstante(data, regra.horaInicio, fuso);
    try {
      await tx.transaction(async (sp) => {
        await sp.insert(schema.session).values({
          clinicId: regra.clinicId,
          patientId: regra.patientId,
          terapeutaId: regra.terapeutaId,
          recorrenteId: regra.id,
          disciplina: regra.disciplina,
          agendadaPara,
          duracaoMin: regra.duracaoMin,
          estado: "agendada",
          tipo: "terapia",
        });
      });
      geradas++;
    } catch (e) {
      const code = codigoPg(e);
      if (code === "23505") continue; // idempotente (uq_session_recorrente_agendada)
      if (code === "23P01") { puladas.push(data); continue; } // overbook (EXCLUDE gist)
      throw e; // erro real → propaga, aborta a tx
    }
  }
  return { geradas, puladas };
}

/** Materializa a regra até `ateISO`, retomando de max(agendada_para)+1dia. */
export async function materializarRegra(
  ctx: TenantContext,
  regraId: string,
  ateISO: string,
): Promise<ResultadoMaterializacao> {
  requireRole(ctx, "coordenador");
  return withTenant(ctx, async (tx) => {
    const [regra] = await tx
      .select()
      .from(schema.agendamentoRecorrente)
      .where(
        and(
          eq(schema.agendamentoRecorrente.id, regraId),
          eq(schema.agendamentoRecorrente.clinicId, ctx.clinicId),
        ),
      );
    if (!regra) return { geradas: 0, puladas: [] };

    const [clinicRow] = await tx
      .select({ timezone: schema.clinic.timezone })
      .from(schema.clinic)
      .where(eq(schema.clinic.id, ctx.clinicId));
    const fuso = clinicRow?.timezone ?? "America/Sao_Paulo";

    const [{ ultimo }] = await tx
      .select({ ultimo: max(schema.session.agendadaPara) })
      .from(schema.session)
      .where(eq(schema.session.recorrenteId, regraId));
    // deISO = max(materializado + 1 dia, vigenciaInicio)
    const deBase = ultimo
      ? new Date(ultimo).toISOString().slice(0, 10)
      : regra.vigenciaInicio;
    const deISO = ultimo
      ? (await import("@/lib/agenda/materializar")).proximoDia(deBase)
      : regra.vigenciaInicio;

    const bloqueios = await tx
      .select({ dataInicio: schema.bloqueio.dataInicio, dataFim: schema.bloqueio.dataFim })
      .from(schema.bloqueio)
      .where(
        and(
          eq(schema.bloqueio.clinicId, ctx.clinicId),
          or(
            eq(schema.bloqueio.escopo, "clinica"),
            and(eq(schema.bloqueio.escopo, "terapeuta"), eq(schema.bloqueio.terapeutaId, regra.terapeutaId)),
            and(eq(schema.bloqueio.escopo, "paciente"), eq(schema.bloqueio.patientId, regra.patientId)),
          ),
          lte(schema.bloqueio.dataInicio, ateISO),
          gte(schema.bloqueio.dataFim, deISO),
        ),
      );

    return materializarNaTx(tx, {
      regra: {
        id: regra.id, clinicId: regra.clinicId, patientId: regra.patientId,
        terapeutaId: regra.terapeutaId, disciplina: regra.disciplina,
        diaSemana: regra.diaSemana, horaInicio: regra.horaInicio, duracaoMin: regra.duracaoMin,
        vigenciaInicio: regra.vigenciaInicio, vigenciaFim: regra.vigenciaFim,
      },
      bloqueios, fuso, deISO, ateISO,
    });
  });
}
```

> **Nota ao executor:** `proximoDia` já é exportado de `@/lib/agenda/materializar` (Task 1) — prefira importá-lo no topo junto de `datasDaRegra`/`resolverInstante` em vez do `await import(...)` inline mostrado (o inline é só p/ deixar o snippet auto-contido). Confirme que `TxMat` compila; se o arquivo já tiver um alias de tipo de tx (ver `withTenant`), reuse-o.

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm test:rls db/tests/agenda2-materializar-regra.int.test.ts`
Expected: PASS (D-1/D-2/D-3/D-4).

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/agenda/queries.ts" db/tests/agenda2-materializar-regra.int.test.ts
git commit -m "feat(agenda): materializarRegra idempotente com savepoint por ocorrencia (etapa D)"
```

---

### Task 4: `criarRegra` materializa na mesma tx (atômico, D-7) + ajuste C1 + atomicidade (F5b)

**Files:**
- Modify: `src/app/(app)/agenda/queries.ts` (`criarRegra`)
- Modify: `db/tests/agenda2-criar-regra.int.test.ts` (ajuste C1)
- Test: `db/tests/agenda2-criar-regra-atomico.int.test.ts` (novo — atomicidade)

**Interfaces:**
- Consumes: `materializarNaTx` (Task 3), `horizontePadrao` (Task 1), `schema.{bloqueio,clinic}`.
- Produces: `criarRegra` inalterada na assinatura (`(ctx, NovaRegra) => Promise<{ id: string }>`), mas agora materializa o horizonte inicial na mesma tx e retorna `{ id }` (comportamento externo estável; o nº de sessions muda de 0 p/ N).
- Nota: a regra recém-inserida tem `id` do `returning`; carregar bloqueios + timezone dentro da mesma tx e chamar `materializarNaTx` com `deISO = regra.vigenciaInicio`, `ateISO = horizontePadrao(dados.hojeISO)`.

- [ ] **Step 1: Write the failing test (ajuste C1 + atomicidade)**

```ts
// db/tests/agenda2-criar-regra.int.test.ts  — AJUSTAR o teste C1 existente:
// o antigo asserta `sess.toHaveLength(0)`. Trocar por:
  test("cria regra E materializa o horizonte na mesma tx (D-7, ex-C1)", async () => {
    const { id } = await criarRegra(ctxA, base); // base.hojeISO = "2026-07-13", segunda
    const [regra] = await appSelectRegra(id); // helper local existente
    expect(regra.vigenciaInicio).toBe("2026-07-13");
    const sess = await owner`SELECT count(*)::int AS n FROM session WHERE recorrente_id = ${id}`;
    expect(sess[0].n).toBeGreaterThanOrEqual(12); // ~12-13 segundas em 84 dias
  });
```

```ts
// db/tests/agenda2-criar-regra-atomico.int.test.ts  (NOVO)
import postgres from "postgres";
import { beforeAll, beforeEach, describe, expect, test, vi } from "vitest";
vi.mock("server-only", () => ({}));
const hasDb = !!process.env.DATABASE_URL && !!process.env.MIGRATION_DATABASE_URL;

const CLINIC_A = "00000000-0000-0000-0000-0000000000d2";
const U_COORD = "00000000-0000-0000-0000-00000000c0d2";
const U_T1 = "00000000-0000-0000-0000-0000000071d2";
const PAC = "00000000-0000-0000-0000-00000000acd2";
const ctx = { clinicId: CLINIC_A, userId: U_COORD, role: "coordenador" } as const;
const base = {
  patientId: PAC, terapeutaId: U_T1, disciplina: "aba", diaSemana: 1,
  horaInicio: "09:00", duracaoMin: 60, semanaVisivelISO: "2026-07-13", hojeISO: "2026-07-13",
};

let owner: ReturnType<typeof postgres>;
let criarRegra: typeof import("@/app/(app)/agenda/queries").criarRegra;

describe.skipIf(!hasDb)("criarRegra — atomicidade (F5b)", () => {
  beforeAll(async () => {
    ({ criarRegra } = await import("@/app/(app)/agenda/queries"));
    owner = postgres(process.env.MIGRATION_DATABASE_URL!, { max: 1 });
  });
  beforeEach(async () => {
    await owner`TRUNCATE clinic, app_user, user_role, patient, agendamento_recorrente, session, bloqueio RESTART IDENTITY CASCADE`;
    await owner`INSERT INTO clinic (id, nome, timezone) VALUES (${CLINIC_A}, 'A', 'America/Sao_Paulo')`;
    await owner`INSERT INTO app_user (id, name, email) VALUES (${U_COORD}, 'C', 'c@d2'), (${U_T1}, 'T', 't@d2')`;
    await owner`INSERT INTO user_role (user_id, clinic_id, papel) VALUES (${U_COORD}, ${CLINIC_A}, 'coordenador'), (${U_T1}, ${CLINIC_A}, 'terapeuta')`;
    await owner`INSERT INTO patient (id, clinic_id, nome) VALUES (${PAC}, ${CLINIC_A}, 'Ana')`;
  });

  test("23P01 numa ocorrência NÃO aborta a criação da regra (regra criada, data pulada)", async () => {
    // avulsa ocupando o terapeuta numa das segundas futuras
    await owner`INSERT INTO session (clinic_id, patient_id, terapeuta_id, agendada_para, estado, duracao_min, tipo)
      VALUES (${CLINIC_A}, ${PAC}, ${U_T1}, '2026-07-20T12:00:00Z', 'agendada', 60, 'avaliacao')`;
    const { id } = await criarRegra(ctx, base);
    const [{ n }] = await owner`SELECT count(*)::int AS n FROM agendamento_recorrente WHERE id = ${id}`;
    expect(n).toBe(1); // regra existe
    const mat = await owner`SELECT count(*)::int AS n FROM session WHERE recorrente_id = ${id}`;
    expect(mat[0].n).toBeGreaterThanOrEqual(11); // 20/07 pulada, resto materializado
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test:rls db/tests/agenda2-criar-regra.int.test.ts db/tests/agenda2-criar-regra-atomico.int.test.ts`
Expected: FAIL — C1 antigo esperava 0 sessions; atômico ainda não materializa.

- [ ] **Step 3: Modify `criarRegra`**

No fim de `criarRegra`, após obter `[row]` do `returning`, antes de `return row!`, materializar na mesma tx:

```ts
// src/app/(app)/agenda/queries.ts  — dentro do withTenant de criarRegra,
// substituir `return row!;` por:
    const fusoRow = await tx
      .select({ timezone: schema.clinic.timezone })
      .from(schema.clinic)
      .where(eq(schema.clinic.id, ctx.clinicId));
    const bloqueios = await tx
      .select({ dataInicio: schema.bloqueio.dataInicio, dataFim: schema.bloqueio.dataFim })
      .from(schema.bloqueio)
      .where(
        and(
          eq(schema.bloqueio.clinicId, ctx.clinicId),
          or(
            eq(schema.bloqueio.escopo, "clinica"),
            and(eq(schema.bloqueio.escopo, "terapeuta"), eq(schema.bloqueio.terapeutaId, dados.terapeutaId)),
            and(eq(schema.bloqueio.escopo, "paciente"), eq(schema.bloqueio.patientId, dados.patientId)),
          ),
        ),
      );
    await materializarNaTx(tx, {
      regra: {
        id: row!.id, clinicId: ctx.clinicId, patientId: dados.patientId,
        terapeutaId: dados.terapeutaId, disciplina: dados.disciplina,
        diaSemana: dados.diaSemana, horaInicio: dados.horaInicio, duracaoMin: dados.duracaoMin,
        vigenciaInicio, vigenciaFim: null,
      },
      bloqueios,
      fuso: fusoRow[0]?.timezone ?? "America/Sao_Paulo",
      deISO: vigenciaInicio,
      ateISO: horizontePadrao(dados.hojeISO),
    });
    return row!;
```

Adicionar `horizontePadrao` ao import de `@/lib/agenda/materializar` e `or` ao import de `drizzle-orm` (se ainda não presente).

- [ ] **Step 4: Run to verify it passes**

Run: `pnpm test:rls db/tests/agenda2-criar-regra.int.test.ts db/tests/agenda2-criar-regra-atomico.int.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/agenda/queries.ts" db/tests/agenda2-criar-regra.int.test.ts db/tests/agenda2-criar-regra-atomico.int.test.ts
git commit -m "feat(agenda): criarRegra materializa horizonte inicial na mesma tx (etapa D)"
```

---

### Task 5: `criarAvulsa` migra p/ `resolverInstante` (F3/D-8) + `codigoPg`

**Files:**
- Modify: `src/app/(app)/agenda/queries.ts` (`criarAvulsa`)
- Test: `db/tests/agenda2-criar-avulsa.int.test.ts` (adicionar não-regressão)

**Interfaces:**
- Consumes: `resolverInstante` (Task 1), `codigoPg` (Task 3), `schema.clinic.timezone`.
- Produces: `criarAvulsa` inalterada na assinatura; internamente ancora via `resolverInstante(dataISO, horaInicio, fuso)` (fuso da clínica) em vez de `new Date(\`...${FUSO_CLINICA_OFFSET}\`)`; catch usa `codigoPg(e)`.

- [ ] **Step 1: Write the failing test (não-regressão)**

```ts
// db/tests/agenda2-criar-avulsa.int.test.ts  (adicionar dentro do describe existente)
  test("ancora via resolverInstante: 09:00 SP grava 12:00Z (não-regressão F3)", async () => {
    const { id } = await criarAvulsa(ctxCoordA, base); // base.dataISO 2026-07-13, horaInicio 09:00
    const [s] = await appSql`SELECT agendada_para FROM session WHERE id = ${id}`;
    expect(new Date(s.agendada_para).toISOString()).toBe("2026-07-13T12:00:00.000Z");
  });
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test:rls db/tests/agenda2-criar-avulsa.int.test.ts`
Expected: PASS já (offset fixo dá o mesmo p/ SP) — este teste **trava o comportamento** antes do refactor. Se já passa, é o baseline; siga ao Step 3 e garanta que continua verde.

- [ ] **Step 3: Refactor `criarAvulsa`**

Carregar o timezone da clínica dentro do `withTenant` e ancorar via `resolverInstante`; simplificar o catch com `codigoPg`:

```ts
// src/app/(app)/agenda/queries.ts  — em criarAvulsa:
// (a) remover a linha `const agendadaPara = new Date(`${dados.dataISO}T${dados.horaInicio}:00${FUSO_CLINICA_OFFSET}`);`
//     de fora do withTenant e resolver o instante DENTRO, após ler o fuso.
  try {
    return await withTenant(ctx, async (tx) => {
      const [clinicRow] = await tx
        .select({ timezone: schema.clinic.timezone })
        .from(schema.clinic)
        .where(eq(schema.clinic.id, ctx.clinicId));
      const agendadaPara = resolverInstante(
        dados.dataISO, dados.horaInicio, clinicRow?.timezone ?? "America/Sao_Paulo",
      );
      const [row] = await tx
        .insert(schema.session)
        .values({
          clinicId: ctx.clinicId,
          patientId: dados.patientId,
          terapeutaId: dados.terapeutaId,
          recorrenteId: null,
          disciplina: dados.disciplina,
          tipo: dados.tipo,
          agendadaPara,
          duracaoMin: dados.duracaoMin,
          estado: "agendada",
          modalidade: dados.modalidade,
        })
        .returning({ id: schema.session.id });
      return row!;
    });
  } catch (e) {
    if (codigoPg(e) === "23P01") throw new ConflitoError("terapeuta");
    throw e;
  }
```

> **Nota:** se `FUSO_CLINICA_OFFSET` deixar de ser usado em `queries.ts` após o refactor, remover o símbolo do import p/ o lint não acusar import não usado. `FUSO_CLINICA` continua em uso (carregarSemana / criarRegra pre-check).

- [ ] **Step 4: Run to verify it passes**

Run: `pnpm test:rls db/tests/agenda2-criar-avulsa.int.test.ts`
Expected: PASS (12:00Z preservado; overbook ainda vira `ConflitoError`).

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/agenda/queries.ts" db/tests/agenda2-criar-avulsa.int.test.ts
git commit -m "refactor(agenda): criarAvulsa ancora via resolverInstante unificado (etapa D)"
```

---

### Task 6: `encerrarRegra` + `contarFuturasDaRegra` + `proximaSessaoDaRegra` (`agenda/queries.ts`)

**Files:**
- Modify: `src/app/(app)/agenda/queries.ts`
- Test: `db/tests/agenda2-encerrar-regra.int.test.ts`

**Interfaces:**
- Consumes: `resolverInstante`/`proximoDia` (Task 1), `schema.{agendamentoRecorrente,session}`, drizzle `gt`, `count`, `min`.
- Produces:
  - `encerrarRegra(ctx, regraId, ateFimISO: string): Promise<{ removidas: number }>` — `cutoff = resolverInstante(proximoDia(ateFimISO), "00:00", fuso)`; `UPDATE status='encerrado', vigencia_fim=ateFimISO`; `DELETE session WHERE recorrente_id=regraId AND estado='agendada' AND agendada_para >= cutoff`.
  - `contarFuturasDaRegra(ctx, regraId, ateFimISO: string): Promise<number>` — mesmo cutoff, `COUNT` sem deletar.
  - `proximaSessaoDaRegra(ctx, regraId): Promise<string | null>` — `min(agendada_para) WHERE agendada_para > now()`, retorna `YYYY-MM-DD` ou null.

- [ ] **Step 1: Write the failing test**

```ts
// db/tests/agenda2-encerrar-regra.int.test.ts
import postgres from "postgres";
import { beforeAll, beforeEach, describe, expect, test, vi } from "vitest";
vi.mock("server-only", () => ({}));
const hasDb = !!process.env.DATABASE_URL && !!process.env.MIGRATION_DATABASE_URL;

const CLINIC_A = "00000000-0000-0000-0000-0000000000d3";
const U_COORD = "00000000-0000-0000-0000-00000000c0d3";
const U_T1 = "00000000-0000-0000-0000-0000000071d3";
const PAC = "00000000-0000-0000-0000-00000000acd3";
const REGRA = "00000000-0000-0000-0000-000000009903";
const ctx = { clinicId: CLINIC_A, userId: U_COORD, role: "coordenador" } as const;

let owner: ReturnType<typeof postgres>;
let q: typeof import("@/app/(app)/agenda/queries");

describe.skipIf(!hasDb)("encerrarRegra / contarFuturas / proximaSessao", () => {
  beforeAll(async () => {
    q = await import("@/app/(app)/agenda/queries");
    owner = postgres(process.env.MIGRATION_DATABASE_URL!, { max: 1 });
  });
  beforeEach(async () => {
    await owner`TRUNCATE clinic, app_user, user_role, patient, agendamento_recorrente, session RESTART IDENTITY CASCADE`;
    await owner`INSERT INTO clinic (id, nome, timezone) VALUES (${CLINIC_A}, 'A', 'America/Sao_Paulo')`;
    await owner`INSERT INTO app_user (id, name, email) VALUES (${U_COORD}, 'C', 'c@d3'), (${U_T1}, 'T', 't@d3')`;
    await owner`INSERT INTO user_role (user_id, clinic_id, papel) VALUES (${U_COORD}, ${CLINIC_A}, 'coordenador'), (${U_T1}, ${CLINIC_A}, 'terapeuta')`;
    await owner`INSERT INTO patient (id, clinic_id, nome) VALUES (${PAC}, ${CLINIC_A}, 'Ana')`;
    await owner`INSERT INTO agendamento_recorrente
      (id, clinic_id, patient_id, terapeuta_id, disciplina, dia_semana, hora_inicio, duracao_min, vigencia_inicio, status)
      VALUES (${REGRA}, ${CLINIC_A}, ${PAC}, ${U_T1}, 'aba', 1, '09:00', 60, '2026-07-06', 'ativo')`;
    // passado realizada (06/07) + futura agendada (20/07, 27/07)
    await owner`INSERT INTO session (clinic_id, patient_id, terapeuta_id, recorrente_id, agendada_para, estado, duracao_min, tipo) VALUES
      (${CLINIC_A}, ${PAC}, ${U_T1}, ${REGRA}, '2026-07-06T12:00:00Z', 'realizada', 60, 'terapia'),
      (${CLINIC_A}, ${PAC}, ${U_T1}, ${REGRA}, '2026-07-20T12:00:00Z', 'agendada', 60, 'terapia'),
      (${CLINIC_A}, ${PAC}, ${U_T1}, ${REGRA}, '2026-07-27T12:00:00Z', 'agendada', 60, 'terapia')`;
  });

  test("contarFuturasDaRegra conta só agendadas a partir de amanhã", async () => {
    // encerrar em 2026-07-13 → futuras = 20 e 27 (2)
    expect(await q.contarFuturasDaRegra(ctx, REGRA, "2026-07-13")).toBe(2);
  });

  test("encerrarRegra remove futuras agendadas e preserva passado (D-5)", async () => {
    const { removidas } = await q.encerrarRegra(ctx, REGRA, "2026-07-13");
    expect(removidas).toBe(2);
    const rest = await owner`SELECT estado FROM session WHERE recorrente_id = ${REGRA} ORDER BY agendada_para`;
    expect(rest.map((r) => r.estado)).toEqual(["realizada"]); // só o passado sobra
    const [regra] = await owner`SELECT status, vigencia_fim FROM agendamento_recorrente WHERE id = ${REGRA}`;
    expect(regra.status).toBe("encerrado");
    expect(String(regra.vigencia_fim)).toBe("2026-07-13");
  });

  test("proximaSessaoDaRegra devolve a menor futura", async () => {
    const prox = await q.proximaSessaoDaRegra(ctx, REGRA);
    expect(prox).toBe("2026-07-20"); // 06/07 já passou
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test:rls db/tests/agenda2-encerrar-regra.int.test.ts`
Expected: FAIL — funções não exportadas.

- [ ] **Step 3: Write implementation**

```ts
// src/app/(app)/agenda/queries.ts  (adicionar `gt`, `count`, `min`, `sql` aos imports de drizzle-orm)
import { count, gt, min, sql } from "drizzle-orm";
import { proximoDia } from "@/lib/agenda/materializar"; // se ainda não importado

async function cutoffEncerramento(
  tx: TxMat, clinicId: string, ateFimISO: string,
): Promise<Date> {
  const [clinicRow] = await tx
    .select({ timezone: schema.clinic.timezone })
    .from(schema.clinic)
    .where(eq(schema.clinic.id, clinicId));
  return resolverInstante(proximoDia(ateFimISO), "00:00", clinicRow?.timezone ?? "America/Sao_Paulo");
}

export async function contarFuturasDaRegra(
  ctx: TenantContext, regraId: string, ateFimISO: string,
): Promise<number> {
  requireRole(ctx, "coordenador");
  return withTenant(ctx, async (tx) => {
    const cutoff = await cutoffEncerramento(tx, ctx.clinicId, ateFimISO);
    const [{ n }] = await tx
      .select({ n: count() })
      .from(schema.session)
      .where(
        and(
          eq(schema.session.recorrenteId, regraId),
          eq(schema.session.estado, "agendada"),
          gte(schema.session.agendadaPara, cutoff),
        ),
      );
    return n;
  });
}

export async function encerrarRegra(
  ctx: TenantContext, regraId: string, ateFimISO: string,
): Promise<{ removidas: number }> {
  requireRole(ctx, "coordenador");
  return withTenant(ctx, async (tx) => {
    const cutoff = await cutoffEncerramento(tx, ctx.clinicId, ateFimISO);
    await tx
      .update(schema.agendamentoRecorrente)
      .set({ status: "encerrado", vigenciaFim: ateFimISO })
      .where(
        and(
          eq(schema.agendamentoRecorrente.id, regraId),
          eq(schema.agendamentoRecorrente.clinicId, ctx.clinicId),
        ),
      );
    const removidas = await tx
      .delete(schema.session)
      .where(
        and(
          eq(schema.session.recorrenteId, regraId),
          eq(schema.session.estado, "agendada"),
          gte(schema.session.agendadaPara, cutoff),
        ),
      )
      .returning({ id: schema.session.id });
    return { removidas: removidas.length };
  });
}

export async function proximaSessaoDaRegra(
  ctx: TenantContext, regraId: string,
): Promise<string | null> {
  requireRole(ctx, "coordenador");
  return withTenant(ctx, async (tx) => {
    const [{ prox }] = await tx
      .select({ prox: min(schema.session.agendadaPara) })
      .from(schema.session)
      .where(
        and(
          eq(schema.session.recorrenteId, regraId),
          eq(schema.session.estado, "agendada"),
          gt(schema.session.agendadaPara, sql`now()`),
        ),
      );
    return prox ? new Date(prox).toISOString().slice(0, 10) : null;
  });
}
```

> **Nota ao executor:** `gte(agendadaPara, cutoff)` com `cutoff` = início do dia seguinte a `ateFimISO` implementa "hoje fica, amanhã+ sai" (F5a). O DELETE de futuras `agendada` não deixa órfão de FK (não há `session_note`/`audio`/`extraction` em sessão futura).

- [ ] **Step 4: Run to verify it passes**

Run: `pnpm test:rls db/tests/agenda2-encerrar-regra.int.test.ts`
Expected: PASS (D-5 + contagem + próxima).

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/agenda/queries.ts" db/tests/agenda2-encerrar-regra.int.test.ts
git commit -m "feat(agenda): encerrarRegra esta-e-futuras + contar/proxima sessao (etapa D)"
```

---

### Task 7: `carregarSemana` lê materializadas como concreto + de-dup previsto (F2)

**Files:**
- Modify: `src/app/(app)/agenda/queries.ts` (`carregarSemana`)
- Test: `db/tests/agenda2-carregar-semana.int.test.ts` (adicionar caso)

**Interfaces:**
- Consumes: `carregarSemana` existente + `paraMinutosLocais`, `projetarSemana`.
- Produces: `carregarSemana` inalterada na assinatura. Mudança de comportamento: (a) o branch de sessões concretas passa a ler **também** as materializadas (`recorrenteId IS NOT NULL`, `estado='agendada'`) — hoje só lê avulsas (`isNull(recorrenteId)`); (b) uma regra só é projetada como `previsto` **se não houver sessão concreta dela naquela ocorrência** na semana (de-dup por `recorrenteId`), evitando bloco duplicado. Ocorrência prevista-sem-concreto = o buraco visível (F2).

- [ ] **Step 1: Write the failing test**

```ts
// db/tests/agenda2-carregar-semana.int.test.ts  (adicionar)
  test("materializada aparece como bloco concreto, sem duplicar o previsto (F2)", async () => {
    // regra segunda 09:00 + 1 sessão materializada dela na semana visível
    await owner`INSERT INTO agendamento_recorrente
      (id, clinic_id, patient_id, terapeuta_id, disciplina, dia_semana, hora_inicio, duracao_min, vigencia_inicio, status)
      VALUES (${REGRA}, ${CLINIC_A}, ${PAC}, ${TERA}, 'aba', 1, '09:00', 60, '2026-07-13', 'ativo')`;
    await owner`INSERT INTO session (clinic_id, patient_id, terapeuta_id, recorrente_id, agendada_para, estado, duracao_min, disciplina, tipo)
      VALUES (${CLINIC_A}, ${PAC}, ${TERA}, ${REGRA}, '2026-07-13T12:00:00Z', 'agendada', 60, 'aba', 'terapia')`;
    const r = await carregarSemana(ctxA, { eixo: "terapeuta", entidadeId: TERA, semanaInicioISO: "2026-07-13" });
    const naSegunda = r.blocos.filter((b) => b.diaSemana === 1 && b.inicioMin === 540);
    expect(naSegunda).toHaveLength(1); // NÃO duplica previsto+concreto
    expect(naSegunda[0].origem).toBe("concreto"); // a materializada vence
  });
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test:rls db/tests/agenda2-carregar-semana.int.test.ts`
Expected: FAIL — hoje o branch concreto ignora materializadas (só avulsa) e projeta previsto → 2 blocos.

- [ ] **Step 3: Modify `carregarSemana`**

Duas mudanças no `withTenant` de `carregarSemana`:

1. Remover o filtro `isNull(schema.session.recorrenteId)` da query de sessões concretas (passa a trazer avulsas **e** materializadas `estado='agendada'` na janela de datas). Adicionar `eq(schema.session.estado, "agendada")` se ainda não presente. Guardar o `recorrenteId` de cada linha concreta num `Set<string>`:

```ts
    const recorrentesConcretos = new Set(
      avulsasRaw.filter((a) => a.recorrenteId).map((a) => a.recorrenteId as string),
    );
```
(Incluir `recorrenteId: schema.session.recorrenteId` no `select` dessa query.)

2. Ao montar `regras` (previsto), filtrar as que já têm concreto na semana:

```ts
    const regras: RegraProjecao[] = regrasRaw
      .filter((r) => !recorrentesConcretos.has(r.id))
      .map((r) => ({ /* ...igual ao existente... */ }));
```

> **Nota ao executor:** confirme o nome do array cru das sessões concretas no código atual (o snippet do plano C chamava `avulsasRaw`; se o nome mudou, ajuste). A projeção `projetarSemana(regras, avulsas)` continua igual — só o conteúdo dos arrays muda. O de-dup é por `recorrenteId` = `RegraProjecao.id`.

- [ ] **Step 4: Run to verify it passes**

Run: `pnpm test:rls db/tests/agenda2-carregar-semana.int.test.ts`
Expected: PASS (1 bloco concreto; previsto de-duplicado).

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/agenda/queries.ts" db/tests/agenda2-carregar-semana.int.test.ts
git commit -m "feat(agenda): carregarSemana le materializadas e de-duplica previsto (etapa D)"
```

---

### Task 8: Server Actions `estenderAction` + `encerrarRegraAction` (`agenda/semana/actions.ts`)

**Files:**
- Modify: `src/app/(app)/agenda/semana/actions.ts`
- Test: `db/tests/agenda2-semana-actions-etapa-d.int.test.ts`

**Interfaces:**
- Consumes: `getTenantContext`, `materializarRegra`/`encerrarRegra`/`contarFuturasDaRegra` (Tasks 3/6), `horizontePadrao` (Task 1), `RoleError`, `EstadoAcao`/`trata` (existentes).
- Produces:
  - `type EstadoEstender = EstadoAcao & { geradas?: number; puladas?: number }`
  - `estenderAction(_prev: EstadoEstender, formData: FormData): Promise<EstadoEstender>` — lê `regraId`, `hojeISO`; `ateISO = horizontePadrao(hojeISO)`; chama `materializarRegra`; retorna `{ ok, geradas, puladas: puladas.length }`.
  - `encerrarRegraAction(_prev: EstadoAcao, formData: FormData): Promise<EstadoAcao>` — lê `regraId`, `ateFimISO`; chama `encerrarRegra`; `{ ok: true }`.
  - `contarFuturasAction(regraId: string, ateFimISO: string): Promise<number>` — leitura fina p/ a confirmação (F5).

- [ ] **Step 1: Write the failing test (defesa de papel)**

```ts
// db/tests/agenda2-semana-actions-etapa-d.int.test.ts
import { describe, expect, test, vi } from "vitest";
vi.mock("server-only", () => ({}));
vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }));
const getTenantContext = vi.fn();
vi.mock("@/auth/tenant", () => ({ getTenantContext: () => getTenantContext() }));
import { encerrarRegraAction, estenderAction } from "@/app/(app)/agenda/semana/actions";

describe("actions Etapa D (defesa em profundidade)", () => {
  test("estenderAction: papel não-coordenador retorna erro", async () => {
    getTenantContext.mockResolvedValue({ clinicId: "c", userId: "u", role: "terapeuta" });
    const fd = new FormData();
    fd.set("regraId", "r1"); fd.set("hojeISO", "2026-07-18");
    const r = await estenderAction({}, fd);
    expect(r.error).toBeTruthy();
    expect(r.ok).toBeUndefined();
  });
  test("encerrarRegraAction: papel não-coordenador retorna erro", async () => {
    getTenantContext.mockResolvedValue({ clinicId: "c", userId: "u", role: "terapeuta" });
    const fd = new FormData();
    fd.set("regraId", "r1"); fd.set("ateFimISO", "2026-07-18");
    const r = await encerrarRegraAction({}, fd);
    expect(r.error).toBeTruthy();
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test db/tests/agenda2-semana-actions-etapa-d.int.test.ts`
Expected: FAIL — actions não exportadas.

- [ ] **Step 3: Implement actions**

```ts
// src/app/(app)/agenda/semana/actions.ts  (adicionar)
import {
  contarFuturasDaRegra,
  encerrarRegra,
  materializarRegra,
} from "@/app/(app)/agenda/queries";
import { horizontePadrao } from "@/lib/agenda/materializar";

export type EstadoEstender = EstadoAcao & { geradas?: number; puladas?: number };

export async function estenderAction(
  _prev: EstadoEstender,
  formData: FormData,
): Promise<EstadoEstender> {
  const ctx = await getTenantContext();
  const regraId = String(formData.get("regraId"));
  const hojeISO = String(formData.get("hojeISO"));
  try {
    const { geradas, puladas } = await materializarRegra(ctx, regraId, horizontePadrao(hojeISO));
    revalidatePath("/agenda/semana");
    return { ok: true, geradas, puladas: puladas.length };
  } catch (e) {
    return trata(e);
  }
}

export async function encerrarRegraAction(
  _prev: EstadoAcao,
  formData: FormData,
): Promise<EstadoAcao> {
  const ctx = await getTenantContext();
  try {
    await encerrarRegra(ctx, String(formData.get("regraId")), String(formData.get("ateFimISO")));
  } catch (e) {
    return trata(e);
  }
  revalidatePath("/agenda/semana");
  return { ok: true };
}

/** Leitura fina p/ a confirmação de encerramento (F5): quantas futuras sairão. */
export async function contarFuturasAction(regraId: string, ateFimISO: string): Promise<number> {
  const ctx = await getTenantContext();
  return contarFuturasDaRegra(ctx, regraId, ateFimISO);
}
```

- [ ] **Step 4: Run to verify it passes**

Run: `pnpm test db/tests/agenda2-semana-actions-etapa-d.int.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/agenda/semana/actions.ts" db/tests/agenda2-semana-actions-etapa-d.int.test.ts
git commit -m "feat(agenda): actions estender/encerrar/contar-futuras (etapa D)"
```

---

### Task 9: UI — overlay de regra interativo (próxima sessão + estender + encerrar) (F2/F4/F5)

**Files:**
- Modify: `src/app/(app)/agenda/semana/calendario-semana.tsx` (overlay de bloco `previsto`/`concreto` vira acionável p/ regra)
- Modify: `src/app/(app)/agenda/semana/semana-cliente.tsx` (estado do popover de regra + wiring das actions + Dialog de confirmação)
- Create: `src/app/(app)/agenda/semana/popover-regra.tsx` (novo componente de detalhe da regra)
- Test: `src/app/(app)/agenda/semana/popover-regra.a11y.test.tsx`

**Interfaces:**
- Consumes: `estenderAction`/`encerrarRegraAction`/`contarFuturasAction` (Task 8), `useActionState` (React 19), DS `Dialog`/`Button` de `@/components/ui/*` (padrão de `popover-alocar.tsx`).
- Produces: `<PopoverRegra>` — recebe `{ regraId, rotulo, proximaSessaoISO, hojeISO, onClose }`; mostra **"próxima sessão: DD/MM"** (F4), botão **"Estender +12 semanas"** (resultado inline com nº gerado/pulado — F2), botão **"Encerrar (esta e futuras)"** que abre confirmação com a contagem de `contarFuturasAction` (F5).
- Nota de dados: para a UI ter `proximaSessaoISO` por regra, `carregarSemana`/`SemanaCarregada` deve expor, por bloco `previsto`/`concreto` de origem recorrente, o `recorrenteId` e (opcional) a próxima sessão. Mínimo viável: o popover chama uma leitura fina no `onOpen` (nova `proximaSessaoAction(regraId)` análoga a `contarFuturasAction`) em vez de pré-carregar — evita inchar `SemanaCarregada`. **Adicionar `proximaSessaoAction` em `actions.ts` (espelha `contarFuturasAction`, chamando `proximaSessaoDaRegra`).**

- [ ] **Step 1: Write the failing a11y test**

```tsx
// src/app/(app)/agenda/semana/popover-regra.a11y.test.tsx
import { render } from "@testing-library/react";
import { axe } from "vitest-axe";
import { describe, expect, test, vi } from "vitest";
import { PopoverRegra } from "./popover-regra";

vi.mock("./actions", () => ({
  estenderAction: vi.fn(),
  encerrarRegraAction: vi.fn(),
  contarFuturasAction: vi.fn(async () => 3),
  proximaSessaoAction: vi.fn(async () => "2026-07-20"),
}));

describe("PopoverRegra a11y", () => {
  test("sem violações axe (wcag2a/aa)", async () => {
    const { container } = render(
      <PopoverRegra
        regraId="r1" rotulo="Ana · aba" proximaSessaoISO="2026-07-20"
        hojeISO="2026-07-18" onClose={() => {}}
      />,
    );
    const results = await axe(container, {
      rules: { "color-contrast": { enabled: true } },
    });
    expect(results).toHaveNoViolations();
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test src/app/(app)/agenda/semana/popover-regra.a11y.test.tsx`
Expected: FAIL — `./popover-regra` inexistente.

- [ ] **Step 3: Implement `PopoverRegra` + wiring**

Criar `popover-regra.tsx` (Client Component) reusando **exclusivamente** DS (`Button`, `Dialog*`). Estrutura mínima:

```tsx
// src/app/(app)/agenda/semana/popover-regra.tsx
"use client";
import { useActionState, useState } from "react";
import { Button } from "@/components/ui/button";
import {
  Dialog, DialogContent, DialogDescription, DialogTitle,
} from "@/components/ui/dialog";
import {
  contarFuturasAction, encerrarRegraAction, estenderAction,
  type EstadoEstender,
} from "./actions";

function formatarBR(iso: string | null): string {
  if (!iso) return "—";
  const [y, m, d] = iso.split("-");
  return `${d}/${m}/${y}`;
}

export function PopoverRegra({
  regraId, rotulo, proximaSessaoISO, hojeISO, onClose,
}: {
  regraId: string;
  rotulo: string;
  proximaSessaoISO: string | null;
  hojeISO: string;
  onClose: () => void;
}) {
  const [estender, estenderFn, estPending] = useActionState<EstadoEstender, FormData>(
    estenderAction, {},
  );
  const [encerrar, encerrarFn, encPending] = useActionState(encerrarRegraAction, {});
  const [confirmando, setConfirmando] = useState(false);
  const [futuras, setFuturas] = useState<number | null>(null);

  async function abrirConfirmacao() {
    setFuturas(await contarFuturasAction(regraId, hojeISO)); // F5: contagem real
    setConfirmando(true);
  }

  return (
    <Dialog open onOpenChange={(o) => !o && onClose()}>
      <DialogContent>
        <DialogTitle>{rotulo}</DialogTitle>
        {/* F4: rótulo honesto */}
        <DialogDescription>
          Próxima sessão: {formatarBR(proximaSessaoISO)}
        </DialogDescription>

        {/* F2: estender declara alvo + reporta gerado/pulado inline (não toast) */}
        <form action={estenderFn}>
          <input type="hidden" name="regraId" value={regraId} />
          <input type="hidden" name="hojeISO" value={hojeISO} />
          <Button type="submit" disabled={estPending}>Estender +12 semanas</Button>
        </form>
        {estender.ok && (
          <p role="status">
            {estender.geradas} sessões criadas
            {estender.puladas ? `, ${estender.puladas} não criadas por conflito` : ""}.
          </p>
        )}

        {/* F5: encerrar com confirmação numérica */}
        {!confirmando ? (
          <Button type="button" variant="destructive" onClick={abrirConfirmacao}>
            Encerrar (esta e futuras)
          </Button>
        ) : (
          <form action={encerrarFn}>
            <input type="hidden" name="regraId" value={regraId} />
            <input type="hidden" name="ateFimISO" value={hojeISO} />
            <p>
              Remove {futuras ?? 0} sessões futuras (a partir de amanhã). As de hoje
              e o histórico ficam.
            </p>
            <Button type="submit" variant="destructive" disabled={encPending}>
              Confirmar encerramento
            </Button>
            <Button type="button" onClick={() => setConfirmando(false)}>Cancelar</Button>
          </form>
        )}
        {(estender.error || encerrar.error) && (
          <p role="alert">{estender.error ?? encerrar.error}</p>
        )}
      </DialogContent>
    </Dialog>
  );
}
```

Adicionar `proximaSessaoAction` em `actions.ts` (espelha `contarFuturasAction`):

```ts
// src/app/(app)/agenda/semana/actions.ts  (adicionar)
import { proximaSessaoDaRegra } from "@/app/(app)/agenda/queries";
export async function proximaSessaoAction(regraId: string): Promise<string | null> {
  const ctx = await getTenantContext();
  return proximaSessaoDaRegra(ctx, regraId);
}
```

Wiring em `semana-cliente.tsx` + `calendario-semana.tsx`: tornar o overlay de bloco de origem recorrente (`b.origem` `"previsto"` ou `"concreto"` **com** `recorrenteId`) acionável — trocar o `<div aria-hidden>` por um `<button>` acessível que seta `regraSelecionada` no `SemanaCliente`, que renderiza `<PopoverRegra>`. Passar `hojeISO` (o `SemanaCliente` já conhece a data corrente).

> **Nota ao executor (dados p/ o botão):** o bloco precisa carregar o `recorrenteId`. Se `BlocoAgenda` ainda não o expõe, adicionar `recorrenteId?: string` a `BlocoAgenda`/`projetarSemana` e preenchê-lo na projeção (regra → o próprio `id`; materializada → `recorrente_id` da session). Overlays sem `recorrenteId` (avulsa pura) continuam não-interativos (`aria-hidden`). Confirme os nomes reais dos componentes DS `Dialog*`/`Button` em `@/components/ui/*` (ver `popover-alocar.tsx`) e ajuste os imports/props se divergirem. Rode o axe **e** o typecheck.

- [ ] **Step 4: Run to verify it passes**

Run: `pnpm test src/app/(app)/agenda/semana/popover-regra.a11y.test.tsx && pnpm typecheck`
Expected: PASS (sem violações axe; typecheck limpo).

- [ ] **Step 5: Commit**

```bash
git add "src/app/(app)/agenda/semana/popover-regra.tsx" "src/app/(app)/agenda/semana/semana-cliente.tsx" "src/app/(app)/agenda/semana/calendario-semana.tsx" "src/app/(app)/agenda/semana/actions.ts" "src/app/(app)/agenda/semana/popover-regra.a11y.test.tsx"
git commit -m "feat(agenda): popover de regra com proxima-sessao/estender/encerrar (etapa D)"
```

---

### Task 10: Verificação final da Etapa D

**Files:** nenhum (gate).

- [ ] **Step 1: Suite completa**

Run: `pnpm lint && pnpm typecheck && pnpm test && pnpm test:rls`
Expected: verde (exceto baseline `revisao/*` conhecido). Buildar: `pnpm build`.

- [ ] **Step 2: Atualizar BACKLOG.md**

Registrar Etapa D concluída; mover dívidas mantidas (cron automático, grupo/co-terapia) p/ a seção de dívida explícita; nota da decisão de implementação F1 (savepoint em vez de onConflictDoNothing) e F3 (ancoragem unificada).

- [ ] **Step 3: Commit + push**

```bash
git add BACKLOG.md
git commit -m "docs(agenda): fecha BACKLOG da Etapa D (materializacao)"
git push -u origin feat/agenda-2.0-etapa-d
```

- [ ] **Step 4: `graphify update .`** (regra global do projeto — grafo reflete a realidade).

---

## Self-Review (cobertura do spec)

- **D-1 IANA** → Task 1 (`resolverInstante` + DST-NY) + Task 3 (uso na materialização). ✅
- **D-2 idempotente** → Task 3 (`23505` skip) + teste "roda 2×". ✅
- **D-3 duplo-book pula+reporta (F1)** → Task 3 (savepoint `23P01`→`puladas`) + teste overbook. ✅
- **D-4 pula bloqueio** → Task 2 (`datasDaRegra`) + Task 3 (teste bloqueio paciente). ✅
- **D-5 esta-e-futuras (F5a)** → Task 6 (`encerrarRegra` cutoff) + teste passado-preservado. ✅
- **D-6 rolling on-demand** → Task 3 (`deISO`=max+1) + Task 8 (`estenderAction`). ✅
- **D-7 atômico (F5b)** → Task 4 (materializa na mesma tx) + teste atomicidade. ✅
- **D-8 ancoragem unificada (F3)** → Task 5 (`criarAvulsa`→`resolverInstante`). ✅
- **D-9 rótulo honesto (F4)** → Task 6 (`proximaSessaoDaRegra`) + Task 9 ("próxima sessão"). ✅
- **D-10 confirmação numérica (F5)** → Task 6 (`contarFuturasDaRegra`) + Task 9 (Dialog com contagem). ✅
- **F2 buraco visível persistente** → Task 7 (previsto-sem-concreto = célula visível) + Task 9 (resultado inline gerado/pulado). ✅
- **Testes §8** (unit resolver/datas, integração idempotência/EXCLUDE/encerrar/RLS, DST, a11y) → Tasks 1–9. ✅

Lacuna consciente registrada: **RLS/IDOR explícito** — cada teste de integração usa `ctx` de um tenant e seed via `owner`; um teste dedicado de vazamento cross-tenant (regra da clínica B não materializa sob ctx A) **fica implícito** no padrão RLS de `withTenant`. Se o executor quiser paridade com Etapas B/C, adicionar 1 caso cross-tenant em `agenda2-materializar-regra.int.test.ts`.
