# Agenda 2.0 — Etapa E+F Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fechar a Agenda 2.0 — dar estado de vida à sessão (realizada/falta/cancelada + substituto + reposição rastreável + modalidade) na grade do dia, e derivar as métricas de carga horária por disciplina nos perfis de paciente e terapeuta.

**Architecture:** Reusa 100% da fundação já migrada (Etapa A) — nenhuma coluna nova. Mutação de estado com CAS otimista (`WHERE estado='agendada'`, sem coluna `versao`). Reposição delega colisão ao `EXCLUDE gist` da Etapa C. Métricas são funções puras sobre queries `withTenant`. `disciplina` vira vocabulário controlado (chaves de `clinic.duracaoDisciplina`).

**Tech Stack:** Next.js 16 App Router, React 19, Drizzle ORM + Postgres, Tailwind v4 (design system "Espectro Brutal"), Vitest (unit + integração com Postgres), axe-core (a11y).

## Global Constraints

- Copy/UI sempre em **pt-BR**.
- Termo de UI para quem atende = **"Terapeuta"** — NUNCA "Profissional".
- **Reusar componentes do design system** (`src/components/ui/`): `Button`, `Dialog`, `Select`, `Field`, `Alert`, `Stack`/`Cluster`/`Split`. Nunca hardcodar componente que já existe. `ComboboxEntidade` (feature agenda) para seleção por nome.
- `disciplina` de qualquer escrita nova ∈ vocabulário da clínica (`Object.keys(clinic.duracaoDisciplina)`).
- **RLS é backstop** — mudanças de gate de papel na camada de app não substituem RLS.
- TDD: teste falha primeiro, implementação mínima, teste passa, commit. Commits frequentes (Conventional Commits pt-BR, `feat(agenda):`/`fix(agenda):`/etc; sufixo `(etapa E)`/`(etapa F)`).
- Ambiente: `corepack pnpm <cmd>`. Postgres local via `docker compose -f infra/docker-compose.yml up -d` (porta 5433). Testes de integração: `pnpm test:rls` com `DATABASE_URL` + `MIGRATION_DATABASE_URL` setados. Unit/a11y: `pnpm test`.
- Cada task termina com `pnpm typecheck` + `pnpm lint` limpos antes do commit.

---

# BLOCO 0 — Pré-requisitos (destrava E e F)

## Task 1: Migration — backfill `session.disciplina` + NOT NULL

**Files:**

- Create: `db/migrations/0036_agenda2_disciplina_notnull.sql`
- Modify: `db/migrations/meta/_journal.json` (nova entrada idx 36)
- Modify: `src/db/schema.ts:538` (`disciplina` → `.notNull()`)
- Create/Modify: snapshot em `db/migrations/meta/` **espelhando exatamente** o que a migration à mão mais recente (`0035_agenda2_session_exclude_overbook`) fez (mesmo bookkeeping de journal+snapshot; não reinventar).

**Interfaces:**

- Produces: `session.disciplina` passa a ser `text NOT NULL`; nenhuma linha null resta.

**Decisão travada:** backfill `null → 'desconhecida'`. Racional: linhas legadas com `disciplina IS NULL` são sessões pré-Agenda-2.0 (era do check-in Fase 1d), anteriores ao conceito de disciplina. Marcá-las com uma disciplina real (ex. `'aba'`) poluiria o realizado daquela disciplina. `'desconhecida'` não é chave de `duracaoDisciplina` de nenhuma clínica → fica naturalmente fora das tabelas de métrica (que iteram as disciplinas da clínica).

- [ ] **Step 1: Escrever a migration à mão**

`db/migrations/0036_agenda2_disciplina_notnull.sql`:

```sql
-- Agenda 2.0 (Etapa E+F) — disciplina vira obrigatória.
-- `disciplina` era text livre nullable em `session` (avulsas/legado sem valor).
-- A métrica por disciplina (Etapa F) junta alvo×agendado×realizado por essa
-- chave; null quebra o join. Backfill de linhas legadas com 'desconhecida'
-- (sessões pré-Agenda-2.0, anteriores ao conceito) — valor que não é chave de
-- `clinic.duracao_disciplina`, então fica fora de qualquer métrica de disciplina
-- real em vez de falsear uma. Depois, NOT NULL. Tudo numa transação implícita
-- (drizzle roda o arquivo atômico).
UPDATE "session" SET "disciplina" = 'desconhecida' WHERE "disciplina" IS NULL;
--> statement-breakpoint
ALTER TABLE "session" ALTER COLUMN "disciplina" SET NOT NULL;
```

- [ ] **Step 2: Atualizar `schema.ts`**

`src/db/schema.ts:538` — de `disciplina: text("disciplina"),` para:

```ts
    disciplina: text("disciplina").notNull(),
```

(Ajustar o comentário 534-537 que diz "null = avulsa": avulsa agora carrega a disciplina escolhida no popover, nunca null.)

- [ ] **Step 3: Adicionar entrada no journal**

`db/migrations/meta/_journal.json` — nova entrada no array `entries`, seguindo o padrão do `+1000ms` sobre a anterior (idx 35 = `when 1784229603545`):

```json
{
  "idx": 36,
  "version": "7",
  "when": 1784229604545,
  "tag": "0036_agenda2_disciplina_notnull",
  "breakpoints": true
}
```

Gerar/copiar o snapshot correspondente do mesmo jeito que `0035` fez (verifique se há `0035_snapshot.json` em `meta/` e replique o bookkeeping para `0036`).

- [ ] **Step 4: Aplicar no Postgres local e verificar**

```bash
docker compose -f infra/docker-compose.yml up -d
# aplicar a migration (se pnpm db:migrate falhar por desync de tracking,
# aplicar o SQL à mão via psql — ver memória "dev-db-migrate-desync"):
corepack pnpm db:migrate
```

Verificar 0 nulls:

```sql
SELECT count(*) FROM session WHERE disciplina IS NULL;  -- espera 0
```

- [ ] **Step 5: Commit**

```bash
git add db/migrations/0036_agenda2_disciplina_notnull.sql db/migrations/meta/ src/db/schema.ts
git commit -m "feat(agenda): disciplina NOT NULL + backfill legado 'desconhecida' (etapa E+F)"
```

---

## Task 2: Gate `requireAgendar` — recepção agenda no fluxo novo

**Files:**

- Modify: `src/app/(app)/agenda/queries.ts` (funções de agendar/ler: trocar `requireRole(ctx,"coordenador")` por `requireAgendar(ctx)`)
- Create: helper `requireAgendar` em `src/auth/require-role.ts` (ou arquivo vizinho, onde `requireRole` vive)
- Test: `src/app/(app)/agenda/semana/actions.int.test.ts` (criar se não existir; senão estender)

**Interfaces:**

- Consumes: `requireRole` existente.
- Produces: `requireAgendar(ctx: TenantContext): void` — permite `coordenador` **e** `admin_recepcao`; lança `RoleError` senão.

**Quais funções abrir** (agendar/ler): `listarPacientes` (`queries.ts:28`), `carregarSemana` (`:59`), `carregarConfigClinica` (`:469`), `criarRegra` (`:238`), `criarAvulsa`, `materializarRegra` (`:713`). **Permanecem `coordenador`-only** (estrutural): `encerrarRegra` (`:791`), `conflitosNaTx`/edição de regra (`:623,633`), config de disponibilidade/bloqueio.

- [ ] **Step 1: Teste de integração falhando** — recepção consegue listar pacientes; terapeuta puro não.

`src/app/(app)/agenda/semana/actions.int.test.ts` (padrão de `agenda/actions.int.test.ts`: `hasDb` gating, `vi.mock("server-only")`, import dinâmico, ctx literais `ctxCoord`/`ctxAdmin`/`ctxT1`, setup owner com TRUNCATE+seed):

```ts
it("recepção lista pacientes (gate agendar)", async () => {
  const r = await listarPacientes(ctxAdmin, "");
  expect(Array.isArray(r)).toBe(true);
});
it("terapeuta puro é barrado de listar pacientes", async () => {
  await expect(listarPacientes(ctxT1, "")).rejects.toBeInstanceOf(RoleError);
});
```

- [ ] **Step 2: Rodar — falha** (`admin_recepcao` hoje é barrado por `requireRole coordenador`).
      Run: `corepack pnpm test:rls -- semana/actions.int.test.ts`
      Expected: FAIL (recepção recebe RoleError).

- [ ] **Step 3: Implementar `requireAgendar` e aplicá-lo**

Em `src/auth/require-role.ts`:

```ts
export function requireAgendar(ctx: TenantContext): void {
  requireRole(ctx, "coordenador", "admin_recepcao");
}
```

Nas funções de agendar/ler de `queries.ts`, trocar `requireRole(ctx, "coordenador")` por `requireAgendar(ctx)` (só nas listadas acima — NÃO nas estruturais).

- [ ] **Step 4: Rodar — passa.**
      Run: `corepack pnpm test:rls -- semana/actions.int.test.ts` → PASS.

- [ ] **Step 5: Commit**

```bash
git commit -am "feat(agenda): recepção agenda no fluxo novo via requireAgendar (etapa E)"
```

---

## Task 3: Aposentar form legado + rename "Terapeuta"

**Files:**

- Delete: `src/app/(app)/agenda/agendar-form.tsx`
- Modify: `src/app/(app)/agenda/actions.ts` (remover `agendarSessao`, `agendarSessaoAction`, `agendarSchema`, `AgendarState`)
- Modify: `src/app/(app)/agenda/page.tsx` (remover import + bloco `<section>` "Agendar sessão" 118-133; adicionar link "Agendar → /agenda/semana")
- Modify: sweep de strings "Profissional"→"Terapeuta" em componentes de UI (ver Step 3)

- [ ] **Step 1: Remover o form e a action legada**

Deletar `agendar-form.tsx`. Em `actions.ts` remover `agendarSchema`, `agendarSessao`, `agendarSessaoAction`, tipo `AgendarState` e imports órfãos (`requireRole` continua usado por `checkInSessao`? verificar; `z` continua se outra action usa). Manter `listarSessoesDoDia`, `checkInSessao`, `checkInAction`, `SessaoDoDia`, `SessionEstado`.

- [ ] **Step 2: Ajustar `/agenda/page.tsx`**

Remover `import { AgendarForm }` e o bloco `{podeAgendar ? (<Stack …>…<AgendarForm /></Stack>) : null}` (118-133). No lugar, para `podeAgendar`, um link para o calendário (reusa `abrirSessaoClasses` ou `Button` as-link):

```tsx
{
  podeAgendar ? (
    <div className="animate-fade-in-up animate-delay-225 border-graphite border-t-2 border-dashed pt-6">
      <Link href="/agenda/semana" className={abrirSessaoClasses}>
        Agendar no calendário
      </Link>
    </div>
  ) : null;
}
```

`podeAgendar` já cobre `coordenador || admin_recepcao` (`page.tsx:55`).

- [ ] **Step 3: Sweep "Profissional" → "Terapeuta" na UI**

Grep `grep -rn "profissional\|Profissional" src/` e corrigir **apenas strings de UI visíveis** (label/placeholder/aria/mensagem de erro). Alvos conhecidos: mensagens zod em `actions.ts` que sobreviverem; labels em `equipe/convidar/`, `pacientes/[id]/equipe/adicionar-membro-form.tsx`. **NÃO** tocar `docs/legal/`, nem nomes internos de variável/coluna (já são `terapeuta`).

- [ ] **Step 4: Verificar build + testes existentes**
      Run: `corepack pnpm typecheck && corepack pnpm lint && corepack pnpm test`
      Expected: verde (nenhum teste referência o form removido; se algum a11y/unit importava `agendar-form`, removê-lo).

- [ ] **Step 5: Commit**

```bash
git commit -am "refactor(agenda): aposenta form legado de UUID + rename Terapeuta na UI (etapa E)"
```

---

## Task 4: Validar disciplina ∈ vocabulário nas escritas

**Files:**

- Modify: `src/app/(app)/agenda/queries.ts` (`criarRegra`, `criarAvulsa` — validar `disciplina`)
- Test: `src/app/(app)/agenda/semana/actions.int.test.ts`

**Interfaces:**

- Consumes: `carregarConfigClinica(ctx)` → `{ disciplinas: string[], duracaoDisciplina }`.
- Produces: `criarRegra`/`criarAvulsa` rejeitam disciplina fora de `disciplinas` com `ConflitoError` (ou nova `ValidacaoError` reaproveitando o `trata()` da action).

- [ ] **Step 1: Teste falhando** — criar regra com `disciplina:"inexistente"` deve falhar.

```ts
it("rejeita disciplina fora do vocabulário da clínica", async () => {
  await expect(
    criarRegra(ctxCoord, { ...regraValida, disciplina: "inexistente" }),
  ).rejects.toThrow(/disciplina/i);
});
```

- [ ] **Step 2: Rodar — falha** (hoje aceita qualquer texto).
- [ ] **Step 3: Implementar** — no topo de `criarRegra`/`criarAvulsa`, após resolver ctx:

```ts
const { disciplinas } = await carregarConfigClinica(ctx);
if (!disciplinas.includes(dados.disciplina)) {
  throw new ConflitoError("Disciplina não configurada nesta clínica.");
}
```

- [ ] **Step 4: Rodar — passa.**
- [ ] **Step 5: Commit** `feat(agenda): valida disciplina contra vocabulário da clínica (etapa E+F)`

---

# BLOCO E — Ciclo de vida da sessão

## Task 5: Lib pura `transicoes.ts`

**Files:**

- Create: `src/lib/agenda/transicoes.ts`
- Test: `src/lib/agenda/transicoes.test.ts`

**Interfaces:**

- Produces: `transicaoPermitida(de, para): boolean`, `exigeJustificada(estado): boolean`, tipo `SessionEstado`.

- [ ] **Step 1: Teste falhando** — `src/lib/agenda/transicoes.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { transicaoPermitida, exigeJustificada } from "./transicoes";

describe("transicaoPermitida", () => {
  it("agendada → realizada/falta_*/cancelada é permitido", () => {
    for (const p of [
      "realizada",
      "falta_paciente",
      "falta_terapeuta",
      "cancelada",
    ] as const)
      expect(transicaoPermitida("agendada", p)).toBe(true);
  });
  it("agendada → agendada não é transição", () => {
    expect(transicaoPermitida("agendada", "agendada")).toBe(false);
  });
  it("estado terminal nunca transiciona", () => {
    expect(transicaoPermitida("realizada", "cancelada")).toBe(false);
    expect(transicaoPermitida("cancelada", "agendada")).toBe(false);
  });
});
describe("exigeJustificada", () => {
  it("só falta_* é justificável", () => {
    expect(exigeJustificada("falta_paciente")).toBe(true);
    expect(exigeJustificada("falta_terapeuta")).toBe(true);
    expect(exigeJustificada("realizada")).toBe(false);
    expect(exigeJustificada("cancelada")).toBe(false);
  });
});
```

- [ ] **Step 2: Rodar — falha** (`transicoes` não existe). `corepack pnpm test -- transicoes`
- [ ] **Step 3: Implementar** — `src/lib/agenda/transicoes.ts`:

```ts
import { sessionEstado } from "@/db/schema";

export type SessionEstado = (typeof sessionEstado.enumValues)[number];

const TERMINAIS: readonly SessionEstado[] = [
  "realizada",
  "falta_paciente",
  "falta_terapeuta",
  "cancelada",
];

/** Só sessão `agendada` transiciona, e só para um estado terminal. */
export function transicaoPermitida(
  de: SessionEstado,
  para: SessionEstado,
): boolean {
  if (de !== "agendada") return false;
  return TERMINAIS.includes(para);
}

/** `justificada` só é relevante em faltas. */
export function exigeJustificada(estado: SessionEstado): boolean {
  return estado === "falta_paciente" || estado === "falta_terapeuta";
}
```

- [ ] **Step 4: Rodar — passa.**
- [ ] **Step 5: Commit** `feat(agenda): lib pura de transição de estado da sessão (etapa E)`

---

## Task 6: Server action `marcarEstado` (CAS otimista)

**Files:**

- Modify: `src/app/(app)/agenda/actions.ts` (nova `marcarEstado` + wrapper `marcarEstadoAction`)
- Test: `src/app/(app)/agenda/actions.int.test.ts`

**Interfaces:**

- Consumes: `transicaoPermitida`, `exigeJustificada` (Task 5); `withTenant`, `session`, `userRole`.
- Produces:
  - `marcarEstado(ctx, input: MarcarEstadoInput): Promise<{ error?: string; ok?: boolean }>`
  - `MarcarEstadoInput = { sessionId: string; estado: SessionEstado; justificada?: boolean; atendidoPorId?: string | null; modalidade?: "presencial"|"online" }`
  - `marcarEstadoAction(_prev, formData): Promise<{ error?: string; ok?: boolean }>`

- [ ] **Step 1: Testes de integração falhando** — CAS barra 2ª escrita; substituto cross-tenant barrado.

```ts
it("marca realizada uma vez; 2ª chamada (estado já mudou) devolve erro", async () => {
  const id = /* agendar sessão via insert owner ou criarAvulsa */;
  const a = await marcarEstado(ctxCoord, { sessionId: id, estado: "realizada" });
  expect(a.ok).toBe(true);
  const b = await marcarEstado(ctxCoord, { sessionId: id, estado: "falta_paciente" });
  expect(b.error).toMatch(/já foi atualizada/i);
});
it("substituto de outra clínica é rejeitado", async () => {
  const id = /* sessão da clínica A */;
  const r = await marcarEstado(ctxCoord, { sessionId: id, estado: "realizada", atendidoPorId: userClinicaB });
  expect(r.error).toMatch(/substitut|terapeuta/i);
});
```

- [ ] **Step 2: Rodar — falha** (`marcarEstado` não existe). `corepack pnpm test:rls -- agenda/actions.int.test.ts`
- [ ] **Step 3: Implementar** em `actions.ts`:

```ts
import { transicaoPermitida, exigeJustificada } from "@/lib/agenda/transicoes";

export type MarcarEstadoInput = {
  sessionId: string;
  estado: SessionEstado;
  justificada?: boolean;
  atendidoPorId?: string | null;
  modalidade?: (typeof sessionModalidade.enumValues)[number];
};

export async function marcarEstado(
  ctx: TenantContext,
  input: MarcarEstadoInput,
): Promise<{ error?: string; ok?: boolean }> {
  requireRole(ctx, "coordenador", "admin_recepcao", "terapeuta");

  if (!transicaoPermitida("agendada", input.estado)) {
    return { error: "Transição de estado inválida." };
  }

  // Substituto (quem realmente atendeu) tem que ser terapeuta da clínica ativa.
  if (input.atendidoPorId) {
    const ok = await withTenant(ctx, (tx) =>
      tx
        .select({ id: userRole.userId })
        .from(userRole)
        .where(
          and(
            eq(userRole.userId, input.atendidoPorId!),
            eq(userRole.clinicId, ctx.clinicId),
            eq(userRole.papel, "terapeuta"),
          ),
        )
        .limit(1),
    );
    if (ok.length === 0)
      return { error: "Substituto não é terapeuta desta clínica." };
  }

  const just = exigeJustificada(input.estado)
    ? (input.justificada ?? false)
    : null;

  const rows = await withTenant(ctx, (tx) =>
    tx
      .update(session)
      .set({
        estado: input.estado,
        justificada: just,
        ...(input.atendidoPorId !== undefined
          ? { atendidoPorId: input.atendidoPorId }
          : {}),
        ...(input.modalidade ? { modalidade: input.modalidade } : {}),
      })
      .where(
        and(
          eq(session.id, input.sessionId),
          eq(session.estado, "agendada"), // CAS: só transiciona de agendada
        ),
      )
      .returning({ id: session.id }),
  );

  if (rows.length === 0) {
    return {
      error:
        "Esta sessão já foi atualizada por outra pessoa. Recarregue a página.",
    };
  }
  return { ok: true };
}

export async function marcarEstadoAction(
  _prev: { error?: string; ok?: boolean },
  formData: FormData,
): Promise<{ error?: string; ok?: boolean }> {
  const ctx = await getTenantContext();
  const atendido = String(formData.get("atendidoPorId") ?? "").trim();
  try {
    const r = await marcarEstado(ctx, {
      sessionId: String(formData.get("sessionId") ?? "").trim(),
      estado: formData.get("estado") as SessionEstado,
      justificada: formData.get("justificada") === "true",
      atendidoPorId: atendido === "" ? undefined : atendido,
      modalidade:
        (formData.get("modalidade") as MarcarEstadoInput["modalidade"]) ||
        undefined,
    });
    if (r.ok) revalidatePath("/agenda");
    return r;
  } catch (err) {
    if (err instanceof RoleError)
      return { error: "Você não tem permissão para atualizar a sessão." };
    console.error("marcarEstadoAction:", err);
    return { error: "Não foi possível atualizar a sessão." };
  }
}
```

(Adicionar `sessionModalidade` ao import de `@/db/schema` em `actions.ts`.)

- [ ] **Step 4: Rodar — passa.**
- [ ] **Step 5: Commit** `feat(agenda): marcarEstado com CAS otimista + validação de substituto (etapa E)`

---

## Task 7: UI "Gerir sessão" na grade do dia

**Files:**

- Create: `src/app/(app)/agenda/gerir-sessao.tsx` (client)
- Modify: `src/app/(app)/agenda/page.tsx` (renderizar `<GerirSessao>` no card quando `estado==="agendada"` e o usuário pode gerir; passar `terapeutas` da clínica)
- Modify: `src/app/(app)/agenda/actions.ts` — expor `listarTerapeutas(ctx)` se não existir (para o combobox de substituto); reusar de `queries.ts` se já houver.
- Test: `src/app/(app)/agenda/gerir-sessao.a11y.test.tsx`

**Interfaces:**

- Consumes: `marcarEstadoAction` (Task 6); `ComboboxEntidade`/`Opcao`; DS `Dialog`, `Select`, `Button`, `Field`, `Alert`.
- Produces: `GerirSessao({ sessionId, terapeutas }: { sessionId: string; terapeutas: Opcao[] })`.

**Contrato de UI** (segue o padrão de `popover-alocar.tsx` + `checkin-button.tsx`):

- Botão "Gerir" abre `Dialog`. Dentro: `Select` de estado (Realizada / Falta do paciente / Falta do terapeuta / Cancelada) com hidden input espelho (Radix não emite no FormData).
- Quando estado ∈ falta_* → toggle `justificada` (checkbox do DS ou `Button aria-pressed`), hidden `justificada`.
- `ComboboxEntidade label="Substituto (quem atendeu)"` opcional (terapeutas), hidden `atendidoPorId`.
- `Select` de modalidade (Presencial/Online), hidden `modalidade`.
- `<form action={formAction}>` com `useActionState(marcarEstadoAction, {})`; hidden `sessionId`. Erro via `<Alert severidade="erro">` (como popover-alocar) — a mensagem de CAS ("já foi atualizada, recarregue") aparece aqui.
- `Button type="submit"` "Salvar".

- [ ] **Step 1: Teste a11y falhando** — `gerir-sessao.a11y.test.tsx` no padrão dos `*.a11y.test.tsx` (`vi.mock("server-only")`, `vi.mock("@/db/client")`, import dinâmico, helper `semViolacoes` com axe tags wcag + regras `region`/`color-contrast`/etc desligadas). Testa: axe sem violações; `getByRole("button",{name:/gerir/i})` abre dialog; select de estado presente.
- [ ] **Step 2: Rodar — falha** (componente não existe). `corepack pnpm test -- gerir-sessao`
- [ ] **Step 3: Implementar** `gerir-sessao.tsx` conforme o contrato acima (espelhar imports/estrutura de `popover-alocar.tsx`).
- [ ] **Step 4: Ligar na grade** — em `page.tsx`, no `<Cluster>` de ações do card (103-112), quando `s.estado === "agendada"` e `ctx.role !== "terapeuta" || s.terapeutaId === ctx.userId`, renderizar `<GerirSessao sessionId={s.id} terapeutas={terapeutas} />`. Carregar `terapeutas` uma vez na página (server) via `listarTerapeutas(ctx)`.
- [ ] **Step 5: Rodar a11y + typecheck + lint — verde.**
- [ ] **Step 6: Commit** `feat(agenda): UI gerir sessão (estado+substituto+modalidade) na grade (etapa E)`

---

## Task 8: Reposição rastreável

**Files:**

- Modify: `src/app/(app)/agenda/semana/actions.ts` + `queries.ts` (`criarAvulsa`/`NovaAvulsa` aceita `repostaDe`)
- Modify: `src/app/(app)/agenda/semana/page.tsx` (ler `searchParams` de reposição → props de prefill)
- Modify: `src/app/(app)/agenda/semana/semana-cliente.tsx` (aceitar prefill: eixo+entidade+disciplina fixados, hidden `repostaDe`)
- Modify: `src/app/(app)/agenda/gerir-sessao.tsx` (botão "Repor" em faltas → `Link` para `/agenda/semana?...`)
- Test: `src/app/(app)/agenda/semana/actions.int.test.ts`

**Interfaces:**

- Produces: `NovaAvulsa` ganha `repostaDe?: string`; `criarAvulsa` grava `session.repostaDe`.

- [ ] **Step 1: Teste int falhando** — criar avulsa com `repostaDe` grava o vínculo.

```ts
it("reposição grava repostaDe apontando a falta original", async () => {
  const faltaId = /* sessão falta_paciente da clínica A */;
  await criarAvulsa(ctxCoord, { ...avulsaValida, repostaDe: faltaId });
  const [nova] = await withTenant(ctxCoord, tx =>
    tx.select({ repostaDe: session.repostaDe }).from(session)
      .where(eq(session.repostaDe, faltaId)).limit(1));
  expect(nova?.repostaDe).toBe(faltaId);
});
```

- [ ] **Step 2: Rodar — falha** (`repostaDe` ignorado). `corepack pnpm test:rls -- semana/actions.int.test.ts`
- [ ] **Step 3: Implementar** — `NovaAvulsa` + `criarAvulsa` (queries.ts) passam `repostaDe`; `criarAvulsaAction` lê `formData.get("repostaDe")`; `PopoverAlocar` renderiza `<input type="hidden" name="repostaDe" value={repostaDe ?? ""} />` quando recebido via prop.
- [ ] **Step 4: Prefill via searchParams** — `semana/page.tsx` (server) lê `searchParams` (`repor`, `patientId`, `terapeutaId`, `disciplina`) e passa como props `prefill?: { repostaDe, patientId, terapeutaId, disciplina }` ao `semana-cliente`. Quando `prefill` presente: fixa `eixo="terapeuta"`, `entidade` = terapeuta do prefill, e ao abrir o slot o `PopoverAlocar` recebe paciente+disciplina+`repostaDe` fixados. `terapeutaId` do prefill = **previsto** da falta (editável no calendário).
- [ ] **Step 5: Botão "Repor"** — em `gerir-sessao.tsx`, quando a sessão é `falta_*`, mostrar `Link` "Repor" para `/agenda/semana?repor={sessionId}&patientId={patientId}&terapeutaId={terapeutaId}&disciplina={disciplina}`. (Requer que a grade passe `patientId`/`terapeutaId`/`disciplina` ao `GerirSessao` — estender props e `SessaoDoDia`/`listarSessoesDoDia` para incluir `patientId` e `disciplina`.)
- [ ] **Step 6: Rodar testes + typecheck + lint — verde.**
- [ ] **Step 7: Commit** `feat(agenda): reposição rastreável via /semana pré-preenchido + repostaDe (etapa E)`

---

## Task 9: Listas de pendências na grade

**Files:**

- Modify: `src/app/(app)/agenda/actions.ts` (ou `queries.ts`): `pendentesDeConsolidacao(ctx)`, `reposicoesPendentes(ctx)`
- Modify: `src/app/(app)/agenda/page.tsx` (renderizar as duas listas no topo, para coordenador/recepção)
- Test: `src/app/(app)/agenda/actions.int.test.ts`

**Interfaces:**

- Produces:
  - `pendentesDeConsolidacao(ctx): Promise<SessaoDoDia[]>` — `estado='agendada' AND agendada_para < now()`.
  - `reposicoesPendentes(ctx): Promise<SessaoDoDia[]>` — sessões `falta_paciente|falta_terapeuta` sem nenhuma `session` filha com `repostaDe = id`.

- [ ] **Step 1: Teste int falhando** — falta sem filho aparece em reposições pendentes; falta com filho, não.

```ts
it("falta sem reposição aparece como pendente; com reposição, some", async () => {
  const semRepor = /* falta_paciente sem filho */;
  const comRepor = /* falta_paciente com uma session filha repostaDe */;
  const pend = await reposicoesPendentes(ctxCoord);
  const ids = pend.map(s => s.id);
  expect(ids).toContain(semRepor);
  expect(ids).not.toContain(comRepor);
});
```

- [ ] **Step 2: Rodar — falha.**
- [ ] **Step 3: Implementar** — `reposicoesPendentes` via `NOT EXISTS (SELECT 1 FROM session f WHERE f.reposta_de = session.id)` + `estado IN ('falta_paciente','falta_terapeuta')`. `pendentesDeConsolidacao` via `estado='agendada' AND agendada_para < now()`. Ambas `withTenant` (RLS escopa clínica).
- [ ] **Step 4: UI** — em `page.tsx`, para `podeAgendar`, duas `<Stack como="section">` no topo com as listas (reusa o mesmo card visual das sessões do dia; cada item com link "Abrir"/"Repor"/"Gerir" conforme). Vazio → nada renderizado (sem `Alert` de vazio para não poluir).
- [ ] **Step 5: a11y** — estender `agenda/a11y.test.tsx` cobrindo as seções novas.
- [ ] **Step 6: Rodar tudo + typecheck + lint — verde.**
- [ ] **Step 7: Commit** `feat(agenda): pendentes de consolidação + reposições pendentes na grade (etapa E)`

---

# BLOCO F — Métricas por disciplina

## Task 10: Lib pura `horas.ts`

**Files:**

- Create: `src/lib/agenda/horas.ts`
- Test: `src/lib/agenda/horas.test.ts`

**Interfaces:**

- Produces:
  - `horasAgendadasPorDisciplina(regras: RegraAtiva[]): Record<string, number>`
  - `horasRealizadasPorDisciplina(sessoes: SessaoRealizada[], agora: Date): Record<string, number>`
  - `defasagem(alvo: number, agendado: number): number`
  - `temDefasagemSustentada(semanas: { alvo: number; agendado: number }[], limiar?: number): boolean`
  - `alocadoTerapeuta(regras: RegraAtiva[]): number`
  - `vagoTerapeuta(capacidade: number, alocado: number, horasBloqueadas: number): number`
  - Tipos: `RegraAtiva = { disciplina: string; duracaoMin: number }`, `SessaoRealizada = { disciplina: string; duracaoMin: number; agendadaPara: Date }`.

- [ ] **Step 1: Teste falhando** — `horas.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import {
  horasAgendadasPorDisciplina,
  horasRealizadasPorDisciplina,
  defasagem,
  temDefasagemSustentada,
  alocadoTerapeuta,
  vagoTerapeuta,
} from "./horas";

describe("horasAgendadasPorDisciplina", () => {
  it("soma duracaoMin por disciplina ÷ 60", () => {
    const r = horasAgendadasPorDisciplina([
      { disciplina: "aba", duracaoMin: 60 },
      { disciplina: "aba", duracaoMin: 30 },
      { disciplina: "fono", duracaoMin: 30 },
    ]);
    expect(r.aba).toBeCloseTo(1.5);
    expect(r.fono).toBeCloseTo(0.5);
  });
});
describe("horasRealizadasPorDisciplina", () => {
  it("média por semana ativa desde a 1ª sessão da disciplina", () => {
    const agora = new Date("2026-07-20T12:00:00-03:00");
    // 1ª sessão há ~2 semanas, 2 sessões de 60min → 2h em 2 semanas = 1h/sem
    const r = horasRealizadasPorDisciplina(
      [
        {
          disciplina: "aba",
          duracaoMin: 60,
          agendadaPara: new Date("2026-07-06T12:00:00-03:00"),
        },
        {
          disciplina: "aba",
          duracaoMin: 60,
          agendadaPara: new Date("2026-07-13T12:00:00-03:00"),
        },
      ],
      agora,
    );
    expect(r.aba).toBeCloseTo(1, 1);
  });
  it("paciente novo (1ª semana) não infla defasagem: divide por ≥1 semana", () => {
    const agora = new Date("2026-07-07T12:00:00-03:00");
    const r = horasRealizadasPorDisciplina(
      [
        {
          disciplina: "aba",
          duracaoMin: 60,
          agendadaPara: new Date("2026-07-06T12:00:00-03:00"),
        },
      ],
      agora,
    );
    expect(r.aba).toBeCloseTo(1, 1);
  });
});
describe("defasagem / sustentada", () => {
  it("defasagem = alvo - agendado", () => {
    expect(defasagem(12, 8)).toBe(4);
  });
  it("alerta quando agendado<alvo por ≥ limiar semanas", () => {
    expect(
      temDefasagemSustentada(
        [
          { alvo: 12, agendado: 8 },
          { alvo: 12, agendado: 10 },
        ],
        2,
      ),
    ).toBe(true);
    expect(
      temDefasagemSustentada(
        [
          { alvo: 12, agendado: 8 },
          { alvo: 12, agendado: 12 },
        ],
        2,
      ),
    ).toBe(false);
  });
});
describe("terapeuta", () => {
  it("alocado = Σ duracaoMin ÷ 60; vago = capacidade - alocado - bloqueado", () => {
    expect(
      alocadoTerapeuta([
        { disciplina: "aba", duracaoMin: 60 },
        { disciplina: "fono", duracaoMin: 60 },
      ]),
    ).toBeCloseTo(2);
    expect(vagoTerapeuta(40, 30, 5)).toBe(5);
  });
});
```

- [ ] **Step 2: Rodar — falha.** `corepack pnpm test -- horas`
- [ ] **Step 3: Implementar** `src/lib/agenda/horas.ts`:

```ts
export type RegraAtiva = { disciplina: string; duracaoMin: number };
export type SessaoRealizada = {
  disciplina: string;
  duracaoMin: number;
  agendadaPara: Date;
};

const MS_SEMANA = 7 * 24 * 60 * 60 * 1000;

function somaPorDisciplina(itens: RegraAtiva[]): Record<string, number> {
  const acc: Record<string, number> = {};
  for (const i of itens)
    acc[i.disciplina] = (acc[i.disciplina] ?? 0) + i.duracaoMin;
  return acc;
}

export function horasAgendadasPorDisciplina(
  regras: RegraAtiva[],
): Record<string, number> {
  const min = somaPorDisciplina(regras);
  return Object.fromEntries(Object.entries(min).map(([d, m]) => [d, m / 60]));
}

/** Média de horas realizadas por semana ativa, POR disciplina, contando as
 * semanas desde a 1ª sessão daquela disciplina (mínimo 1) — paciente novo não
 * aparece com aderência falsa. */
export function horasRealizadasPorDisciplina(
  sessoes: SessaoRealizada[],
  agora: Date,
): Record<string, number> {
  const porDisc = new Map<string, SessaoRealizada[]>();
  for (const s of sessoes) {
    const arr = porDisc.get(s.disciplina) ?? [];
    arr.push(s);
    porDisc.set(s.disciplina, arr);
  }
  const out: Record<string, number> = {};
  for (const [disc, arr] of porDisc) {
    const primeira = Math.min(...arr.map((s) => s.agendadaPara.getTime()));
    const semanas = Math.max(
      1,
      Math.ceil((agora.getTime() - primeira) / MS_SEMANA),
    );
    const horas = arr.reduce((acc, s) => acc + s.duracaoMin, 0) / 60;
    out[disc] = horas / semanas;
  }
  return out;
}

export function defasagem(alvo: number, agendado: number): number {
  return alvo - agendado;
}

export function temDefasagemSustentada(
  semanas: { alvo: number; agendado: number }[],
  limiar = 2,
): boolean {
  let seq = 0;
  for (const s of semanas) {
    if (s.agendado < s.alvo) {
      seq++;
      if (seq >= limiar) return true;
    } else seq = 0;
  }
  return false;
}

export function alocadoTerapeuta(regras: RegraAtiva[]): number {
  return regras.reduce((acc, r) => acc + r.duracaoMin, 0) / 60;
}

export function vagoTerapeuta(
  capacidade: number,
  alocado: number,
  horasBloqueadas: number,
): number {
  return capacidade - alocado - horasBloqueadas;
}
```

- [ ] **Step 4: Rodar — passa.**
- [ ] **Step 5: Commit** `feat(agenda): lib pura de cálculo de horas por disciplina (etapa F)`

---

## Task 11: Queries de horas

**Files:**

- Create: `src/app/(app)/agenda/horas-queries.ts` (server, ctx-accepting — FORA de `"use server"`, padrão do refactor de segurança de 2026-07-17)
- Test: `src/app/(app)/agenda/horas-queries.int.test.ts`

**Interfaces:**

- Consumes: `horas.ts` (Task 10); `agendamentoRecorrente`, `session`, `patientAlvoDisciplina`, janelas/bloqueios.
- Produces:
  - `carregarHorasPaciente(ctx, patientId): Promise<{ disciplina: string; alvo: number | null; agendado: number; realizado: number; alerta: boolean }[]>`
  - `carregarHorasTerapeuta(ctx, terapeutaId): Promise<{ capacidade: number; alocado: number; vago: number; pacientes: { id: string; nome: string }[] }>`

- [ ] **Step 1: Teste int falhando** — paciente com alvo 12 e regras somando 8 mostra defasagem.
- [ ] **Step 2: Rodar — falha.**
- [ ] **Step 3: Implementar** — juntar por disciplina: alvo vigente (`patient_alvo_disciplina` com `vigencia_fim IS NULL OR >= hoje`), agendado (regras `status='ativo'` do paciente → `horasAgendadasPorDisciplina`), realizado (`session` `estado='realizada'` do paciente → `horasRealizadasPorDisciplina`). Terapeuta: capacidade via `horasDisponiveisSemana` (já existe em `@/lib/agenda/janela`), alocado via regras ativas do terapeuta, vago via `vagoTerapeuta` descontando bloqueios do período.
- [ ] **Step 4: Rodar — passa.**
- [ ] **Step 5: Commit** `feat(agenda): queries de horas paciente/terapeuta (etapa F)`

---

## Task 12: UI perfil paciente — seção "Horas"

**Files:**

- Create: `src/app/(app)/pacientes/[id]/horas/page.tsx`
- Modify: `src/app/(app)/pacientes/[id]/page.tsx` (adicionar `<Link>` "Horas" no bloco de abas 53-72)
- Test: `src/app/(app)/pacientes/[id]/horas/horas.a11y.test.tsx`

**Interfaces:**

- Consumes: `carregarHorasPaciente` (Task 11); DS `Stack`, `Alert`.

- [ ] **Step 1: Teste a11y falhando** — tabela por disciplina + alerta acessível (axe).
- [ ] **Step 2: Rodar — falha.**
- [ ] **Step 3: Implementar** rota-filha seguindo o padrão de `briefing/`/`ausencias/` (server component, `requireRole(ctx,"terapeuta","coordenador")`). Corpo: tabela `<table>` semântica (cabeçalho Disciplina | Alvo | Agendado | Realizado), uma linha por disciplina; quando `alerta`, `<Alert severidade="info" titulo="Abaixo do prescrito há ≥ 2 semanas">`. Aba "Horas" ativa/inativa com as mesmas classes do bloco 53-72.
- [ ] **Step 4: Rodar a11y + typecheck + lint — verde.**
- [ ] **Step 5: Commit** `feat(pacientes): aba Horas — alvo vs agendado vs realizado por disciplina (etapa F)`

---

## Task 13: UI perfil terapeuta — capacidade vs alocado vs vago

**Files:**

- Modify: `src/app/(app)/equipe/[id]/page.tsx` (novo bloco de horas + lista de pacientes fixos)
- Create: `src/app/(app)/equipe/[id]/horas-terapeuta.tsx` se precisar de client (provavelmente server puro basta)
- Test: estender `src/app/(app)/equipe/[id]/a11y.test.tsx`

**Interfaces:**

- Consumes: `carregarHorasTerapeuta` (Task 11); DS layout.

- [ ] **Step 1: Teste a11y falhando** — bloco capacidade/alocado/vago + lista de pacientes acessível.
- [ ] **Step 2: Rodar — falha.**
- [ ] **Step 3: Implementar** — em `equipe/[id]/page.tsx`, após o header (já mostra "Disponibilidade oferecida", linha 46), chamar `carregarHorasTerapeuta(ctx, id)` e renderizar um bloco: capacidade | alocado | vago (reusa `Stat` do DS se couber, senão `Split`/`Cluster` com tokens) + `<ul>` de pacientes fixos com link para `/pacientes/[id]/horas`.
- [ ] **Step 4: Rodar a11y + typecheck + lint — verde.**
- [ ] **Step 5: Commit** `feat(equipe): perfil do terapeuta com capacidade vs alocado vs vago (etapa F)`

---

# Fechamento

- [ ] **Verificação final:** `corepack pnpm typecheck && corepack pnpm lint && corepack pnpm test && corepack pnpm test:rls` — tudo verde.
- [ ] **BACKLOG:** registrar o que ficou como dívida (faturamento/glosa, grupo/co-terapia, cron de consolidação, limiar de defasagem por clínica) e fechar os itens da Etapa E+F.
- [ ] **graphify update .** para o grafo refletir o código novo.
- [ ] **PR** com descrição contextual (decisões D-E1..E8/F1-F2, endurecimentos do pre-mortem, backfill `desconhecida`) — Jules só vê o diff.

## Notas de execução

- **Ordem obrigatória:** Task 1 antes de 4/11 (disciplina NOT NULL + vocab). Task 2 antes de 8 (recepção). Task 5 antes de 6. Task 6 antes de 7. Task 10 antes de 11 antes de 12/13.
- **Paralelizável:** Bloco F (10→11→12/13) é independente do Bloco E depois que Task 1 passa — 12 e 13 podem correr em paralelo. Tasks 3 (rename) roda a qualquer momento.
- **DB para int-tests:** subir `infra/docker-compose.yml` (porta 5433) e exportar `DATABASE_URL`/`MIGRATION_DATABASE_URL` antes de `pnpm test:rls`. Se `db:migrate` falhar por desync, aplicar SQL à mão via psql (memória `dev-db-migrate-desync`).
</content>
