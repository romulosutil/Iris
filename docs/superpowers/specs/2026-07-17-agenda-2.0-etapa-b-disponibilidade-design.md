# Agenda 2.0 — Etapa B: Disponibilidade + bloqueio + perfil-base do terapeuta (design)

> Data: 2026-07-17 · Status: **aprovado para virar plano de implementação**
> Addendum de execução do design mãe
> (`2026-07-16-agenda-2.0-disciplina-aware-design.md`, §7 Etapa B).
> Validado sob a persona de Coordenador de terapia.

## 0. Pré-condição — Etapa A concluída

Auditoria em código confirma Etapa A 100% implementada: schema
(`src/db/schema.ts`), migrations `0021`–`0035` (tabelas novas, `btree_gist`,
EXCLUDE gist anti-overbook, enum `session_estado` recriado, FKs compostas,
RLS `WITH CHECK`), alvo-por-disciplina gravado na mesma transação do cadastro
de paciente, e 9 arquivos de teste de integração. **Etapa B não toca DDL** —
as tabelas `janela_trabalho` e `bloqueio` já existem no banco. B é a camada
de **UI + server actions + rota nova do terapeuta**.

## 1. Escopo

### Entrega
- Editor de disponibilidade do terapeuta (grava `janela_trabalho`).
- Bloqueios nos 3 escopos (`bloqueio`): terapeuta, paciente, clínica.
- Rota nova `/equipe/[id]` (perfil-base do terapeuta) com **capacidade/sem**.
- Aba **Ausências** no perfil do paciente (`/pacientes/[id]`).
- Seção mínima **Clínica → Feriados** (casa do bloqueio escopo=clínica).

### Adia (dívida explícita, registrada no BACKLOG)
- **Alocado/vago** e **lista de pacientes fixos** no perfil do terapeuta —
  dependem de regras (`agendamento_recorrente`) e sessões → Etapa F.
- Materialização de ocorrências → Etapa D.
- Calendário semanal e alocação → Etapa C.
- Indicador "de férias esta semana" no perfil → Etapa F (junto de vago).

## 2. Decisões desta sessão

| # | Decisão |
| --- | --- |
| B1 | Perfil do terapeuta em B mostra **só capacidade/sem** (Σ janelas ÷ 60); alocado/vago adiados para F |
| B2 | Bloqueio de **paciente** mora no perfil do paciente (aba Ausências); de **terapeuta** no `/equipe/[id]`; de **clínica** em Clínica → Feriados |
| **D3-revisada** | Editor de disponibilidade = **grade visual click-drag**, revisando a D3 original (selects de hora). **Obrigatoriamente operável por teclado + leitor de tela**; drag é enhancement. Motivo: acessibilidade é compromisso de 1ª classe do produto |
| B3 | Escopo=clínica ganha casa mínima agora (Clínica → Feriados), não é adiado |

> D3-revisada substitui a decisão D3 do design mãe. O design mãe deve ser
> atualizado para apontar para este addendum.

## 3. Dados — sem DDL nova; server actions + invariantes

Nenhuma migration nova. B adiciona server actions e uma query derivada.
Invariantes que o coordenador exige e o design mãe não escreveu:

- **I-B1 — faixas não sobrepõem:** ao salvar as janelas de um dia, faixas
  sobrepostas ou encostadas (`horaFim` de uma == `horaInicio` da seguinte)
  **fundem** numa só (08–12 + 10–14 → 08–14; 08–12 + 12–17 → 08–17). Sem
  isso a capacidade/sem conta hora dobrada. Lógica pura, testável isolada.
- **I-B2 — só coordenador edita** disponibilidade e bloqueio. RLS já isola o
  tenant; falta o **gate de papel** na action (checar papel coordenador via
  `user_role`). Terapeuta autenticado vê o próprio `/equipe/[id]` em modo
  leitura; não-coordenador não pode `create/update/delete` janela nem bloqueio.
- **I-B3 — snap a `passoGradeMin`** (default 30 na clínica): a grade pinta em
  blocos de `passoGradeMin`; `horaInicio`/`horaFim` salvos são sempre
  múltiplos do passo.
- **I-B4 — `horaFim > horaInicio`** por faixa (design mãe §3.3; a grade
  garante por construção, a action revalida).
- **I-B5 — bloqueio `dataFim >= dataInicio`**; escopo casa com o id
  obrigatório (escopo=terapeuta ⇒ `terapeutaId`; escopo=paciente ⇒
  `patientId`; escopo=clínica ⇒ ambos null). Já é a forma da tabela; a action
  valida antes de inserir.

## 4. Superfícies e arquitetura de informação

| Rota / lugar | Conteúdo | Observações |
| --- | --- | --- |
| `/equipe/[id]` **(nova)** | header do terapeuta (nome, papel) · **capacidade/sem** · editor de janelas · lista de bloqueios do terapeuta | empty-state se sem janela (CTA "configurar disponibilidade"). Edição só coordenador (I-B2) |
| `/pacientes/[id]` → aba **Ausências** | bloqueios escopo=paciente (férias/viagem) | mata a falta-fantasma que dispararia "abaixo do prescrito" |
| **Clínica → Feriados** (seção mínima nova) | bloqueios escopo=clínica (lista + adicionar intervalo + motivo) | registro único vale para todos; feriado nacional |

Cada superfície reusa o padrão de server action + revalidação das fases
anteriores. Componentes de calendário/alocação **não** entram aqui (Etapa C).

## 5. Grade de disponibilidade (a11y-first)

- Estrutura: `role="grid"`, 7 linhas (dom→sáb) × colunas de largura
  `passoGradeMin`, cobrindo a faixa de horas útil da clínica.
- Célula = toggle "trabalha / não trabalha".
- **Teclado (requisito, não enhancement):** setas navegam a grade;
  Enter/Espaço alterna a célula focada; Shift+seta pinta em faixa contínua.
  Cada célula anuncia dia + intervalo + estado ao leitor de tela.
- **Drag do mouse** = enhancement por cima do modelo de teclado (mesma
  mutação de estado).
- **"Copiar dia":** botão que replica as faixas de um dia (ex. segunda) para
  os demais dias úteis — a dor real do coordenador é montar 5 dias idênticos.
- Persistência: as células pintadas de cada dia viram faixas contíguas
  (`janela_trabalho`), aplicando I-B1 (fusão) no salvamento. Sáb/dom vazios
  por default.

## 6. Cálculo exposto em B

- **Capacidade/sem do terapeuta** = Σ (`horaFim` − `horaInicio`) de todas as
  janelas ÷ 60 (design mãe §5). Derivado, sem denormalizar. Bloqueios **não**
  entram aqui (capacidade é bruta semanal; desconto de bloqueio compõe *vago*,
  que é Etapa F).

## 7. Testes

- **Unit (lógica pura):** fusão de faixas (I-B1, incluindo encostadas e
  sobrepostas parciais), cálculo de capacidade/sem, snap a `passoGradeMin`.
- **Integração (com Postgres):** gate de papel coordenador nas actions de
  janela e bloqueio (I-B2) — não-coordenador barrado; validação de escopo do
  bloqueio (I-B5). RLS de tenant já coberta na Etapa A.
- **a11y (axe):** grade de disponibilidade, `/equipe/[id]`, aba Ausências,
  seção Feriados — padrão das fases anteriores. A grade precisa passar
  navegável 100% por teclado.

## 8. Riscos residuais

- **D3-revisada (grade vs selects):** o risco é a grade drag virar armadilha
  de a11y. Mitigado tornando o modelo de teclado o caminho primário e drag o
  secundário; axe + teste de teclado obrigatórios antes de fechar a etapa.
- **Editar janela com sessões futuras:** em B ainda não há sessões (vêm em C),
  então encolher janela é seguro agora. A action é escrita para **não assumir**
  "sem sessões"; a cascata real (reagendar/avisar) é tratada na Etapa C/D.
- **Seção Feriados mínima:** deliberadamente enxuta; se a clínica ganhar uma
  página de configuração ampla depois, Feriados migra para lá sem mudar o dado.
