# Resumo diário — 2026-08-10

**Data**: 2026-08-10

## Commits

| Hash      | Assunto                                                                         |
| --------- | ------------------------------------------------------------------------------- |
| `e4bae4c` | tech(billing): ativação do Pix declara o que cobra (D22)                        |
| `fb09940` | tech(billing): ativação descarta vínculo de outro gateway (D25)                 |
| `d89c8f2` | docs(claude): regra de atomização e checkpoint de contexto por sessão           |
| `c2e5b4c` | tech(billing): desabilita o Mercado Pago como gateway ativo (D24, D26-D28)      |
| `b456a5d` | tech(billing): erro de provedor desconhecido não cita BILLING_PROVIDER          |
| `eb8eda7` | tech(billing): coluna de documento na clínica e provedor sem default (D29, D30) |
| `994572d` | tech(billing): definer de documento da clínica e CHECK de provedor              |
| `fdf8963` | tech(billing): backfill do provedor fantasma nas assinaturas existentes         |
| `6501e42` | test(billing): provedor explícito nas assinaturas de teste e trava do default   |
| `7200a8d` | docs(billing): status e evidência medida das tarefas T1-T4 e T9 (#36)           |
| `5bbe43c` | feat(billing): validador de CPF ou CNPJ para o documento da clínica             |
| `26110af` | feat(billing): persiste o id do cliente do gateway (D32)                        |
| `ac0dc38` | feat(billing): ativação envia o CPF/CNPJ da clínica ao gateway (D30)            |

**Total**: 13 commits.

## Checkpoints

`checkpoint.md` gravado na sessão de especificação (2ª do dia), superado
pela execução de T1-T9 na sessão seguinte — não commitado (convenção do
`CLAUDE.md`: checkpoint é ajuda de sessão, não artefato do repo).

## Sessões

3 sessões no dia: (1) fechamento do D22 + regra de gestão de tokens no
`CLAUDE.md`; (2) especificação da issue #36 (spec + design + tasks,
débitos D29-D32 abertos); (3) execução da Fase A — T1, T2, T3 (escrito),
T4, T5, T6, T7, T9.

## Resumo

Dia dividido em três blocos. De manhã, o D22 fechou (Pix Automático passou
a declarar o valor de R$ 0,01 antes do QR Code) e o `CLAUDE.md` ganhou a
regra de atomização/checkpoint derivada do post-mortem daquele débito. À
tarde, a virada de chave Mercado Pago → Asaas se completou no código (D24,
D25, D26-D28) e, ao medir produção, ficou claro que a ativação nunca
completava de fato — faltava CPF/CNPJ no modelo de dados. A issue #36 foi
especificada (spec + design + 18 tarefas em `tasks.md`) com os débitos D29
(default do banco sobrevivendo à remoção do default no código), D30
(documento fiscal ausente), D31 (página de dados da clínica, sem urgência)
e D32 (id do cliente do gateway nunca persistido). À noite, a Fase A da
execução: migração `0090` (coluna `clinic.cpf_cnpj`, `provider` nullable
sem default, função `SECURITY DEFINER`, CHECK de provedor), validador único
de CPF/CNPJ, persistência de `provider_customer_id`, e `assinatura/logic.ts`
passando a validar, gravar e enviar o documento ao Asaas — fechando D29,
D30 (parcial — falta o campo no formulário, T8) e D32. Suíte de testes
cresceu de 1076 para 1093 (unit) + 808 (`test:rls`), sem nenhuma perda.
