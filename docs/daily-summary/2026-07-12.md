# Resumo diário — 2026-07-12

**Date**: 2026-07-12

**Commits**:

- `b56e84f` fix(build): lazy-init conexões DB p/ build Docker sem env de runtime
- `b9f4030` fix(build): desliga standalone no build local do Windows (EPERM em symlink)
- `6a33060` docs(backlog): marca Fase 2 concluída (Plano 4 entregue, PR #23)
- `fa9653e` feat(fase-2): seed de demonstração + entrada do diário na agenda (Plano 4)
- `371736c` fix(metas): move schemas Zod para fora do módulo "use server" (build)
- `084423d` chore(docs): centraliza instruções de agentes no CLAUDE.md
- `ab6a831` fix(metas): allow null in disciplina in atualizarSchema
- `e81fa9a` fix(metas): deduplicate milestoneIds in criarSchema
- `43edde6` feat(metas): CRUD de metas + critério de domínio N/M + transição domi

**Checkpoints**: Nenhum (diretório `docs/checkpoints/` não existe).

**Session count**: 13 sessões registradas em `~/.claude/session-log.txt`.

**Summary**: Fase 2 concluída — CRUD de metas com critério de domínio N/M e
transição de status entregue, seed de demonstração e entrada do diário na
agenda finalizados (Plano 4, PR #23). Rodada de correções de build fechou o
dia: schemas Zod movidos p/ fora de `"use server"`, standalone desligado no
build local do Windows (EPERM em symlink) e lazy-init das conexões DB p/
build Docker sem env de runtime.
